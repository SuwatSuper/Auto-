# 📘 คู่มือการใช้งาน Kingdom Prime (ฉบับละเอียด)

ระบบเทรด BTC/THB อัตโนมัติบน Bitkub — **เริ่มที่ paper (จำลอง) ปลอดภัยก่อน**, ต่อเงินจริงได้
เมื่อพร้อม (ผ่าน 4 ประตูกันพลาด). ทุกตัวเลขมาจากตลาดจริง ไม่มีข้อมูลปลอม.

> ⚠️ **ความจริงที่ต้องรู้ก่อน:** ไม่มีระบบไหนการันตีกำไรได้ รวมถึงระบบนี้. เป้า 5-10%/วัน
> เป็น "เป้าหมาย" ไม่ใช่การการันตี — ตลาด + ค่าธรรมเนียม Bitkub เป็นผู้กำหนดผลจริง.

---

## 1) ระบบนี้ทำอะไรได้บ้าง (สรุปความสามารถ — ทดสอบแล้วทุกข้อ)
| ความสามารถ | ได้ไหม | หมายเหตุ |
|---|---|---|
| ซื้อเหรียญ (BUY) | ✅ ได้ | paper = จำลอง / live = เงินจริงบน Bitkub |
| ขายเหรียญ (SELL/ปิด) | ✅ ได้ | ปิดทั้งโพซิชัน |
| เทรดอัตโนมัติ (เอเจนท์เทรดเอง) | ✅ ได้ | 181 เอเจนท์ หาจังหวะ + ยิงผ่านเกตความเสี่ยง |
| สลับ "เทรดลม (paper)" ↔ "เทรดจริง (live)" | ✅ ได้ | ปุ่มในหน้า Settings (ต้องมี token) |
| ยิงออเดอร์เงินจริงบน Bitkub | ✅ ได้ | ต้องเปิดครบ 4 ประตู + ใส่ API key |
| **ถอนเงินจริง** | ❌ **ไม่มี** | ถอนผ่านแอป Bitkub เอง (ปลอดภัยกว่า) |
| ดูพอร์ต/กราฟ/เอเจนท์ เรียลไทม์ | ✅ ได้ | แดชบอร์ดเว็บ |

---

## 2) ติดตั้งและรัน
```bash
# 1. ติดตั้ง dependencies (ข้าม pandas-ta ได้ มี stub ในตัว)
pip install -r requirements.txt

# 2. ตั้งค่า (คัดลอกไฟล์ตัวอย่าง)
cp .env.example .env        # ไม่ใส่คีย์ก็รันแบบ paper ได้เลย

# 3. รัน
./start.sh                  # Linux/Mac
start.bat                   # Windows (หรือ start.ps1)

# 4. เปิดเบราว์เซอร์
http://127.0.0.1:8000
```
รันโดยไม่ใส่อะไรเลย = โหมด **paper** ดึงราคา BTC จริงจาก Bitkub มาเทรดจำลอง (ไม่ใช้เงินจริง).

---

## 3) ทัวร์แดชบอร์ด (เมนูซ้าย)
- **Dashboard** — มูลค่าพอร์ต, กำไรวันนี้, ราคา BTC, win rate, drawdown, สถานะระบบ (ทุกค่าเรียลไทม์)
- **Trading** — ตั๋วสั่งซื้อ/ขายเอง (ดูข้อ 5) + รายการเทรดจริงที่เกิดขึ้น
- **Analytics** — กราฟ equity, Sharpe/Sortino/Profit Factor/MaxDD/VaR (คำนวณจากเส้นทุนจริง)
- **Risk** — Daily Loss, Drawdown, Net Exposure, VaR + ปุ่มหยุดฉุกเฉิน
- **Agents** — เอเจนท์ทั้ง 181 ตัว แยกตามฝ่าย + กระดานการเรียนรู้ (จำความผิดพลาด/การแก้)
- **CEO** — มุมมองผู้บริหาร + audit trail (ใครตัดสินใจอะไร เพราะอะไร ผลเป็นไง)
- **Settings** — ห้องบังคับบัญชา: ความเสี่ยง, โหมด, breaker, สั่งเทรดเอง, เชื่อม Bitkub

---

## 4) โหมด "เทรดลม (Paper)" vs "เทรดจริง (Live)" — สวิตช์สลับ
ที่หน้า **Settings → 🚦 โหมดการเทรด**

### Paper (ค่าเริ่มต้น — แนะนำให้เริ่มที่นี่)
- ใช้พอร์ตจำลอง (เริ่มต้น 10,000,000฿) เทรดบนราคาจริง — **ไม่มีเงินจริงเคลื่อน**
- เหมาะกับการทดสอบกลยุทธ์/ปรับจูนก่อนใช้เงินจริง

### Live (เงินจริง) — ต้องเปิดครบ **4 ประตูกันพลาด**:
1. **execution_engine = live** (กดปุ่ม LIVE)
2. **confirm token** = `I_ACCEPT_REAL_MONEY_RISK` (พิมพ์ในช่อง token)
3. **ไม่มีไฟล์** `data/KILL_SWITCH` (สวิตช์ฉุกเฉิน — ลบออกก่อน)
4. **breaker ปิดอยู่** (ไม่ถูก trip) + **ตั้ง cap/ออเดอร์ > 0** (`max_single_order_thb`)

**ขั้นตอนเปิดเทรดจริง (ทำตามลำดับ):**
```
1. Settings → เชื่อม Bitkub: ใส่ API Key + Secret (ต้องเปิดสิทธิ์ "ซื้อขาย")  → กด Connect
2. Settings → ความเสี่ยง: ตั้ง "cap/order (THB)" เช่น 1000  → Apply   (เริ่มน้อย ๆ ก่อน!)
3. ตรวจ breaker = CLOSED (ปกติ) และไม่มีไฟล์ data/KILL_SWITCH
4. Settings → โหมด: พิมพ์ token I_ACCEPT_REAL_MONEY_RISK  → กด LIVE
5. หน้าจอจะขึ้น "🔴 LIVE: real orders WILL fire on Bitkub"
```
> เมื่อเปิดครบ ปุ่ม BUY/SELL และเอเจนท์ = **ส่งคำสั่งเงินจริง**. ปิดกลับเป็น paper ได้ทุกเมื่อ
> (กด PAPER — ไม่ต้องใช้ token).

---

## 5) สั่งซื้อ/ขายเอง (Manual)
**Settings → 📋 สั่งเทรดเอง** หรือหน้า **Trading**:
- **ซื้อ BUY** — เปิดไม้ (ขนาดคำนวณอัตโนมัติจาก risk% — ไม่ต้องกรอกจำนวน)
- **ขาย SELL / ปิด position** — ปิดไม้ที่ถืออยู่
- **ปิดทั้งหมด** — flatten ทุกโพซิชัน
- ช่องราคา: เว้นว่าง = ใช้ราคาตลาดล่าสุด
> โหมด paper = เปลี่ยนพอร์ตจำลอง · โหมด live = ส่งคำสั่งจริงบน Bitkub

---

## 6) ตั้งค่าความเสี่ยง (ปรับเองได้ 100%) — Settings → ⚖️ ตั้งค่าความเสี่ยง
| ช่อง | ความหมาย | ค่าเริ่มต้น | ช่วงที่ตั้งได้ |
|---|---|---|---|
| risk %/trade | เสี่ยงกี่ % ของทุนต่อไม้ | 1.0 | 0.01–**100** |
| stop % | จุดตัดขาดทุน | 0.5 | 0.05–99 |
| take-profit % | จุดทำกำไร | 1.5 | 0.05–1000 |
| max daily loss % | เพดานขาดทุนรายวัน | 100 (ปลดล็อก) | 0.1–100 |
| max consec. losses | เบรกเกอร์แพ้ติดกัน | **0 = ไม่จำกัด** | 0–1,000,000 |
| max open positions | จำนวนไม้พร้อมกัน | 3 | 1–50 |
| cap/order (THB) | เพดานเงินต่อออเดอร์ | 0 = ไม่จำกัด | ≥ 0 |
| **เกตชนะขั้นต่ำ p_win** | ยิงเมื่อโอกาสชนะย้อนหลัง ≥ ค่านี้ | **0.55** | 0–1 |

> 🔑 **p_win คือกุญแจ "ยิงบ่อย/ยิงน้อย"**: ตั้งต่ำ (เช่น 0.50) = เทรดบ่อยขึ้น · ตั้งสูง (เช่น 0.70)
> = เทรดน้อยแต่คัดแม่นกว่า. **ถ้ารู้สึกว่าเอเจนท์ไม่ค่อยเทรด ลดค่านี้ลง.**

**เป้าหมายรายวัน** (ใน `.env` หรือปรับโค้ด): `target_daily_profit_pct` (เป้า %), `stop_at_daily_target`
(ถึงเป้าแล้วล็อกกำไร หยุดเปิดไม้ใหม่), `max_trades_per_day` (เพดานไม้/วัน ~200).

---

## 7) 181 เอเจนท์ ทำงานกันยังไง (จัดทัพให้สมูท)
```
ราคา BTC ─→ ศูนย์ข้อมูลแท่งเทียน (1s/1m/1h/1d)
              ├─ 50 นักวิเคราะห์กราฟอดีต  ─┐
              ├─ 50 นักอ่านราคาปัจจุบัน    ─┤→ หัวหน้าฝ่ายสรุป "ฉันทามติ"
              └─ 50 พรานหาจุดเข้าซื้อ      ─┘        │
                                                    ▼
        กลยุทธ์ (เทรนด์/สวนเทรนด์/เบรกเอาต์) ─→ Supreme (โหวตข้างมาก)
                                                    ▼
                          เกตความเสี่ยง (p_win + breaker + cap + budget)
                                                    ▼
                               คลังเงิน (อนุมัติ/veto) ─→ เปิด/ปิดไม้
```
- **พราน 50 ตัว = ที่ปรึกษา** (ยกธง "พร้อม") → **หัวหน้าฝ่ายยิงสัญญาณรวมก้อนเดียว** (ไม่ท่วมท่อ)
- เอเจนท์ทุกตัวมี **ความจำ** (Learner): จำว่าเคยผิดอะไร แก้อะไร — เซฟลงเครื่องที่ `data/state.db`
  ทุก 30 วิ + ตอนปิด → เปิดใหม่จำได้

---

## 8) เชื่อมบัญชี Bitkub จริง (Settings → 🔌 เชื่อม Bitkub)
1. ไปที่ Bitkub → API Management → สร้าง Key + Secret
   - **อ่านยอดอย่างเดียว** (reconciliation) → ไม่ต้องเปิดสิทธิ์ซื้อขาย
   - **เทรดจริง** → ต้องเปิดสิทธิ์ "ซื้อขาย" (ไม่ต้องเปิดสิทธิ์ถอน — ระบบไม่ถอน)
2. ใส่ Key+Secret → Connect → ระบบอ่าน wallet จริงมากระทบยอด
3. คีย์ถูกเก็บใน `.env` (ไม่เคยส่งกลับออกจอ)

---

## 9) ความปลอดภัย (ตาข่ายกันล้ม)
- **Survival floor 70%** — ถ้าทุนเหลือ < 70% ของทุนเริ่มต้น คลังจะ **หยุดเปิดไม้ใหม่** อัตโนมัติ
- **Circuit breaker** — กด TRIP เพื่อหยุดเทรดทันที / รีเซ็ตด้วย token `MANUAL_RESET_CONFIRMED`
- **Emergency stop** — flatten ทุกไม้ + หยุดทุกเอเจนท์
- **Kill switch** — สร้างไฟล์ `data/KILL_SWITCH` = บล็อกการยิงเงินจริงทั้งหมด
- **cap/order** — จำกัดเงินต่อออเดอร์ (บังคับต้องตั้งก่อนเปิด live)

---

## 10) ถอนเงิน
ระบบ **ไม่มีปุ่มถอนเงินจริง** โดยตั้งใจ (กดถอนอัตโนมัติ = อันตราย/ย้อนไม่ได้).
**ถอนผ่านแอป/เว็บ Bitkub เอง** — ปลอดภัยและมี 2FA ของ Bitkub คุม.

---

## 11) แก้ปัญหาที่พบบ่อย
| อาการ | สาเหตุ/วิธีแก้ |
|---|---|
| เอเจนท์ไม่เทรด | ลด **p_win** ในตั้งค่า (เช่น 0.50) · ระบบต้อง warm-up เก็บข้อมูลสักครู่ก่อนเริ่มเทรด |
| "กระทบยอดบัญชี" ขึ้นค้าง | ปกติแล้ว (แก้ไปแล้วใน Phase 6) — ถ้ายังค้าง เช็คว่าใส่ API key ถูก |
| ราคาขึ้น "รอราคาจาก Bitkub…" | ฟีดยังไม่ติด — เช็คเน็ต/ไฟร์วอลล์ (ใช้ REST polling พอร์ต 443) |
| อยากเทรดถี่ขึ้น | ลด p_win + ลด take-profit % · เพิ่ม max_trades_per_day |
| กลัวขาดทุน | ตั้ง max daily loss % ต่ำ ๆ + max consec. losses เป็นเลข (ไม่ใช่ 0) |

---

## 12) คำเตือนสุดท้าย (ไม่โกหก)
- ระบบนี้ **พยายาม** ทำกำไรอย่างแข็งขัน แต่ **ไม่การันตีกำไร** — ทดสอบจริงเจอทั้งกำไรและขาดทุน
- ค่าธรรมเนียม Bitkub (~0.25%/ข้าง = 0.5% ไป-กลับ) กินกำไร — ยิ่งเทรดถี่ยิ่งต้องระวัง
- **เริ่มที่ paper เสมอ** ทดสอบให้มั่นใจ แล้วค่อยเปิด live ด้วย cap เล็ก ๆ
- เงินจริงที่เสีย = ความรับผิดชอบของผู้ใช้ — ใช้เท่าที่รับความเสี่ยงได้
