> ⚠️ **SUPERSEDED — บันทึกอดีต (2026-06-22, golden `ae84d3f0` ปลดระวางหลัง rebaseline ADR-064..076).**
> **จุดเริ่มต้นเดียวปัจจุบัน = [`_ส่งมอบ_FINAL_5YR_08e6abfd_TH.md`](./_ส่งมอบ_FINAL_5YR_08e6abfd_TH.md)** (golden `08e6abfd`). · ADR-083

# ส่งมอบ — pukpui v9.3.4 (golden ae84d3f0) · รอบ audit + packaging fix ADR-060 · 2026-06-22

> โหมด: เริ่ม LOCKED → audit (read-only) → เจอ 1 จุดควรแก้ (golden-neutral) → Tor อนุมัติ "นายจัดการเลย"
> → แก้ + พิสูจน์ golden เท่าเดิม + 1 ADR + แพ็ก release. **golden ไม่ขยับตลอดทั้งรอบ.**

---

## 0) สรุปผู้บริหาร

- **Audit เต็มระบบ:** ไม่มีบั๊กร้ายแรง/กลาง (เชิงการทำงาน). ระบบ production-grade.
- **ปิดจบ golden ทางการ:** เซสชันนี้รันบน **Python 3.12.3 + corpus /mnt/project 148 ไฟล์จริง** →
  `regression_full` ได้ **`ae84d3f0`** (engine==agent==baseline) — สิ่งที่เซสชัน cloud 2026-06-22 ทำไม่ได้
  (ไม่มี corpus + Python 3.11) ตอนนี้ยืนยันบนข้อมูลจริงแล้ว.
- **แก้ 1 จุด (ADR-060, golden-neutral):** ถอด `pythainlp` ออกจาก auto-install ใน `requirements.txt`
  (แก้ความขัดแย้ง requirements ↔ landmine #6) + แก้ corpus count 106→148 ใน `constraints.txt`.
- **พิสูจน์หลังแก้:** golden `ae84d3f0` เท่าเดิม · doc-sync PASS · check_invariants 4/4 · run_ci.sh เต็ม ✅ (84 step).

---

## 1) Audit findings (severity-rated)

**ร้ายแรง:** ไม่มี · **กลาง (เชิงการทำงาน):** ไม่มี.

**ต่ำ — ของใหม่จาก empirical probe (ทั้งหมด golden-neutral, ไม่อยู่ใน corpus):**
- L1 · เลขติดลบไม่สมมาตร: subtotal/total รับเฉพาะ `>0` (ติดลบ→None) แต่ VAT กรองแค่ `|v|≤1`.
  กระทบเฉพาะ input ยอดติดลบ (ไม่ใช่ใบกำกับภาษีปกติ). คงไว้ (defensible) — ถ้าจะแก้ = STOP-AND-ASK.
- L2 · BE→CE ไม่มีเพดานบน (`yr4>2400 → -543`): ปี 9999 → 9456 แต่โดน DT002/003/004 ฟ้องเสมอ. cosmetic.
- L3 · `_SYSTEM_ISSUES`/`_SYSTEM_ISSUE_SEEN` ไม่มี hard cap (ต่างจาก TEXT_NUM@20k). โตเฉพาะ distinct (dedup) + reset ทุกรอบ. theoretical.

**ต่ำ — เดิมที่ document ไว้แล้ว (ยกมาเตือน):** L-5/V-F2 (DT004 false-pos เลขรันล้วน≥5หลัก, live rule→รออนุมัติ) · L-7/P-LOW3 (ANTI_PREFIX dead code) · M-2/M-3 ฯลฯ ตามรายงาน 2026-06-21.

**สิ่งที่ verify แล้วแข็งแรง:** input hardening (10 adversarial เคส 0 crash/0 hang) · 0 circular import · cache cap ครบ (50k/50k/20k) + reset 11/11 global · ไม่มี wall-clock รั่วเข้า logic · ไม่มี div-by-zero · master kill-safe (atomic+backup+stub) · รายงาน 2 ตัวสร้างได้ครบ + ตรง locked req · 0 open TODO/FIXME.

---

## 2) ADR-060 — packaging fix (รายละเอียดเต็มใน INVARIANTS/DECISIONS.md)

**ปัญหา:** `requirements.txt` สั่งลง `pythainlp==5.0.5` แต่ landmine #6 ห้ามลง (golden 51 typo จาก CONSTRUCTION_DICT) → คนดูแลใหม่ทำ `pip install -r requirements.txt` ตรง ๆ = เสี่ยง golden drift เงียบ.

**แก้ (surgical, comment-level เท่านั้น):**
- `requirements.txt`: ถอด pythainlp ออกจาก auto-install + comment อธิบาย (ลงแยกเองได้ถ้าจำเป็น).
- `constraints.txt`: คง `pythainlp==5.0.5` แบบ constraint-only (มีผลถ้าลงแยก) + แก้ "106 ไฟล์"→"148 ไฟล์" (2 จุด).
- **ไม่แตะ** `config._LOCKED` (version_gate source; ถือ pythainlp = OPTIONAL_CRITICAL/missing-OK อยู่แล้ว).

**golden:** ไม่เปลี่ยน (`ae84d3f0` ก่อน/หลังเท่ากัน). ไม่ rebaseline.

---

## 3) วิธี reproduce (สำหรับเครื่องเจ้าของ — Python 3.12 + 148 ไฟล์)

```bash
export PYTHONHASHSEED=0 PUOPUY_AUDIT_DATE=2026-06-02 PUOPUY_OFFLINE=1
pip install -r requirements.txt -c constraints.txt     # หมายเหตุ: จะ "ไม่" ลง pythainlp แล้ว (ตั้งใจ — ADR-060)
python3 regression_full.py . /mnt/project baseline.json # ต้องได้ ae84d3f0 (engine==agent==baseline)
bash run_ci.sh /mnt/project                              # ด่านเต็ม ต้อง exit 0
```

---

## 4) งานค้าง (รอ Tor สั่ง — ไม่แตะเองเพราะ golden-sensitive / เป็นการตัดสิน)
- L-5/V-F2 DT004 false-positive (live rule) → ต้อง regression 148 ไฟล์ + STOP-AND-ASK ก่อนแก้.
- M-3/CMP004 advisory display policy (เลือก A/B) — เจ้าของตัดสิน.
- L1/L2/L3 ข้างบน — ถ้าอยากให้แก้ ผมจะเสนอ delta simulation + ADR ก่อน.

### สรุป 1 บรรทัด
**audit ผ่าน ไม่มีร้ายแรง/กลาง · golden ae84d3f0 ปิดจบบนข้อมูลจริง · ADR-060 ปิดกับดัก pythainlp (golden-neutral) · release ผ่าน build→verify gate.**
