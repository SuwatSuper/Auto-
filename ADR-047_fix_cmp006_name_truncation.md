# ADR-047 — แก้ CMP006 ตัดชื่อ บจ. ยาวกลางคำ + Rebaseline golden: `662c9132` → `df91493f`

สถานะ : ACCEPTED — 18.06.2026
บริบท : ออดิตเชิงรีพอร์ต — รันระบบจริงครบ 106 ไฟล์ / 834 บิล แล้วตรวจ "รายงานลูกค้า" **ทั้งสองตัว**
อย่างละเอียด (company_summary.txt จาก super_ultra_viewer + รายงานรายผู้ขาย .txt จาก agents/vendor_report)
ว่าผลตามกฎที่เปิดอยู่ถูกต้องครบไหม. พบบั๊กการแสดงผล 1 จุดในข้อความ issue ที่ใช้ร่วมกันทั้งสองรายงาน.

env ตรวจ: `PYTHONHASHSEED=0 PUOPUY_AUDIT_DATE=2026-06-02`

---

## สิ่งที่ตรวจแล้ว "ผ่าน" (ก่อนเจอบั๊ก)

ตรวจรีพอร์ตทั้งสองตัวเทียบผลออดิตจริง:
- **ยอด (ก่อน VAT) ถูกต้องเป๊ะ:** Σ ยอดในรายงาน = Σ subtotal ของบิล = **162,860,908.87** (ตรงทศนิยม) ; Σ จำนวนบิล = 834 (ไม่หาย/ไม่ซ้ำ).
- **ยุบต่อบริษัท×เดือน:** 1 บริษัท+เดือน = 1 บล็อก (ไม่แตกเป็น ต้องแก้/พร้อมส่ง).
- **ชี้จุดแบบ pinpoint:** ไม่มีรูปแบบ "(N ใบ)" — ระบุไฟล์/เลขที่เอกสาร/วันที่/ลำดับ ครบ.
- **สำนวน "รีเช็ค"** (ไม่ใช่ "แก้") ทั้งหัวช่องและท้ายบล็อก ; ฟุตเตอร์ถูก ("รีเช็ค...ครับ" / "ตรงเท่าที่ตรวจได้").
- **ไม่มีข้อมูลรั่ว** (None/nan/[soft]/provenance/amount_source) ; ไม่มีบล็อกยอดว่าง ; วันที่ DD.MM.YYYY สม่ำเสมอ.
- **ความต่างของสองรายงานเรื่อง typo fuzzy = เจตนา** (ล็อกด้วยเทสต์): company_summary โชว์ ITM011 fuzzy
  ("คำสินค้าผิด" — test_super_ultra_viewer.py) ; vendor_report ซ่อน fuzzy ผ่าน `_is_noise_issue()` markers
  "ใกล้เคียง"/"อาจสะกดผิด"/"(~" (test_vendor_report.py). **ไม่ใช่บั๊ก — ห้ามแก้** (จะทำให้เทสต์ที่ล็อกพัง).

---

## บั๊กที่พบ + แก้ (สืบจาก source/เซลล์จริง ไม่เดา)

### CMP006 ตัดชื่อนิติบุคคลยาวกลางคำ — `[:45]` → `[:80]` + ellipsis

- **ราก:** กฎ `r_cmp006` (rules_engine_rules_a.py:121–122) ตอนสร้างข้อความ issue detail ตัดทั้งชื่อในบิล
  และชื่อ ภ.พ.20 ด้วย `.strip()[:45]`. ชื่อบริษัทไทยที่มี suffix สถานที่ (เช่น "(ไทยแลนด์)") ยาวเกิน 45 ตัว
  → ถูกตัด **กลางคำ** ทำให้ "...จำกัด" กลายเป็น "...จำ" ในรายงาน — หลอกตา (ดูเหมือนชื่อบิลพิมพ์ขาด ทั้งที่ parser
  ดึงชื่อเต็มถูกต้อง). detail ของ CMP006 ถูก hash เข้า golden → เป็นทั้งปัญหา golden และรายงาน (โผล่ทั้งสองรายงาน
  เพราะใช้ detail เดียวกัน).
- **หลักฐานเซลล์จริง:** `KRR_69_012.xls` ช.17.1 — เซลล์ [5,7] =
  `'บริษัท ฉี อัน คอนสตรัคชั่น กรุ๊ป(ไทยแลนด์) จำกัด (สำนักงานใหญ่)'` ; parser ดึง company =
  `'บริษัท ฉี อัน คอนสตรัคชั่น กรุ๊ป(ไทยแลนด์) จำกัด'` (เต็ม ถูกต้อง) ; แต่ detail เดิม =
  `บิล='บริษัท ฉี อัน คอนสตรัคชั่น กรุ๊ป(ไทยแลนด์) จำ'` (ตัดที่ 45 ตัว → เสีย "กัด").
- **ขอบเขตชื่อจริง:** ชื่อบริษัทในคอร์ปัส 42 ชื่อ — ยาวสุด **67 ตัว** ("บริษัท แวนซัน วิคตอรี เมคานิคอล แอนด์
  อิเล็คทริค อิควิปเมนท์ จำกัด") ; เกิน 45 ตัว = 7 ชื่อ ; **เกิน 80 ตัว = 0 ชื่อ** → cap 80 แสดงชื่อจริงทั้งหมดเต็ม.
- **แก้:** `rules_engine_rules_a.py` — เปลี่ยน `[:45]` เป็นตัดที่ 80 ตัว + เติม `…` เฉพาะเมื่อยาวเกินจริง
  (กันบั๊ก "ตัดกลางคำ" สำหรับชื่อพิเศษอนาคต >80 ด้วย — ไม่ใช่แค่ขยับเพดาน). ไฟล์ 590 LOC (≤600).

---

## ผลกระทบ golden (validate ทีละใบ)

- **80/834 บิลเปลี่ยน — เปลี่ยนเฉพาะฟิลด์ `issues` (ข้อความ detail ของ CMP006) เท่านั้น ; 0 บิลที่ฟิลด์อื่นเปลี่ยน.**
- ตัวอย่าง KRR_69_012 ช.17.1: `บิล='...กรุ๊ป(ไทยแลนด์) จำ'` → `บิล='...กรุ๊ป(ไทยแลนด์) จำกัด'` (ชื่อเต็ม).
- **issue-code totals เหมือนเดิมเป๊ะ** (CMP006 นับ 80→80 — ไม่เปลี่ยนว่ากฎไหนฟ้อง เปลี่ยนแค่ข้อความ) ;
  ไม่เหลือการตัดกลางคำ "จำ" สักจุด ; โครงสร้างคงที่ (iv_seq=29, iv_date=2, typos=42, dup=1, companies=2).
- ยอดเงิน/provenance/ฟิลด์อื่นทั้งหมดไม่ขยับ. deterministic (PYTHONHASHSEED=0).
- ความต่างกระทบเฉพาะบิล ฉี อัน (ชื่อยาว 48 ตัว ที่ต่างจาก master stub ด้วย "(ไทยแลนด์)") — กับ master จริงของ
  ผู้ใช้ (ที่มี "(ไทยแลนด์)") ชื่อจะตรง CMP006 ไม่ฟ้องเลย ; บั๊กนี้แก้ที่ "รากของ detail" ให้ถูกในทุกกรณีชื่อยาว.

golden 662c9132 → df91493f · surfaces sync ครบ · 662c9132 retired (RETIRED_PREFIXES + GOLDEN.md) ·
backup `baseline.662c9132.pre-trunc.json`.

---

## ทางเลือกที่พิจารณาแล้ว "ไม่ทำ"

- **แก้ที่ชั้นรายงาน (code_labels.py) แทนกฎ:** ทำไม่ได้สะอาด — formatter ดึงชื่อจาก detail ที่ถูกตัดแล้วผ่าน
  regex `บิล='([^']*)'` กู้ชื่อเต็มกลับไม่ได้. แก้ที่รากของ detail (กฎ) ถูกต้องกว่า แม้กระทบ golden.
- **ขยาย cap ของ "ชื่อสินค้า" (rules_engine_rules_b.py `[:30–50]`) ด้วย:** ไม่ทำ — ชื่อสินค้าเป็นคำบรรยายยาวจริง
  การตัดเพื่อแสดงผลสมเหตุผล (ต่างจากชื่อนิติบุคคลที่เป็นอัตลักษณ์) + จะขยาย blast radius มากโดยไม่จำเป็น.
