# รอบแก้บั๊ก 2026-08-04 — ADR-179..189

> เอกสารสรุปสำหรับเจ้าของระบบ · รายละเอียดเต็มอยู่ใน `INVARIANTS/DECISIONS.md` (ADR-179 ถึง ADR-189)
> **หลักที่ยึดตลอดรอบ:** ทุกข้อสรุปมาจากการเปิดไฟล์ดิบเทียบเอง ไม่มีข้อไหนมาจากการอ่านโค้ดแล้วเดา

---

## 1. เปลี่ยนอะไรไปบ้าง (11 จุด)

| # | ADR | ระดับ | เรื่อง | ไฟล์ |
|---|---|---|---|---|
| 1 | 179 | 🔴 CRITICAL | parser อ่านคอลัมน์เงินผิด → false positive ทางการเงิน | `parser_p2.py` |
| 2 | 180 | 🟡 | console นับ 2 ถังจาก 3 → 10 บริษัทหายเงียบ | `pukpui_modular_funcs.py` |
| 3 | 181 | 🟡 | หัวรีพอร์ต Ultra ปนหน่วยนับ "บริษัท" กับ "จุดที่ฟ้อง" | `ultra_agent.py` |
| 4 | 182 | 🔴 CRITICAL | เอกสารไม่พิมพ์อัตรา 7% → VAT001 เทียบตัวเองตลอดกาล (384 บิล) | `parser_p2.py` |
| 5 | 183 | 🔴 | รวมบิลข้ามหน้าแล้วเขียนทับยอดที่พิมพ์บนเอกสาร | `parser_p0a.py` |
| 6 | 184 | 🟠 | ITM015 ฟ้องหน่วยเดียวกันคนละภาษา (Pcs. ↔ ท่อน/เส้น) 154 จุด | `puopuy_units.py` |
| 7 | 185 | 🔴 | `_HIDE_IN_SUMMARY` ซ่อน 3 รหัส → ช่องขึ้น "ตรง" ทั้งที่มีจุดฟ้องค้าง | `super_ultra_viewer.py` |
| 8 | 186 | 🟠 | ถอดชั้น "ตรวจตาเพิ่ม / ก้ำกึ่ง" — ทุกจุดขึ้นตามฟอร์ม | `super_ultra_viewer.py` |
| 9 | 187 | — | rebaseline golden + fixture + canary (แยก data drift ออกจาก code drift) | `baseline.json` ฯลฯ |
| 10 | 188 | 🟠 | **false-green**: `test_typing_leaf` ข้ามเงียบเพราะเครื่องไม่มี mypy → ซ่อน annotation ที่ขาด | `core_utils.py` |
| 11 | 189 | 🔴 CRITICAL | **ITM001 เดาว่าเป็น "ส่วนลด" แล้วข้ามเงียบ** → ยอดผิดจริง 1–50% ถูกกลืนทั้งแถบ | `parser_p1.py` · `rules_engine_rules_a.py` |

---

## 2. หลักฐานของบั๊กที่หนักที่สุด

### ADR-179 — `TKH_69_07.xls` ชีต 2 (IV `CB69070044`)

เปิดไฟล์ดิบด้วย `xlrd` แล้วบวกมือ: รายการ 8 บรรทัด (R11..R18) รวม = **89,374.55**
= `R19` = `R21` · `R22` = 6,256.2185 (= 89,374.55 × 7%) · `R23` = 95,630.7685
· ตัวอักษร `A21` = "เก้าหมื่นห้าพันหกร้อยสามสิบบาทเจ็ดสิบเจ็ดสตางค์" = 95,630.77 ✓

**เอกสารใบนี้ถูกต้องสมบูรณ์** แต่ระบบเดิมอ่าน `subtotal = 134,103.35` แล้วฟ้อง VAT001 (ต่าง 44,728.80) + VAT012

| | ระบบเดิม | หลังแก้ | เอกสารจริง |
|---|---|---|---|
| subtotal | 134,103.35 | **89,374.55** | 89,374.55 |
| VAT | 9,387.23 | **6,256.2185** | 6,256.2185 |
| total | 143,490.58 | **95,630.7685** | 95,630.7685 |

`W20 = 134,103.35` เท่ากันทั้ง 3 ชีต และ 89,374.55 + 15,734.80 + 28,994.00 = 134,103.35 พอดี
→ **W20 คือยอดรวมทั้งไฟล์ ไม่ใช่ยอดของบิล**

**ตัวชี้ขาดที่ใช้แก้ (เชิงโครงสร้าง ไม่ใช่การเดา):** คอลัมน์เงินจริงต้องมีตัวเลขในแถว VAT เสมอ (ยอด VAT พิมพ์คู่กับอัตรา) — คอลัมน์ helper ไม่มี

### ADR-182 — "ตรงหลอก" 384 บิล (35% ของ corpus)

เทมเพลต CHM/TSH/SEE/HSH/SBT/TNT/KTV ไม่พิมพ์อัตรา `0.07` → `_pb_find_vat_row` หาไม่เจอ →
footer ไม่ถูกอ่าน → `subtotal = ผลรวมรายการ` → **VAT001 ฟ้องไม่ได้ตลอดกาล** เพราะสองฝั่งเป็นตัวเลขเดียวกัน
→ รีพอร์ตขึ้น "ยอดก่อน vat : ตรง" ทั้งที่ **ไม่เคยเทียบกับเอกสารเลย**

**ผลรวม ADR-179 + 182 + 183 — ที่มาของยอดก่อน VAT:**

| | ก่อน | หลัง |
|---|---|---|
| อ่านจากเอกสารจริง (`ocr`) | 626 | **1085** |
| ระบบคำนวณเอง (`item_sum`) | 459 | **0** |

### ADR-184 — ไล่ดูคู่หน่วยที่ ITM015 ฟ้องทั้ง 254 จุด

| คู่หน่วย | จำนวน | คำตัดสิน |
|---|---|---|
| `['Pcs.','ท่อน']` | 115 | หน่วยเดียวกัน คนละภาษา → FP |
| `['Pcs.','เส้น']` | 33 | FP |
| `['PCS.','เส้น']` | 6 | FP |
| `ม้วน↔แพ็ค` `ตัว↔ม้วน` `คิว↔ตัน` `M↔กล่อง` `ตัน↔ถุง` `ขวด↔ซอง` `ถุง↔ลบ.ม.` | 100 | ต่างหน่วยจริง → ต้องแจ้ง |

แก้ที่ต้นเหตุของกฎ (ยุบ synonym) ไม่ใช่ปิดปากรีพอร์ต → **ITM015 254 → 100** · ITM005 ไม่ขยับ (249)

### ADR-185 — เคสที่เจ้าของจับได้เอง

กลุ่ม "จงฉือ คอนสตรัคชั่น 68.01" — `company_summary.txt` ขึ้น **"รายการสินค้า : ตรง"** + "ที่เหลือตรงครับผม"
ขณะที่ `company_summary_ultra.txt` รายงาน ITM015 **9 จุด** ในช่องเดียวกัน

นับทั้ง corpus: ultra มี ITM015 **254 ครั้ง** · `company_summary.txt` **0** · `agent_report.txt` **0**

---

## 3. เรื่อง "รันนิ่งลำดับรายการสินค้าขาด"

กฎ `ITM002` มีอยู่แล้วและจับ **ทั้งซ้ำและขาด** — และแจ้งจริง:

> `KTV_68_041.xls` ชีต 7 (IV `HS690400199`) — ไฟล์ดิบ `B18=5`, `B19=5` · 8 บรรทัดแต่เลขวิ่งถึง 7
> → รีพอร์ตขึ้น **"KTV วันที่ 07.04.2026 ลำดับที่ 5 ซ้ำกัน รีเช็คครับ"** ✓

เขียน oracle อิสระอ่านคอลัมน์ลำดับจากไฟล์ดิบเองทั้ง **1033 ชีต** เทียบกับที่ engine อ่านได้
→ **จำนวนรายการตรงกันทุกบิล ผิด 0** ไม่มีรายการหายเงียบ

ที่เคยไม่แจ้ง = ADR-185: พอ ITM002 ปนกับ ITM015/016/018 ที่ถูกซ่อน ทั้งช่องกลายเป็น "ตรง"

**ตรวจความครบของรีพอร์ต:** finding เลน fix/check ในบิล vs ที่ขึ้นรีพอร์ต → **ตกหล่น 0 จุด**
(ITM010+ITM011 ยุบ 47 → 39 บรรทัด แต่จุด unique `file/sheet/seq` = 39 = 39 ไม่หาย)

---

## 4. Golden ใหม่ + การแยกสาเหตุ (ADR-187)

| hash | โค้ด | corpus | ความหมาย |
|---|---|---|---|
| `f05358aa…` | เดิม | 152 ไฟล์ / 1153 บิล | baseline เดิม |
| `39f84497…` | **เดิม** | **146 ไฟล์ / 1085 บิล** | **data drift ล้วน** |
| `814c7cf0…` | ใหม่ | 146 ไฟล์ / 1085 บิล | **baseline ใหม่** |

**Diff เฉพาะส่วน code drift (โค้ดเดิม vs ใหม่ บน corpus เดียวกัน):**
- รหัสที่หายไป = `{ITM015: 154, VAT001: 1, VAT012: 1}` — false positive ทั้งหมด มีหลักฐานราย ADR
- **รหัสที่เพิ่มมา = `{}` — ไม่มี false positive ใหม่แม้แต่ตัวเดียว**

**fixture `ad0c9dad` → `d05e5de3`:** ยอดเงินเหมือนเดิมทุกบาท · รหัสที่ฟ้องเหมือนเดิมทุกตัว · เปลี่ยนเฉพาะ provenance เป็น `ocr`

**canary `148 ไฟล์` → `146 ไฟล์`:** พิสูจน์แล้วเป็น data drift — `CHM_69_051.xls` (8→75) และ `TSH_69_06.xls` (3→17)
ให้ผลเท่ากันทั้งโค้ดเดิมและโค้ดใหม่ (75 / 17) = เนื้อไฟล์เปลี่ยน ไม่ใช่ parser เปลี่ยน

**ไฟล์หลักฐานที่เก็บไว้ (append-only ห้ามลบ):**
- `baseline.f05358aa.pre-ADR179-186.json`
- `tests/fixtures/baseline_fixture.ad0c9dad.pre-ADR179-186.json`
- `canary_baseline.148files.pre-ADR187.json`

---

## 5. สถานะการพิสูจน์

| ด่าน | ผล | หมายเหตุ |
|---|---|---|
| `pytest` 131 สคริปต์ | ✅ ผ่านหมด | รวมเทสใหม่ `test_adr179_moneycol_footer.py` (24 เช็ก) · รันซ้ำหลังติดตั้ง mypy ครบแล้วก็เขียว |
| `regression_full` ข้อมูลจริง | ✅ | engine == agent == baseline `814c7cf0` |
| `regression_full` fixture | ✅ | `d05e5de3` |
| `verify_golden` | ✅ | AGENT == BASELINE |
| `verify_corpus_manifest` | ✅ | MATCH 146 ไฟล์ เป๊ะทุกไฟล์ |
| `verify_parallel` | ✅ | serial == parallel (workers=2 — sandbox มี 1 CPU) |
| `verify_report_det` | ✅ | รัน 2 รอบได้ hash เดียวกัน |
| `_audit_iv_truth` ข้อมูลจริง | ✅ | **1085/1085 เลขเอกสารอ่านตรงเซลล์จริง 100%** |
| `parse_canary` ข้อมูลจริง | ✅ | อัตรา parse ปกติ |
| `INVARIANTS/check_invariants` | ✅ | invariant ครบ |
| `coverage_gate` (branch ≥85) | ✅ | parser **85.1%** (โค้ดเดิม 84.9% = ตกอยู่ก่อนแล้ว) |
| `test_golden_single_source` | ✅ | 14 พื้นผิว sync กับ `baseline.json` |
| `black --check` | ✅ | 9 ไฟล์ในสโคป |
| `mypy` | ✅ | no issues in 4 source files |
| `test_typing_leaf` | ✅ | **แดงตอนแรก** — เป็นของเดิมที่ซ่อนอยู่ แก้แล้ว (ADR-188) |
| `ruff` | ⚠️ 73 errors | **มีเท่ากันเป๊ะทั้งโค้ดเดิมและใหม่** — เกิดจาก ruff 0.16.1 ใหม่กว่าที่โปรเจกต์ pin ไม่ใช่ของรอบนี้ |
| `pip-audit` | ⏭️ ข้าม | sandbox ปิด network ออกนอก |

---

## 6. ผลรีพอร์ตรอบสุดท้าย (146 ไฟล์ / 1085 บิล)

```
99 บริษัท×เดือน: ต้องแก้ 43 / ควรตรวจ 8 / ตรง 48      (43+8+48 = 99 ครบ)
SYS-* = 0 — ไม่มีกฎข้ามเงียบ (ทุกกฎรันครบทุกบิล)
คำว่า "ตรวจตาเพิ่ม" / "ก้ำกึ่ง" ในรีพอร์ต = 0 ครั้ง
```

เทียบกับก่อนแก้: console เคยพิมพ์ `ต้องแก้ 28 / ตรง 61` = 89 จาก 99 (ขาด 10)

---

## 7. สิ่งที่ยังค้างและควรทำต่อ

1. **ruff 73 errors** — ควร pin ruff เวอร์ชันใน `requirements.txt` ให้ตรงกับที่โปรเจกต์เคย lint ผ่าน
   หรือไล่แก้ทีละข้อ (ส่วนใหญ่เป็น `datetime.now()` ไม่ระบุ tz และ import ordering) · **ไม่กระทบผลตรวจ**
2. **`master_companies.json`** — ระบบยังไม่มี master จริง 99 กลุ่มจึงขึ้น "ไม่มีใน master (ตรวจไม่ได้)"
   ในช่อง ชื่อบจ./ที่อยู่/เลขภาษี/สาขา · ถ้าใส่ master จริงจะได้ ADDR/TAX/BR ครบ
3. **`verify_parallel` workers=8** — sandbox มี 1 CPU จึงรันได้แค่ workers=2
   (docstring ระบุว่า workers>1 บังคับ merge หลาย chunk อยู่แล้ว) ควรรัน workers=8 บนเครื่องจริงอีกรอบ
4. **ITM015 ที่เหลือ 100 จุด** — เป็นคู่หน่วยต่างจริง (`ม้วน↔แพ็ค`, `คิว↔ตัน` ฯลฯ) ตอนนี้ขึ้นรีพอร์ตหมดแล้ว
   ถ้าตรวจแล้วพบว่าคู่ไหนเป็นเรื่องปกติของผู้ขาย ให้เพิ่มเข้า `_UNIT_SYNONYMS` แบบเดียวกับ ADR-184
   (แก้ที่ต้นเหตุ **ห้ามกลับไปใช้ `_HIDE_IN_SUMMARY` ซ่อนอีก**)

---

## 8. รอบตรวจที่ 2 — ไล่ผลรีพอร์ตเทียบไฟล์แบบเต็มสูบ (2026-08-04)

เจ้าของสั่งตรวจซ้ำว่า **"มีอะไรผิดจริงตามไฟล์ แล้วระบบไม่แจ้งบ้าง"** โดยเน้น ราคา/จำนวน/ยอด
เขียน oracle อิสระ 4 ตัวอ่านไฟล์ดิบเองทั้ง corpus แล้วเทียบกับสิ่งที่ระบบฟ้อง

| สิ่งที่ตรวจ | จำนวน | ผล |
|---|---|---|
| `จำนวน × ราคา = ยอดรายการ` ทีละบรรทัด | 3,210 รายการ | **พบไม่ตรง 18 บรรทัด · ระบบฟ้อง 0** → ADR-189 |
| ↳ ตรวจซ้ำ: มีคอลัมน์ส่วนลดจริงไหม | 18 บรรทัด | **มีครบทั้ง 18 · กระทบยอดเป๊ะ · อธิบายไม่ได้ 0** |
| `qty/price/amount` มีอยู่จริงในแถวเดียวกันของไฟล์ | 3,198 รายการ | ✅ **ครบทุกค่า · parser ไม่แต่งตัวเลขเลย 0** |
| `ผลรวมรายการ = ยอดก่อน VAT` | 1,085 บิล | ✅ ไม่มีที่ผิดแล้วไม่แจ้ง |
| `ยอดก่อน VAT × 7% = VAT` | 1,085 บิล | ✅ ไม่มีที่ผิดแล้วไม่แจ้ง |
| `ยอดก่อน VAT + VAT = ยอดรวม` | 1,085 บิล | ✅ ไม่มีที่ผิดแล้วไม่แจ้ง |
| บาทตัวอักษร = ยอดตัวเลข | ทุกใบที่มีข้อความ | ✅ ไม่มีที่ผิดแล้วไม่แจ้ง |
| จำนวน/ราคาติดลบ · จำนวน=0 แต่มียอด | 3,210 รายการ | ✅ ไม่พบ |
| รายการซ้ำในบิล · ลำดับรายการขาด/ซ้ำ | 1,085 บิล | ✅ แจ้งครบ (ITM002/ITM016) |
| ชื่อสินค้าว่าง · ไม่มีเลขที่เอกสาร | 1,085 บิล | ✅ ไม่พบ |
| เลขผู้เสียภาษี 13 หลัก checksum | 1,085 บิล | ✅ **ถูกต้องทุกใบ · ขาดหาย 0** |
| เลขภาษีเดียวแต่ชื่อบริษัทต่างกัน | ทั้ง corpus | ✅ ไม่พบ |
| บิลไม่มีวันที่ / วันที่ไม่มีจริง | 1,085 บิล | ✅ **แจ้งครบ 3 ใบ** (ดูด้านล่าง) |

### 3 ใบที่วันที่มีปัญหา — ระบบแจ้งถูกต้องทั้งหมด

```
วันที่ : ไฟล์ SHS เลขที่เอกสาร IV6905-000850 ไม่มีวันที่ในบิล ต้องเติมวันที่ รีเช็คครับ
วันที่ : ไฟล์ TSH เลขที่เอกสาร IV681204-58 วันที่ 40/12/2568 ไม่มีจริงในปฏิทิน ต้องแก้วันที่ รีเช็คครับ
วันที่ : ไฟล์ KTV เลขที่เอกสาร HS680900884 ไม่มีวันที่ในบิล ต้องเติมวันที่ รีเช็คครับ
```
ตรวจไฟล์ดิบยืนยัน: `SHS_69_05` ชีต 29 เซลล์วันที่เป็น `"."` · `TSH_68_0112` ชีต 4.12 เป็น `'40/12/2568'` ·
`KTV_68_097` ชีต 29 เป็น `datetime.time(0,0)` (มีแต่เวลา ไม่มีวันที่) — **ผิดจริงตามไฟล์ทั้งสามใบ**

### สรุปรอบที่ 2

**พบช่องโหว่จริง 1 จุด (ADR-189)** — ที่เหลือระบบแจ้งครบหมด ไม่มีจุดไหนที่ผิดจริงแล้วเงียบ

ITM008 (ราคาต่อหน่วยผิดปกติ) ฟ้อง 0 ครั้งเพราะต้องมีรายการ ≥5 ในใบเดียว ซึ่งมีแค่ 192/1085 ใบ (17.7%)
— เป็นข้อจำกัดเชิงโครงสร้างของกฎ ไม่ใช่บั๊ก · ตรวจ outlier ข้ามบิลเองพบ 13 จุด แต่เป็นสินค้าชื่อกว้าง
(`Distribution frame`, `ผ้า`, `Flexible fiber`) ที่ต่างสเปกกันจริง → ฟ้องไปจะเป็น false positive

---

## 9. รอบตรวจที่ 3 — เต็มสูบทุกมิติ (2026-08-04)

ตรวจเพิ่มอีก 9 มิติที่รอบก่อนยังไม่ได้แตะ **พบผิดจริงแล้วไม่แจ้ง 2 จุด** (ADR-190, ADR-191)

| มิติที่ตรวจ | ขอบเขต | ผล |
|---|---|---|
| ชีตที่เป็นใบกำกับจริง ต้องออกบิล | 1,086 ชีต | ✅ **ครบ 1,086/1,086 ไม่มีบิลหาย** |
| ชื่อบริษัท / เลขภาษี / ที่อยู่ / เลขเอกสาร มีจริงในชีต | 1,085 บิล | ✅ ตรงทุกค่า (ที่ต่างคือ tax id ที่ Excel กิน 0 นำหน้า → ระบบเติมคืนถูก) |
| ชื่อสินค้า / หน่วย มีจริงในชีต | 3,210 รายการ | ✅ ตรงทุกค่า |
| **การ normalize เงียบที่ซ่อนความผิดของต้นฉบับ** | ทุกเซลล์ทุกไฟล์ | ❌ **พบ 57 เซลล์ · ไม่แจ้ง** → **ADR-190** |
| **เลขที่เอกสารซ้ำข้ามไฟล์ (นับเงินซ้ำ)** | 1,085 บิล | ❌ **พบ 20 ใบ 7.46 ล้านบาท · ไม่แจ้ง** → **ADR-191** |
| เลขเอกสารชนกันข้ามผู้ขาย | 28 คู่ | ✅ ยอดต่างกันหมด = คนละใบจริง ไม่ควรฟ้อง (ไม่ฟ้องถูกแล้ว) |
| ราคาต่อหน่วยผิดปกติข้ามบิล | 354 สินค้า | ✅ 13 จุดเป็นสินค้าชื่อกว้างต่างสเปก → ฟ้องไปจะเป็น FP |
| จำนวนบิลที่รีพอร์ตนับ vs จริง | 1,085 | ✅ ตรง |
| ITM008 ขอบเขตใช้งาน | 192/1085 ใบ (≥5 รายการ) | ✅ ข้อจำกัดโครงสร้าง ไม่ใช่บั๊ก |

### ADR-190 — ตัวอักษรไทยผิดรูปถูกกลบเงียบ

`KRR_69_057.xls` ชีต 26 รายการ #4 สะกด `ไม้อัดเคลือบฟิล์มดํา` ด้วย **นิคหิต ◌ํ + สระอา า**
ขณะที่รายการ #3 ในบิลเดียวกันสะกด `ดำ` ถูกต้อง → ระบบเคยรายงานว่าบิลนี้ **สะอาด 100%**

พบทั้ง corpus 57 เซลล์ใน 4 ไฟล์ — รวมถึง **ชื่อบริษัท** (`บริษัท โปร-พลัส … (สํานักงานใหญ่)` ×16)
และ **ที่อยู่** (`อําเภอบางบัวทอง` ×16) ซึ่งช่องเหล่านี้ **ไม่เคยถูกตรวจอักขระเลย** มีแต่ช่องรายการสินค้า

แก้: เพิ่มรหัส `CMP007` (ชื่อบจ.) + `ADDR011` (ที่อยู่) และเก็บ `address_raw` ไว้ให้กฎตรวจได้
→ CMP007 17 · ADDR011 22 · ITM004 127→130

### ADR-191 — ใบกำกับซ้ำข้ามไฟล์ = นับเงินซ้ำ 7.46 ล้าน

รีพอร์ตเดิมของ "สยามมิตร สตีล รีไซเคิล 69.05" ขึ้น **ตรงทุกช่อง**:
`ยอด : 32,238,324.60 บาท ตรง` · `บิล : 100 บิล ตรง` · `เลขที่ : ตรง` · `เลขที่ iv : ตรง`

ทั้งที่มีใบกำกับซ้ำ 20 ใบ (ผู้ขายเดียวกัน เลขเดียวกัน วันเดียวกัน **ยอดเท่ากันเป๊ะ**) คนละไฟล์:
`HSH_69_05 ↔ HSH_69_051` 10 ใบ **3,845,030.00** · `SEE_69_053 ↔ SEE_69_055` 10 ใบ **3,618,740.00**

ต้นเหตุ: `r_doc003` ถูกจำกัดเป็น "ไฟล์เดียวกัน" ตั้งแต่ v8.6 โดยอ้างว่าชีต `บิลซ้ำ` ใน Excel จับอยู่แล้ว
**ตรวจแล้วเหตุผลนี้ไม่จริง** — Excel มีข้อมูล แต่รีพอร์ตหลักและ console ไม่พูดถึงเลย และช่องขึ้น "ตรง"

รีพอร์ตหลังแก้:
```
เลขที่ : ไฟล์ HSH มีใบซ้ำกับไฟล์ HSH_69_051.xls จำนวน 10 ใบ รวม 3,845,030.00 บาทที่ถูกนับซ้ำ
         — ต้องเลือกใช้ไฟล์เดียว ; ไฟล์ SEE … 10 ใบ รวม 3,618,740.00 บาท … รีเช็คครับ
บริษัท สยามมิตร สตีล รีไซเคิล จำกัด 05/69 รีเช็คเลขที่ครับ
```

### ผลรีพอร์ตรอบสุดท้าย
```
99 บริษัท×เดือน: ต้องแก้ 46 / ควรตรวจ 9 / ตรง 44      (46+9+44 = 99 ครบ)
golden 814c7cf0…  ·  fixture d05e5de3…
```

---

## 10. ถอดระบบให้เล็กลง + พร้อมท์สำหรับ Claude Code (2026-08-05)

### ขนาดแพ็กเกจ 73 MB → **2.9 MB**

| รายการ | ก่อน | หลัง | ทำอะไร |
|---|---|---|---|
| `vendor/wheels/` | 73.0 MB | **0** | ถอดออก (plotly/numpy/pandas/matplotlib/pillow wheels) — วิธีติดตั้ง offline อยู่หัว `requirements.txt` |
| `baseline.json` | 6.7 MB | 5.3 MB | minify (ตัด indent) — `_sha256` เท่าเดิม golden ไม่ขยับ |
| baseline เก่า 3 ไฟล์ | 17.7 MB | 1.0 MB | ย้ายเข้า `_evidence/` แบบ `.gz` (append-only ยังอยู่ครบ) |
| `.coverage`, `__pycache__`, cache | ~0.5 MB | 0 | ลบ |
| **ไฟล์ zip** | **73 MB** | **2.9 MB** | |

โค้ดจริง: 234 ไฟล์ `.py` / ~42,000 บรรทัด · เอกสาร 92 ไฟล์ `.md`

### ADR-192 — เจอเพิ่มตอนตรวจรับ

acceptance จากแพ็กแกะสดทำ `test_perf_budget.py` แดง — ไล่แล้วพบว่า DOC003 ข้ามไฟล์ (ADR-191)
ที่เพิ่งเพิ่ม เดิน `all_bills` เต็ม batch ต่อทุกบิล = **O(n²)**

แก้ด้วยการเพิ่ม `iv_index` เข้า `_xbill_indexes` (cache ตาม batch อยู่แล้ว)

| | ก่อน | หลัง |
|---|---|---|
| ratio (เพดาน 3.2) | 3.06 | **1.91** |
| N=2500 | 1.53s | **0.83s** |
| N=5000 | 4.70s | **1.58s** |

golden `814c7cf0` ไม่ขยับ — เปลี่ยนเฉพาะเส้นทางการเดิน ไม่เปลี่ยนผลตรวจ

### `CLAUDE_CODE_BUGHUNT.md`

ไฟล์พร้อมท์สำหรับส่งต่อให้ Claude Code ล่าบั๊กต่อ — มี 11 หัวข้อ:
กฎเหล็ก 10 ข้อ · การตั้งสภาพแวดล้อม · **วิธีล่าบั๊กที่ได้ผลจริง 6 แบบ** (เรียงตามอัตราการเจอ)
· เกตที่ต้องผ่าน 10 ด่าน · ขั้นตอนเมื่อเจอบั๊ก 9 ขั้น · แผนที่ระบบ · **ADR 179–192 ที่ห้ามถอยกลับ**
· งานที่ยังค้าง 6 ข้อ · รูปแบบรายงาน · สิ่งที่จะทำให้ล้มเหลว · คำสั่งเริ่มงาน
