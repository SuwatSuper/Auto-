# FIX 11.06.69 — ปิด 3 เคสจริงจากการเทสบริษัทใหม่ (golden ไม่ขยับ)

> ชุดเทส: KRR 69.05 / SHS 69.05 / SSN 69.05 3 / TNT 69.01 (บริษัทใหม่ ไม่มี master)
> ผลก่อนแก้: ยอด/บิลคูณสอง + ฟ้องซ้ำมั่ว, "ไม่มีเลขที่ใบกำกับ" ทั้งที่เลขมี, "ไม่มีหน่วย (5 รายการ)" ทั้งที่หน่วยครบ
> ผลหลังแก้: ทั้ง 3 เคสหาย — **golden 106 ไฟล์/834 บิล ไม่ขยับ (d6b23d12…)** ทุก slice

---

## F1 — บิลคูณสอง (ธ.การช่าง 24 บิล/200,020 + DOC003 ฟ้องทุกใบ)

**ต้นตอ (พิสูจน์สองทิศ):** `get_files_via_drive` (parser_p2) สแกน `glob('**', recursive=True)`
→ ถ้าในโฟลเดอร์ "พร้อมตรวจ" มี **โฟลเดอร์ย่อยที่มีสำเนาไฟล์ชื่อเดียวกัน** (แบ็คอัพ/ของเก่า)
ไฟล์ถูก parse ซ้ำเงียบ ๆ และเพราะบิลเก็บ `file` เป็น basename → DOC003 (จำกัดไฟล์เดียวกัน)
เห็นสำเนาเป็น "ไฟล์เดียวกัน" → ฟ้องซ้ำกับเงาตัวเอง, ยอด/จำนวนบิล/ปี๊ป ×2 เฉพาะไฟล์ที่มีสำเนา
(จึงอธิบายได้ว่าทำไม TNT/SSN ไม่โดน)

- ป้อน 6 path ตรง (จำลองก่อนการ์ด): **24 บิล / 200,020 / DOC003×24** — ตรงรีพอร์ตที่เจอ 100%
- ผ่านการ์ดใหม่: **12 บิล / 100,010 / สะอาด**

**แก้:** การ์ดใน `get_files_via_drive` — เจอ basename ซ้ำหลาย path → เก็บตัว **ตื้นสุด**
(ไฟล์หลักหน้าโฟลเดอร์) ข้ามสำเนา + เตือนดัง 🛑 + `log_system_issue(SYS004)` ลง audit trail
(ถ้าตั้งใจให้เป็นคนละบิลจริง ให้เปลี่ยนชื่อไฟล์). ไม่มีชื่อซ้ำ = พฤติกรรมเดิมเป๊ะ.

**เทส:** `test_input_dedupe.py` (8 เคส) | CI ด่าน [3b1]

---

## F2 — TNT "ไม่มีเลขที่ใบกำกับ ต้องเติมเลขที่" ทั้งที่เลข 01954/01955 มีในไฟล์

**ต้นตอ (ซ้อนสองชั้น):** layout TNT มีเลขเดียวโดด ๆ '01954' มุมขวาบน (string 5 หลัก
ไม่มี label/prefix, ใต้เลขเป็น cell วันที่)
1. `_pick_best_iv` ตัด candidate ตัวเลข <6 หลักทิ้ง → '01954' ตกรอบ
2. weak-fallback ดันไปคว้า **เศษ float ของยอด VAT** ('1416233.0000000002' → '0000000002')
   → D2-GUARD (`validate_iv_post`) ล้างทิ้งเป็น '' ภายหลัง → IV005 ฟ้อง false alarm

**แก้:** `_pb_iv_lastresort` (parser_p2) — fallback ขั้นสุดท้าย ทำงานเฉพาะเมื่อ iv
**ว่างหรือเป็นขยะที่ D2-GUARD จะล้างแน่** (gate ด้วย `iv_digits_garbage`):
สแกนโซน header หา "ตัวเลขล้วน 4-5 หลักทั้ง cell" ที่มีสัญญาณตำแหน่งเอกสาร
(ครึ่งขวาของชีต หรือติด cell วันที่รัศมี ≤2) — กันจับ zip มุมซ้าย/excel serial (20000..60000)/
จำนวนสินค้า. หลายตัว → คะแนนสูงสุดชนะ (ขวา +10, ติดวันที่ +20, ความยาว ×2) deterministic.

**ความปลอดภัยต่อ golden:** corpus 834 บิล — iv ว่างหลัง post = **0 ใบ** → ทั้ง gate
dormant 100% บน corpus (วัดจาก baseline.json + ยืนยันด้วย regression เต็ม).

**ผล:** TNT ได้ iv '01954'/'01955', IV005 = 0, รีพอร์ต pinpoint ด้วยเลขเอกสารได้
("ไฟล์ TNT เลขที่เอกสาร 01954 วันที่ 02.01.2026 …")

**เทส:** `test_pb_iv_lastresort.py` (8 เคส) | CI ด่าน [3b3]

---

## F3 — SSN ชื่อสินค้ากลายเป็น 'Pcs.' + "ไม่มีหน่วย (5 รายการ)" ทั้งที่ไฟล์ครบ

**ต้นตอ:** `_dic_text_score` นับ "ข้อความ" เฉพาะ `len(s) > 3` → ชีต '9' มีสินค้าชื่อ
**'Fan' (3 ตัวอักษร)** ตกเกณฑ์ → คะแนนคอลัมน์ชื่อจริง (col2: Tube/Fan/หลอด 280/…) = 444
แพ้คอลัมน์หน่วย (col13: 'Pcs.' = 4 ตัว ผ่านทุกแถว) = 520 → `_dic_find_name` เลือก
**คอลัมน์หน่วยเป็นชื่อสินค้า** → ชื่อ='Pcs.' หน่วย='' ทั้งบิล → ที่มา "(5 รายการ)".
บั๊กคลาสนี้จะกัดทุกบริษัทใหม่ที่มีสินค้าชื่อสั้น ('ปูน', 'หิน', 'Fan').

**แก้ (conservative):** ใน `_dic_find_name` — ถ้าผู้ชนะคะแนนเดิมมี **distinct==1**
(ค่าซ้ำเดียวล้วน = ลายเซ็นคอลัมน์หน่วย) และมีคู่แข่งผ่านเกณฑ์ที่ **distinct≥2 + tlen มากกว่า**
→ เลือกคู่แข่งคะแนนสูงสุดในกลุ่มนั้น; **เคสกำกวมทุกแบบคืนผลเดิมเป๊ะ**
(รวมเคสอันตราย: ชื่อซ้ำเดียวล้วน + หน่วยหลากหลายแต่ข้อความสั้นกว่า → ไม่สลับ).

**ผล:** SSN ทุกบิลชื่อ+หน่วยถูกครบ (Tube/Fan/หลอด 280 + Pcs. ทุกรายการ),
หมายเหตุ "(5 รายการ)" หายจากรีพอร์ต, KRR "ปี๊ป" ยังถูกจับปกติ, **golden ไม่ขยับ**
(กติกา dormant ทั้ง corpus — TOR_67_08 ที่หน่วยว่างจริง 107 รายการเป็น layout
ไม่มีคอลัมน์หน่วย ไม่เข้าเงื่อนไข ไม่กระทบ).

**เทส:** `test_dic_find_name_unitlike.py` (6 เคส) | CI ด่าน [3b2]

---

## หลักฐานปิดงาน

| ด่าน | ผล |
|---|---|
| regression_full (106 ไฟล์/834 บิล) หลังทุก slice | engine = agent = baseline = `d6b23d12…` ✅ |
| test_input_dedupe | 8/8 ✅ |
| test_dic_find_name_unitlike | 6/6 ✅ |
| test_pb_iv_lastresort | 8/8 ✅ |
| ชุดเทส 4 ไฟล์ (จำลองโฟลเดอร์มีสำเนาซ่อน) | 12 บิล/100,010 + SYS004 เตือน ✅ |
| company_summary ชุดเทส | IV005=0, "(5 รายการ)"=หาย, DOC003=เงียบ ✅ |

## งานรอบหน้า (ตกลงกับเจ้าของระบบแล้ว — ดู HANDOFF_NEXT_CHAT_TH.md)
1. **Slice 4 (advisory):** ยุบช่อง "เลขที่" รวมเข้า "เลขที่ iv" (vendor_report_base.FIELDS) /
   หมายเหตุหน่วยว่างแบบ pinpoint (ไฟล์+วันที่+ลำดับที่, cap จำนวน) / ยืนยัน dup ข้ามไฟล์ไม่ขึ้น .txt
2. **Slice 5:** เพิ่มชุดเทส 4 ไฟล์เป็น fixture + test_new_company_layouts.py (กันถอยหลังบริษัทใหม่)
