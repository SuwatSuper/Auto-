> ⚠️ **เอกสารนี้ถูกแทนที่แล้ว (SUPERSEDED) — เป็นบันทึกอดีต (snapshot 20 มิ.ย. 2026, golden `ba9deda0` ปลดระวาง).**
> **จุดเริ่มต้นเดียวของการส่งมอบปัจจุบัน = [`_ส่งมอบ_FINAL_5YR_08e6abfd_TH.md`](./_ส่งมอบ_FINAL_5YR_08e6abfd_TH.md)** (golden ปัจจุบัน `08e6abfd`).
> เนื้อหาด้านล่างคงไว้เป็นหลักฐานประวัติ (ห้ามเชื่อค่า hash ในนี้ว่าเป็นปัจจุบัน — ดู `GOLDEN.md`). · ADR-083

# 📦 ส่งมอบระบบ ปุ้มปุ้ย (Puopuy) v9.3.4 — ฉบับสมบูรณ์ (production-stable + hardened 5 ปี)

ส่งมอบ : 20 มิถุนายน 2026 · สถานะ : **พร้อมใช้งานจริง** · golden : **`ba9deda0`**

> ระบบตรวจสอบใบกำกับภาษี/VAT แบบ offline — อ่านไฟล์ผู้ขาย (.xls/.xlsx) → ตรวจ ~56 กฎ → ออกรายงาน. เอกสารนี้คือจุดเริ่มต้นเดียวที่ต้องอ่าน (ที่เหลือคือ ADR/handoff เก่า).

---

## ✅ การรับรอง (certification)

| รายการ | ค่า |
|---|---|
| **Golden hash** (ความจริงเดียวของผลตรวจ) | `ba9deda0dcde668462224a4721b062d7f54054aa773612d610010539f1a8e169` |
| คลังตรวจจริง | **1,056 บิล · 148 ไฟล์** |
| engine == agent == baseline | ✅ ตรงทั้งสามเส้น |
| เทสทั้งหมด | **82 / 82 ผ่าน** |
| CI gates | regression_full · check_invariants · doc-sync · package_integrity · reachability ✅ |

## 🔒 สภาพแวดล้อมที่ตรึง (ต้องตั้งให้ตรงเพื่อ reproduce golden)
```
PYTHONHASHSEED=0   PUOPUY_AUDIT_DATE=2026-06-02   PUOPUY_OFFLINE=1   Python 3.12
pandas==2.2.2  numpy==2.2.6  xlrd==2.0.1  openpyxl==3.1.5  rapidfuzz==3.10.1
```
optional (ไม่ติดตั้งก็รันได้ — **ห้ามติดตั้งบนเครื่อง golden** เพราะอาจ drift): pythainlp / plotly / tqdm

## ▶️ วิธีใช้
```bash
# ตรวจ + ออกรายงาน
python3 run_agents.py --data /path/to/bills --report-dir ./out

# รายงานความผิดพลาด (Excel 4 ชีต)
python3 build_consolidated_report.py /path/to/bills error_report.xlsx

# รายงานรายบริษัท (txt + xlsx)
python3 super_ultra_viewer.py /path/to/bills ./out

# ตรวจ golden / regression (oracle)
python3 verify_golden.py
python3 regression_full.py . /path/to/bills
```

## 🚀 ปล่อยแพ็กเกจ — ต้องผ่านประตูนี้เสมอ (ห้าม zip มือ)
```bash
PYTHONHASHSEED=0 PUOPUY_AUDIT_DATE=2026-06-02 \
  python3 make_release.py <pkg_dir> <data_dir> <out.zip>
```
`make_release.py` จะ build→extract→รัน oracle บน tree ที่แตกจาก zip→เทียบ hash. **ถ้าไม่ตรง golden = ลบ zip ทิ้ง ส่งไม่ได้** (กันเหตุการณ์แพ็กเกจ drift หลุดออกไป).

---

## 📋 ประวัติการแก้ในรอบนี้ (golden ไม่ขยับทุก ADR)

**ADR-052 — กู้ golden จาก drift**  ที่อยู่ `"2/12-2/13 หมู่..."` ถูกอ่านเป็นวันที่ 1970 → 5 ใบ false-flag. แก้ regex ปี 2 หลักกำกวมไม่ให้สร้างวันที่มั่ว. กู้กลับ `ba9deda0`.

**ADR-053 — เสริมทนทาน 5 ปี** (golden-safe ทั้งหมด)
- `make_release.py` — ประตูปล่อยแพ็กเกจ (negative test พิสูจน์: drift → ปฏิเสธ+ลบ zip)
- `parse_date_any` รองรับ serial เก็บปี พ.ศ.ตรงๆ (244471=พ.ศ.2569) ทาง float
- `test_date_parse_characterization.py` — 36 เคสล็อกทุกสาขา date + adversarial (ตาข่ายจับ regression เร็ว)
- ยืนยัน pythainlp เป็น optional แท้ (degrade graceful)

**ADR-054 — แก้รีพอร์ตไม่ตรงกัน** (advisory)  `ITM015` ตกหล่นจาก `REVIEW_ONLY` → consolidated report จัด 34 ใบ (ทราย/ปูนหลายหน่วยปกติ) ผิดเป็น "ต้องแก้". แก้ให้ sync → ต้องแก้ 204→161.

## 🔍 ผลตรวจสอบรายงาน (รอบนี้)
- **เลขที่เอกสาร: อ่านตรง 100%** — ประกบ 1,056 บิลกับเซลล์จริง: เจอครบ 1,056 · fabricate 0 · subtotal-misread 0. (ปัญหา "เพี้ยน" รอบก่อนไม่เกิดซ้ำ)
- **error sheet: flag ทุกตัวชี้เนื้อหาเซลล์จริง** — typo/รูปแบบ (ITM011/019/010, DOC001, IV004) = ผิดจริง ; CMP006(ชื่อ~master)/DT001(งวด≠เดือนบิล) = soft ตรวจเพิ่ม ; DT002 = artifact ของ audit date ตรึง.

---

## ⚠️ งานที่ "เลือกพักไว้" (รอผู้ใช้ตัดสิน — ไม่ใช่บั๊ก)
1. **ITM011/ITM019 whitelist** — อยู่ใน golden snapshot; จับ typo จริงเป็นส่วนใหญ่. suppress = เสี่ยง false negative + ต้อง rebaseline. ทำได้ทันทีถ้าให้รายการคำที่ยืนยัน legit.
2. **lane logic ใช้ REVIEW_ONLY hardcoded แทน code_labels.MAP action tier** — ทำให้รหัส tier อ่อน (DT001='note', CMP006/IV004/ITM019='check') ตกเลน "ต้องแก้". ปรับให้ "ต้องแก้" เหลือเฉพาะ 'fix' tier ได้ แต่เปลี่ยน output มาก = design call.
3. **DT002 future-date** — artifact ของ `PUOPUY_AUDIT_DATE` ตรึง; production จริง (วันนี้จริง) หายเอง.

## 🧭 กติกาบำรุงรักษา (สำหรับ 5 ปีข้างหน้า)
- ทุกการแก้โค้ด → เขียน ADR ต่อท้าย `INVARIANTS/DECISIONS.md` · golden gate ทุก slice · revert ทันทีถ้า golden ขยับ
- แก้ parser/date → รัน `test_date_parse_characterization.py` ก่อน
- ปล่อยแพ็กเกจ → ผ่าน `make_release.py` เท่านั้น
- **ห้าม suppress การจับของจริงเพื่อลด false positive** เว้นแต่มี ground-truth + ผู้ใช้อนุมัติ + rebaseline (false negative แพงกว่าในระบบ audit)
