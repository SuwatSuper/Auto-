# รายงานล่าบั๊ก ปุ้มปุ้ย v9.3.5 — รอบ 2026-08-05

> ทำตาม `CLAUDE_CODE_BUGHUNT.md` · รูปแบบรายงานตามข้อ 9
> สภาพแวดล้อม: Python **3.12.3** · `PYTHONHASHSEED=0` · `PUOPUY_AUDIT_DATE=2026-06-02`
> เวอร์ชันไลบรารีตรง pin ครบทุกตัว (version_gate ✅ — pandas 2.2.2 / numpy 2.2.6 / xlrd 2.0.1 /
> openpyxl 3.1.5 / rapidfuzz 3.10.1) · QA toolchain ตรง `requirements-dev.txt` (ruff 0.15.19 ฯลฯ)

---

## ขอบเขตที่ตรวจได้จริง (แจ้งตรง ๆ ก่อน)

แพ็กนี้เป็น **SLIM** — **ไม่มี corpus จริง 146 ไฟล์** (`/mnt/project` ไม่มีบนเครื่องนี้)
ข้อมูลที่ใช้ได้จริงคือ **`tests/real_cases` 3 ไฟล์ / 15 บิล** และ **`tests/fixtures` 1 ไฟล์ / 3 บิล**
บวก **คอร์ปัสขอบที่สร้างเอง 8 ชีต** (`oracle_edge_corpus.py`)

ผลที่ตามมา (ต้องรู้ก่อนอ่านตัวเลขข้างล่าง):
- เกต `regression_full` บน **fixture** (`d05e5de3…`) รันได้และผ่าน — แต่เกตบน **corpus จริง**
  (`814c7cf0…`) **รันไม่ได้** เพราะไม่มีไฟล์ ; `verify_corpus_manifest` ข้ามด้วยเหตุผลเดียวกัน
- ตัวเลข "ผลกระทบกี่บิล/กี่บาท" ของบั๊กด้านล่าง วัดบนข้อมูลที่มีเท่านั้น
- **golden `814c7cf0` ไม่ถูกแตะและไม่ถูก rebaseline** — เอกสารทุกจุดยัง sync กับค่าเดิม (doc-sync ✅)

---

## บั๊กที่เจอ 2 จุด (แก้แล้ว) + 1 จุดเปิดค้าง (ต้องให้เจ้าของตัดสิน)

### [สูง · false-clean] ITM001 มอง "ศูนย์" เป็น "ค่าหาย" — `rules_engine_rules_a.py`

**อาการ** : บรรทัดรายการที่ `qty=5 price=0 amount=500` **ไม่มีกฎใดฟ้องเลย**
ยอด 500 บาทที่ขัดกับ `qty×price=0` ผ่านออกไปเป็น "ตรง" ทั้งที่ไม่เคยถูกตรวจ
ระบบควรฟ้องทุกบรรทัดที่ `qty×price ≠ amount` เว้นแต่มีเซลล์ส่วนลดที่กระทบยอดลงตัว (ADR-189)

**หลักฐาน** : `oracle_itm_matrix.py` — เมทริกซ์ 12 เคส เทียบ oracle อิสระกับกฎจริง

| เคส | oracle | กฎที่ฟ้อง (ก่อนแก้) | (หลังแก้) |
|---|---|---|---|
| `qty=5 price=100 amount=500` | ถูก | (เงียบ) | (เงียบ) |
| `qty=5 price=100 amount=0` | ผิด | ITM018 | ITM001,ITM018 |
| `qty=0 price=100 amount=500` | ผิด | ITM018 | ITM001,ITM018 |
| **`qty=5 price=0 amount=500`** | **ผิด** | **(เงียบ) ❌** | **ITM001 ✅** |
| `qty=0 price=0 amount=0` | ถูก | (เงียบ) | (เงียบ) |
| ส่วนลด 10% พิสูจน์ได้ (450) | ถูก | (เงียบ) | (เงียบ) |

ยืนยันปลายทางด้วยไฟล์ Excel จริง (`oracle_edge_corpus.py` ชีต 3) → รีพอร์ตพิมพ์
`รายการสินค้า : ... ลำดับที่ 1 10.0×0.0=0.00 แต่=3000.00 (ไม่พบส่วนลดในเอกสาร)`

**Root cause** : `r_itm001` คัดบรรทัดด้วย **ความจริงเชิงตรรกะ**
`if not (it['qty'] and it['price'] and it['amount']): continue`
ใน Python `0`/`0.0` เป็น falsy เท่ากับ `None` → ค่าที่ "อ่านได้จริงว่าเป็นศูนย์" ถูกจัดชั้นเดียวกับ
"ค่าหาย" แล้วข้ามเงียบ. เจตนาเดิมของบรรทัดคือ "ข้ามเมื่อข้อมูลไม่ครบ" = `is None`
ตัวสำรองไม่ช่วย: `r_itm018` ครอบ `qty=0`/`amount=0` ก็จริง แต่ทุกสาขาที่เทียบ `qty×price`
ต้องการ `price_f > 0` → `price=0` หลุดทั้งคู่ ; และ `r_itm008` จงใจไม่ตรวจตัวเลขเอง
(คอมเมนต์ในโค้ด: *"ปล่อย ITM001 จับเรื่องตัวเลขแทน"*) → พึ่ง ITM001 ตัวเดียว

**ผลกระทบ** : ข้อมูลที่ตรวจได้มี **0 บรรทัด** ที่มีศูนย์ (`oracle_zero_lines.py` — real_cases 0/80,
fixtures 0/3) → **ไม่กระทบผลตรวจปัจจุบัน** แต่เป็นช่องโหว่แฝง: เซลล์ราคาที่ OCR อ่านไม่ติดแล้ว
ได้ 0 จะกลายเป็น "ตรวจแล้วผ่าน" ทันที · ชนิด: **false-clean**

**การแก้** : เปลี่ยนเงื่อนไขคัดเป็น `is None` (1 บรรทัด)
*ทางเลือกที่ไม่เลือก* — (ก) ไปเพิ่มสาขา `price=0` ใน ITM018: รักษาอาการ ไม่ใช่ต้นเหตุ และทิ้ง
ITM001 ให้ตาบอดต่อไป (ข) ให้ parser แปลง 0 → None: ผิดกฎเหล็กข้อ 1 (ห้ามแต่งค่า) และทำลาย
หลักฐานว่าเอกสารพิมพ์ 0 มาจริง

**เทส** : `test_adr193_itm001_zero_line.py` — 14 เช็ก · **แดง 4 ข้อก่อนแก้ เขียวทั้งหมดหลังแก้**
เคสบวก: price/qty/amount = 0 ที่ผิดจริง + ยืนยันมีกฎจับอย่างน้อย 1 ตัว + ADR-189 ไม่ถอย
เคสลบ (กันแก้เกิน): ศูนย์ล้วน `0×0=0`, ส่วนลดพิสูจน์ได้, `None` ต้องยังข้ามเหมือนเดิม

**diff** : รหัสที่หายไป — **ไม่มี** · รหัสที่เพิ่มมา — ITM001 เฉพาะบรรทัดที่มีศูนย์และ
`qty×price ≠ amount` (พิสูจน์แล้วว่าเป็น error จริงทุกเคส ไม่ใช่ FP) · **golden fixture ไม่ขยับ**

---

### [กลาง · false-red] เกต lint แดง 73 ข้อเพราะ "เครื่องมือคนละเวอร์ชัน" — `pyproject.toml` / `run_ci.sh`

**อาการ** : `run_ci.sh [11] ruff` แดง 73 ข้อ (ตรงกับ "งานค้างข้อ 1" ในโจทย์) ทั้งที่ไม่มีใครแก้
โค้ดใน LINT_SCOPE เลย → CI ไม่เคยเขียวครบ

**หลักฐาน** (แยกตัวแปรทีละตัว ไม่แตะโค้ดเลยทั้งสองรอบ):

| เครื่องมือ | pyproject | ผล |
|---|---|---|
| ruff **0.16.1** | เดิม | **73 errors** |
| ruff **0.15.19** (= pin) | เดิม | **All checks passed** |
| ruff 0.16.1 | + `select` ชัด | All checks passed |

`ruff check --show-settings` : 0.16.1 เปิดกฎ **413 ตัว** (BLE/DTZ/I/UP/SIM/RUF/PIE)
ขณะที่ `requirements-dev.txt` pin `ruff==0.15.19` ไว้แล้วตั้งแต่ ADR-088

**Root cause (สองชั้น)** :
1. `run_ci.sh` เช็คแค่ว่า *"มีเครื่องมือไหม"* (`$PY -m ruff --version`) ไม่เคยเช็คว่า *"ตรง pin ไหม"*
   → ติดตั้งแบบไม่ระบุเวอร์ชัน (ซึ่งเป็นสิ่งที่คู่มือ bughunt ข้อ 2 สั่งให้ทำ:
   `pip install pytest pytest-cov ruff black mypy`) ได้ตัวใหม่กว่า pin แล้วเกตเปลี่ยนความหมายเงียบ
   → **ตระกูลเดียวกับ ADR-188** (mypy ไม่มี → SKIP เขียวหลอก) เพียงกลับด้านเป็น "แดงหลอก"
2. `pyproject.toml` เขียนสัญญา lint ไว้เป็น *คอมเมนต์* (`# default (E,F)`) แล้วปล่อยให้ ruff
   เลือกชุดกฎเอง → สัญญาเลื่อนตามเวอร์ชันเครื่องมือ ไม่ใช่ตามที่โปรเจกต์ตั้งใจ

**ผลกระทบ** : ไม่กระทบผลตรวจ/golden เลย (คนละชั้น) แต่ทำให้ CI ไม่เขียว และเสี่ยงกว่านั้นคือ
ทำให้คนชินกับ "เกตแดงเป็นปกติ" ซึ่งเป็นทางลื่นไปสู่การมองข้ามเกตที่แดงจริง

**การแก้** :
1. ประกาศสัญญา lint ให้ชัดใน `pyproject.toml` → `select = ["E4","E7","E9","F"]`
   (= สิ่งที่คอมเมนต์เดิมระบุอยู่แล้ว) → ชุดกฎคงที่ข้ามเวอร์ชัน
2. เพิ่ม `qa_toolchain_gate.py` (ใหม่) — เทียบเวอร์ชันเครื่องมือกับ pin:
   ไม่ได้ติดตั้ง = ข้าม (คงพฤติกรรม graceful เดิม) · **ติดตั้งแล้วผิดเวอร์ชัน = ตก พร้อมคำสั่งแก้**
   เสียบเป็น `[9pre]` ก่อนเกต 10-13

*ไม่ได้ทำ* : ไล่ "แก้" 73 ข้อนั้น — เป็นกฎสไตล์ที่โปรเจกต์ไม่เคยรับเป็นมาตรฐาน และบางข้อ
**ขัดข้อตกลงของโปรเจกต์โดยตรง** (`UP009` สั่งลบ `# -*- coding: utf-8 -*-` ที่ทุกไฟล์ใช้กับ
ข้อความไทย ; `DTZ001` สั่งใส่ tzinfo ทั้งที่ระบบ offline และตรึงวันที่ด้วย `PUOPUY_AUDIT_DATE`)
การรับกฎเพิ่มต้องเป็นการตัดสินใจที่ตั้งใจ — ใส่ใน `select` ไม่ใช่ถูกเปิดให้เองตอนอัปเครื่องมือ

**เทส** : ทดสอบเกตใหม่จริงทั้งสองทาง — ตั้งใจลง ruff 0.16.1 → `[9pre]` **ตก** พร้อมพิมพ์
`pip install ruff==0.15.19` ; ลงกลับ 0.15.19 → **ผ่าน** · `bash run_ci.sh` → **✅ CI ผ่านทั้งหมด**

---

### [สูง · เปิดค้าง — ไม่แก้เอง] ยอดที่ระบบ "เติมเอง" ไม่มีกฎใดแจ้ง → รีพอร์ตขึ้น "ตรง"

> **สถานะ: รายงานอย่างเดียว ยังไม่แก้** — ดู `INVARIANTS/DECISIONS.md` ADR-195

**อาการ** : ใบกำกับที่ **ไม่มียอดท้ายบิลบนเอกสารเลย** ถูก parser เติมยอดให้เอง
(`subtotal` = ผลรวมรายการ · `vat`/`total` = คำนวณจาก 7%) แล้ว **ไม่มีกฎใดฟ้องเลย**

**หลักฐาน** (`oracle_edge_corpus.py` ชีต 2 → `oracle_provenance.py`) :
- `amount_source = {'subtotal':'item_sum', 'vat':'derived', 'total':'derived'}` ·
  `amount_confidence=low` · `parse_confidence=MID` · **รหัสที่ฟ้อง = (เงียบ)**
- รีพอร์ตจริง (`main.py` → `company_summary.txt`) พิมพ์ `ยอด : 33,499 บาท ตรง` และ `บิล : 7 บิล ตรง`
- ตรวจเลข: `33,499 = 4000×4 + 4000(ปลอม) + 9999 + 3500` →
  **4,000.00 จาก 33,499.00 บาท (11.9%) ไม่เคยถูกอ่านจากเอกสารใด แต่อยู่ในผลรวมที่ติดป้าย "ตรง"**

**Root cause** : provenance **ทำงานถูกต้อง** (กลไกจาก ADR-182 ไม่พัง — parser บันทึก
`amount_source` ครบ) แต่ **ผู้บริโภคของข้อมูลนั้นถูกปิด** : `r_vat010`
("VAT ไม่ได้ตรวจจริง (ยอดถูกคำนวณเอง)") ตั้ง `enabled=False` ที่ `rules_engine.py:215`
คอมเมนต์กำกับ: *"v9.1: ปิด/ลบการทำงานตามคำขอ"*
ผลคือ VAT001/002/003 เอาค่าที่ระบบเพิ่งคำนวณไปเทียบกับตัวมันเอง → ตรงเสมอ = false-clean
(คลาสเดียวกับ ADR-182 เป๊ะ เพียงมาทางช่องที่ยังไม่ถูกปิด)

**ทำไมไม่แก้เอง** :
1. กฎถูกปิด **ตามคำขอของเจ้าของระบบ** → การเปิดกลับเองขัดกฎเหล็กข้อ 7
2. `r_vat010` ปัจจุบัน **กว้างเกินไป** — ฟ้องเมื่อ *ตัวใดตัวหนึ่ง* ใน subtotal/vat/total เป็น derived
   แต่ `total:'derived'` (= sub+vat) เป็นเรื่องปกติ: วัดจริง real_cases **5/15 ใบ**, edge **6/7 ใบ**
   → เปิดตรง ๆ = flood false-positive (น่าจะเป็นเหตุผลเดิมที่ถูกปิด)
   สัญญาณที่แคบและถูกต้องคือ **`subtotal` หรือ `vat` ไม่ได้อ่านจากเอกสาร (`!= 'ocr'`)**
3. เปลี่ยนชุดกฎ = ผลตรวจขยับบน corpus จริง 146 ไฟล์ ซึ่งไม่มีในแพ็กนี้ → วัดผลกระทบไม่ได้
   → ข้อ 5.9 ของคู่มือบังคับให้หยุดแล้วรายงานก่อน

**ผลกระทบที่วัดได้** : corpus ที่มีในแพ็กนี้ (real_cases 15 ใบ / fixtures 3 ใบ) มี **0 ใบ**
ที่ `subtotal`/`vat` ไม่ได้อ่านจากเอกสาร → **ไม่กระทบผลตรวจปัจจุบัน** แต่จะกระทบทันทีที่เจอ
ใบที่ OCR อ่านแถบยอดท้ายบิลไม่ติด (ซึ่ง ADR-182 เคยพบ **384 ใบ**)

**ข้อเสนอให้เจ้าของเลือก** (ยังไม่ดำเนินการ):
(ก) เปิด VAT010 โดยแก้เงื่อนไขให้เหลือเฉพาะ `subtotal`/`vat` (ตัด `total:'derived'` ออก)
(ข) ให้เป็น INFO/REVIEW แทน WARNING เพื่อไม่ดันใบเข้าเลน "ต้องแก้"
(ค) ให้รีพอร์ตแสดง "ยอดที่ระบบเติมเอง X บาท" ต่อท้ายบรรทัด `ยอด :` แทนการเพิ่มกฎ
    (แก้ความซื่อสัตย์ของรีพอร์ตโดยไม่แตะผลตรวจ/golden)
(ง) ยืนยันคงสภาพเดิม ถ้าประเมินแล้วรับความเสี่ยงนี้ได้

**วิธีวัดก่อนตัดสินใจ** : `python3 oracle_provenance.py /mnt/project`
→ บอกจำนวนใบ + จำนวนเงินที่ระบบเติมเองบน corpus จริง **ก่อน** เปลี่ยนอะไร

---

## ผลการรันเกต

| # | เกต | ผล | หมายเหตุ |
|---|---|---|---|
| 1 | `pytest -q` | ✅ **132 ผ่าน / 0 ตก** | เดิม 131 + เทสใหม่ 1 |
| 2 | `regression_full . /mnt/project` | ⏭️ **รันไม่ได้** | ไม่มี corpus จริงในแพ็ก SLIM |
| 3 | `regression_full . tests/fixtures` | ✅ | engine=agent=baseline=`d05e5de3…` |
| 4 | `verify_corpus_manifest.py` | ⏭️ ข้าม | ไม่มี `/mnt/project` |
| 5 | `test_golden_single_source.py` | ✅ | เอกสารทุกจุด sync `814c7cf0…` |
| 6 | `INVARIANTS/check_invariants.py` | ✅ | fixture + pin ครบ 4 ข้อ |
| 7 | `_audit_iv_truth.py` | ✅ | 15/15 ใบ อ่านเลขตรงเซลล์จริง (100%) |
| 8 | `verify_parallel.py <data> 8` | ✅ | **serial == parallel** (เครื่อง 4 คอร์จริง) |
| 9 | `coverage_gate.py` (branch≥85) | ✅ | parser 92.0/85.3 · rules 92.9/86.6 · รวม 93.3/87.2 |
| 10 | `mypy` (4 โมดูล) | ✅ | no issues |
| 11 | `ruff check` (LINT_SCOPE) | ✅ | หลัง ADR-194 (เดิมแดง 73) |
| 12 | `black --check` | ✅ | 9 ไฟล์ไม่ต้องแก้ |
| 13 | **`bash run_ci.sh`** | ✅ | **132 ขั้นผ่าน · 0 ตก · rc=0** |

**determinism (ข้อ 3.6)** : `verify_report_det` รัน 2 รอบได้ hash เดียวกัน
(`33187a23…`) · `golden_master` ที่ `PYTHONHASHSEED=0/1/42` ได้ `865040b7…` เท่ากันทั้งสาม

---

## golden

| | ก่อน | หลัง |
|---|---|---|
| corpus (146 ไฟล์) | `814c7cf0…` | `814c7cf0…` (**ไม่แตะ · รันยืนยันไม่ได้ — ไม่มี corpus**) |
| fixture (3 บิล) | `d05e5de3…` | `d05e5de3…` (**รันยืนยันแล้ว ไม่ขยับ**) |

**ไม่มีการ rebaseline ใด ๆ** · เอกสารทุกพื้นผิวยังอ้าง `814c7cf0` ตามเดิม (doc-sync ✅)

---

## งานค้างจากโจทย์ข้อ 8 — สถานะ

| # | เรื่อง | สถานะ |
|---|---|---|
| 1 | ruff แจ้ง 73 errors | ✅ **ปิดแล้ว** (ADR-194) — เป็นเรื่องเวอร์ชันเครื่องมือ ไม่ใช่บั๊กโค้ด |
| 2 | ยังไม่เคยรัน `verify_parallel` ที่ workers=8 | ✅ **ปิดแล้ว** — รันบนเครื่อง 4 คอร์จริง serial==parallel |
| 3 | ไม่มี `master_companies.json` จริง | ⏸️ ยังไม่มี (ต้องใช้ข้อมูลจริงจากเจ้าของ) — 4 ช่องยังขึ้น "ตรวจไม่ได้" |
| 4 | `ITM015` เหลือ 100 จุด | ⏸️ ตรวจไม่ได้ — ต้องมี corpus จริง |
| 5 | `ITM008` ครอบแค่ 192/1085 ใบ | ⏸️ ตรวจไม่ได้ — ต้องมี corpus จริง |
| 6 | `vendor/wheels` ถูกถอด | — ตามออกแบบ (วิธี re-vendor อยู่หัว `requirements.txt`) |

---

## ที่ยังแก้ไม่ได้ / ข้อสังเกต (บอกตรง ๆ)

1. **เกต golden บน corpus จริง (`814c7cf0`) ยืนยันไม่ได้ในรอบนี้** — ไม่มีไฟล์ 146 ไฟล์
   สิ่งที่ยืนยันได้คือ fixture (`d05e5de3`) + doc-sync ว่าเอกสารยังอ้างค่าเดิมครบ
   **ก่อนส่งมอบจริงต้องรัน** `python3 regression_full.py . /mnt/project baseline.json`
   บนเครื่องที่มีข้อมูล แล้วต้องได้ `814c7cf0` เป๊ะ

2. **ADR-195 (ยอดที่ระบบเติมเอง)** — เปิดค้าง รอเจ้าของตัดสิน ตามเหตุผลข้างบน

3. **ช่อง "รายการสินค้า" ขึ้น "ตรง" ทั้งที่มีข้อสังเกต 26 จุด** (ITM004 ×11, ITM005 ×15
   บน real_cases) — ตรวจแล้วพบว่า **ไม่ใช่บั๊กแบบ ADR-185**: `_HIDE_IN_SUMMARY` ว่างจริง
   (มีเทสล็อกไว้) และทั้ง 26 จุดถูกรายงานครบใน `audit_v58_*.xlsx` ชีต **"ข้อควรตรวจสอบ"**
   (26 = 11+15 ตรงกันพอดี ไม่ตกหล่น) — เป็นพฤติกรรมของ **เลน REVIEW** ("ข้อสังเกต ไม่ใช่
   must-fix") ตามที่ออกแบบไว้. แต่คู่มือข้อ 3.4 ตั้งเกณฑ์ว่า *"ช่องไหนขึ้น 'ตรง' ต้องไม่มี finding
   ค้างในช่องนั้น"* → **เป็นคำถามเชิงนโยบายการแสดงผล ไม่ใช่ข้อผิดพลาดเชิงตรรกะ**
   และซ้ำกับที่ ADR-054 บันทึกค้างไว้แล้ว (3-way label inconsistency, design call)
   → **ไม่แก้เอง** เพราะทิศทางถูกคุมด้วย ADR-186 (ห้ามเอาเลน soft กลับมา) — เสนอให้เจ้าของตัดสิน

4. **`pip-audit` ไม่ได้ติดตั้ง** → เกต [9] ถูกข้ามตามพฤติกรรม graceful เดิม
   (`qa_toolchain_gate` รายงานให้เห็นชัดแล้วว่าข้าม ไม่ใช่ผ่าน)

5. **กฎ 24 ตัวไม่ฟ้องเลย** ใน `oracle_rule_liveness.py` — สุ่มตรวจแล้วส่วนใหญ่เป็น
   "generator ยังไม่ครอบเงื่อนไข" ไม่ใช่กฎพัง (พิสูจน์รายตัวแล้ว เช่น `VAT009` ฟ้องเมื่อ
   `subtotal=0`, `ITM013` ฟ้องเมื่อลำดับไม่เริ่มที่ 1, `ITM017` ฟ้องเมื่อ qty ติดลบ,
   `IV007` ฟ้องเมื่อเลขเป็นศูนย์ล้วน, `VAT008` ฟ้องเมื่อ total > 10,000)
   ที่เหลือเป็นกฎที่ต้องใช้พจนานุกรม/บาทตัวอักษร/master — **ยังไม่ได้ไล่ครบทีละตัว**

---

## เครื่องมือ oracle ที่เพิ่มเข้ามา (reproduce ซ้ำได้ทุกเมื่อ)

| ไฟล์ | ตรวจอะไร |
|---|---|
| `oracle_suite.py` | ยอด footer · provenance · บรรทัดรายการ · ความซื่อสัตย์ค่า · ครบชีต · เลขซ้ำ · normalize เงียบ · เลขคณิต · เลขภาษี (checksum mod 11) |
| `oracle_itm_matrix.py` | เมทริกซ์ "บรรทัดผิดจริง → กฎไหนจับ" (จับ false-clean ตระกูล ITM) |
| `oracle_zero_lines.py` | วัดผลกระทบช่องโหว่ "ศูนย์ถูกมองเป็นค่าหาย" บนข้อมูลจริง |
| `oracle_failopen.py` | จับกฎที่กลืน exception เองแล้วรายงานว่า "ผ่าน" (แยกจาก SYS-* ที่ซื่อสัตย์) |
| `oracle_rule_liveness.py` | กฎที่ "ไม่เคยฟ้องเลย" แม้ยิงบิลผิดครบทุกตระกูล |
| `oracle_edge_corpus.py` | สร้างคอร์ปัสขอบ (ไม่มี footer / ราคา 0 / VAT เพี้ยน / ใบซ้ำ / ชีตว่าง) แล้ววัดว่าระบบ "ไม่เงียบ" |
| `oracle_provenance.py` | เงินที่ระบบ "เติมเอง" ถูกแจ้งหรือไม่ + คิดเป็นกี่บาท |
| `qa_toolchain_gate.py` | เครื่องมือเกตตรง pin หรือไม่ (กันเกตเปลี่ยนความหมายเงียบ) |

> ทั้งหมดเป็น standalone CLI · ลงทะเบียนใน allowlist ของ `test_reachability.py` แล้ว
> เจตนา: oracle ต้อง **ไม่แชร์โค้ด** กับสิ่งที่มันตรวจ มิฉะนั้นบั๊กเดียวกันจะกลบกันเอง
