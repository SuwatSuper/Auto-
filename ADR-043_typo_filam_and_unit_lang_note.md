# ADR-043 — เพิ่มคำผิด "ฟิมล์" + หมายเหตุ "หน่วยวัดปนไทย+อังกฤษ" ราย ไฟล์ (v9.3.4)

สถานะ : ACCEPTED — 17.06.2026
บริบท : ผู้ใช้ขอ 2 อย่างหลังตรวจไฟล์จริง 5 ไฟล์ (CHM/HSH/SHS/SSN/TKH) — เพื่อใช้รายงานส่งลูกค้า:
  1. เพิ่มคำผิด **"ฟิมล์"** (ที่ถูกคือ "ฟิล์ม") ให้ขึ้นรายงานเหมือนคำผิดอื่น
  2. ถ้าไฟล์ใดมี **หน่วยสินค้าทั้งไทยและอังกฤษ** ให้ขึ้นหมายเหตุ (ผู้ใช้ชี้ตัวอย่างไฟล์ SHS)

---

## งาน 1 — คำผิด "ฟิมล์" → "ฟิล์ม"

อาการ: ระบบมี pattern `ฟิลม์→ฟิล์ม` อยู่แล้ว แต่ "ฟิมล์" (ม/ล สลับที่) เป็นคำผิดคนละรูป ยังไม่ครอบ.

แก้: เพิ่ม `(r'ฟิมล์', '"ฟิมล์" → "ฟิล์ม"')` ลง `THAI_TYPO_PATTERNS` (config_base.py).
ไหลผ่าน `r_itm010` (ITM010 'Thai char-pattern typo', WARNING) เหมือนคำผิดอื่นทุกประการ →
ขึ้นรายงานช่อง "รายการสินค้า" เป็น `คำว่าฟิมล์ รีเช็คครับ`.

ปลอดภัย: "ฟิมล์" ไม่ใช่ substring ของคำจริงใด ๆ → ไม่ false-positive; "ฟิล์ม" (ถูก) ไม่ถูกฟ้อง.

## งาน 2 — หมายเหตุ "หน่วยสินค้า มีทั้งภาษาไทยและภาษาอังกฤษ" ราย "ไฟล์"

ข้อมูลจริง: ช่อง 'unit' (หน่วยนับ: ท่อน/แผ่น/ตัว) **เป็นไทยล้วนทุกไฟล์** — ไม่ใช่จุดที่ปนภาษา.
ผู้ใช้ยืนยันให้ "จับจากหน่วยสินค้าจริง" = **หน่วยวัด** ที่ฝังในชื่อ/สเปกสินค้า เช่น SHS เขียนทั้ง
"9 มม." (ไทย) และ "9mm." (อังกฤษ) + "120x240ซม.".

ของเดิม `company_unit_notes` ตรวจเฉพาะ "ช่อง unit" (ไทยล้วน → ไม่ฟ้อง) จึงเงียบกับ SHS.

แก้: เพิ่มฟังก์ชันใหม่ `file_spec_unit_lang_notes(bills)` (unit_detection_ext.py) —
สแกน **ชื่อ/สเปกสินค้า** หา "หน่วยวัดที่ติดตัวเลข" (length/area/volume/weight) แล้วจำแนกสคริปต์:
ไฟล์ใดมีทั้งรูปไทย (มม./ซม./นิ้ว/เมตร) และรูปอังกฤษ (mm/cm/inch…) → คืนหมายเหตุระดับไฟล์:

```
ไฟล์ SHS หน่วยสินค้า มีทั้งภาษาไทยและภาษาอังกฤษ (ไทย: ซม/นิ้ว/มม/เมตร · อังกฤษ: mm)
```

ต่อเข้า `super_ultra_viewer.build()` (ชั้น advisory) → ขึ้นบรรทัด "หมายเหตุ :" ในรายงานส่งลูกค้า.

เกณฑ์ปลอดภัย (กัน false-positive):
- ตัดสัญลักษณ์ `"` (นิ้ว) ทิ้ง — เป็นสัญลักษณ์สากล ไม่ใช่ "ภาษา" (ไม่งั้นทุกไฟล์ติดเพราะขนาดท่อ 1/2")
- หน่วยต้อง **ติดตัวเลข** (เช่น 9mm) และ token ยาว ≥2 ตัว → กัน 'm'/'g'/'l' ในรหัสรุ่น (เช่น '9M2')
- เฉพาะตระกูลหน่วย "วัด" (ไม่รวมหน่วยนับ ชิ้น/แผ่น/sheet ที่อยู่ในชื่อปกติ)
- advisory ล้วน — อ่าน bills เท่านั้น ไม่ mutate b['issues'] / ไม่แตะ rule engine → **golden hash ไม่ขยับ**

ผล 5 ไฟล์: SHS ✅, HSH ✅ (มี '9mm.' จริง) ; CHM/SSN/TKH ✗ (หน่วยวัดอังกฤษไม่มี มีแต่ '"').

## Verification

- งาน 1: ฟิมล์ → ITM010 + ขึ้นรายงาน 'คำว่าฟิมล์' ; ฟิล์ม(ถูก) ไม่ฟ้อง
- งาน 2 adversarial: '9M2'→ไม่ฟ้อง · ไทยล้วน→ไม่ฟ้อง · อังกฤษล้วน→ไม่ฟ้อง · มม.+mm→ฟ้อง · '1/2"'+มม→ไม่ฟ้อง
- core issue codes ของ 5 ไฟล์ ไม่เปลี่ยน (ITM004:26/ITM005:6/ITM015:4/DOC001:2) — note เป็น additive
- golden-neutral: hash real_cases (`95852c68…`) + fixtures (`269ddaed…`) ไม่ขยับ
- เทสครบ test_*.py 76/76 ผ่าน (รวม test_unit_detection_ext, ceiling ≤600, package integrity)
- ของเดิมไม่เสีย: TNT 75/75, HSH tax 9/9
