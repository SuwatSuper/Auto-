# ปุ้มปุ้ย v9.1 — Audit fixes (advisory layer only; engine untouched; byte-identical verified ec61907f)
# Generated by code audit. Each hunk maps to a finding ID in AUDIT_FIXES_TH.md.

--- a/agents/__init__.py
+++ b/agents/__init__.py
@@ -13,55 +13,62 @@
 
 หลักการ: ผลตรวจที่ออก Excel วิ่งผ่าน "เครื่องยนต์เดิมชุดเดียว" ใน orchestrator
 → byte-identical พิสูจน์ได้ด้วย golden_master. agent review ทุกตัวอ่านอย่างเดียว.
+
+──────────────────────────────────────────────────────────────────────────────
+⚠️ [P-DECOUPLE] โหลดแบบ lazy (PEP 562 __getattr__)
+   เดิม __init__.py นี้ import ทุก agent ทันที → การ `import agents.contracts`
+   เพียงตัวเดียวก็ลาก core_access (เครื่องยนต์หลักทั้งก้อน) มาด้วย. ผลคือ:
+     • โมดูล "บริสุทธิ์" (contracts/base/mesh/llm_provider/_shared) import ไม่ได้
+       ถ้าไม่มี engine ครบ — แม้มันไม่พึ่ง engine เลย
+     • engine ขาด symbol เดียว = ทั้ง namespace `agents` ล่ม (รวมเทสต์/เครื่องมือ)
+   แก้: ผูกชื่อ → ชื่อโมดูลย่อย แล้วค่อย import ตอนถูกเรียกใช้จริง:
+     - `from agents import FindingsMesh`  → โหลด .mesh (ไม่แตะ engine)
+     - `from agents import ImportAgent`   → โหลด .import_agent (พึ่ง engine จริง — ถูกต้อง)
+     - `import agents.contracts`          → __init__ เบา ๆ + contracts (ไม่แตะ engine)
 """
-from .ai_review_agent import AiReviewAgent
-from .base import Agent, AgentError, bill_ref, issues_with_prefix
-from .confidence_agent import ConfidenceAgent
-from .contracts import (
-    AgentResult,
-    Finding,
-    PipelineContext,
-    Severity,
-    Status,
-)
-from .crosscheck_agent import CrossCheckAgent
-from .formula_agent import FormulaAgent
-from .import_agent import ImportAgent
-from .llm_provider import (
-    LLMProvider,
-    MockProvider,
-    NullProvider,
-    OllamaProvider,
-    OpenAICompatProvider,
-    make_provider,
-)
-from .mesh import FindingsMesh
-from .notepad_agent import NotepadAgent
-from .orchestrator import Orchestrator, run_pipeline, _run_audit_core
-from .report_agent import ReportAgent, resolve_report_dir
-from .super_agent import SuperAgent
-from .synthesis_agent import SynthesisAgent
-from .taxid_agent import TaxIdAgent
-from .vat_agent import VatAgent
-from .wht_agent import WhtAgent
-
-__all__ = [
-    # base / contracts
-    "Agent", "AgentError", "bill_ref", "issues_with_prefix",
-    "AgentResult", "Finding", "PipelineContext", "Severity", "Status",
-    # data plane
-    "FindingsMesh",
-    # agents — Tier-1 review
-    "ImportAgent", "FormulaAgent", "VatAgent", "WhtAgent", "TaxIdAgent",
-    # agents — Tier-2 mesh consumers
-    "CrossCheckAgent", "ConfidenceAgent",
-    # agents — AI / synthesis / supervisor
-    "AiReviewAgent", "SynthesisAgent", "SuperAgent",
-    # agents — output
-    "ReportAgent", "resolve_report_dir", "NotepadAgent",
-    # orchestration
-    "Orchestrator", "run_pipeline", "_run_audit_core",
-    # llm
-    "LLMProvider", "OllamaProvider", "OpenAICompatProvider",
-    "NullProvider", "MockProvider", "make_provider",
-]
+import importlib
+
+# ชื่อสาธารณะ → โมดูลย่อยที่นิยามมัน (lazy: import เมื่อถูกอ้างถึงครั้งแรกเท่านั้น)
+_LAZY = {
+    # base / contracts (บริสุทธิ์ — ไม่พึ่ง engine)
+    "Agent": "base", "AgentError": "base", "bill_ref": "base", "issues_with_prefix": "base",
+    "AgentResult": "contracts", "Finding": "contracts", "PipelineContext": "contracts",
+    "Severity": "contracts", "Status": "contracts",
+    # data plane (บริสุทธิ์)
+    "FindingsMesh": "mesh",
+    # agents — Tier-1 review (import_agent/formula/vat/wht/taxid พึ่ง core_access → engine)
+    "ImportAgent": "import_agent", "FormulaAgent": "formula_agent",
+    "VatAgent": "vat_agent", "WhtAgent": "wht_agent", "TaxIdAgent": "taxid_agent",
+    # agents — Tier-2 mesh consumers (บริสุทธิ์)
+    "CrossCheckAgent": "crosscheck_agent", "ConfidenceAgent": "confidence_agent",
+    # agents — AI / synthesis / supervisor (บริสุทธิ์ — ใช้ llm_provider)
+    "AiReviewAgent": "ai_review_agent", "SynthesisAgent": "synthesis_agent",
+    "SuperAgent": "super_agent",
+    # agents — output (report พึ่ง engine; notepad บริสุทธิ์)
+    "ReportAgent": "report_agent", "resolve_report_dir": "report_agent",
+    "NotepadAgent": "notepad_agent",
+    # orchestration (พึ่ง engine ผ่าน core_access)
+    "Orchestrator": "orchestrator", "run_pipeline": "orchestrator",
+    "_run_audit_core": "orchestrator",
+    # llm (บริสุทธิ์)
+    "LLMProvider": "llm_provider", "OllamaProvider": "llm_provider",
+    "OpenAICompatProvider": "llm_provider", "NullProvider": "llm_provider",
+    "MockProvider": "llm_provider", "make_provider": "llm_provider",
+}
+
+__all__ = list(_LAZY.keys())
+
+
+def __getattr__(name):
+    """PEP 562: เรียกเมื่อ attribute ไม่อยู่ใน namespace → import โมดูลย่อยที่เกี่ยวข้องตอนนี้."""
+    mod_name = _LAZY.get(name)
+    if mod_name is None:
+        raise AttributeError(f"module 'agents' has no attribute {name!r}")
+    module = importlib.import_module(f".{mod_name}", __name__)
+    attr = getattr(module, name)
+    globals()[name] = attr      # cache: ครั้งถัดไปเจอใน namespace ไม่ต้องวิ่ง __getattr__ ซ้ำ
+    return attr
+
+
+def __dir__():
+    return sorted(list(globals().keys()) + __all__)
diff --git a/agents/_shared.py b/agents/_shared.py
new file (ADDED)
--- /dev/null
+++ b/agents/_shared.py
+# -*- coding: utf-8 -*-
+"""
+agents/_shared.py — ตัวช่วยกลาง (pure) ที่ agent หลายตัวใช้ร่วมกัน
+
+ทำไมต้องมีไฟล์นี้ (P-DEDUP):
+  ก่อนหน้านี้โค้ดชุดเดียวกันถูก "ก๊อปวาง" กระจายหลายไฟล์ จนเสี่ยง drift:
+    • _parse_llm_json()        — เหมือนกันเป๊ะใน ai_review / synthesis / super
+    • _SEV_RANK (3/2/1/0)      — ซ้ำใน vat / taxid / super / notepad_report (+ inline ใน mesh, ai_review)
+    • _max_sev()               — เหมือนกันใน vat / taxid
+    • bill key "file|sheet|iv" — เขียนเองซ้ำใน confidence / super / mesh
+  ถ้าแก้บั๊กในสำเนาเดียวแล้วลืมอีกสำเนา → พฤติกรรมเพี้ยนเงียบ. รวมไว้จุดเดียว = แก้ที่เดียว.
+
+⚠️ โมดูลนี้ "บริสุทธิ์": import เฉพาะ stdlib + .contracts (ไม่แตะ core_access/เครื่องยนต์)
+   → import ได้แม้ไม่มี engine, ไม่กระทบ byte-identical (อยู่ชั้น advisory ล้วน).
+"""
+from __future__ import annotations
+
+import json
+import re
+from typing import Any, Dict, List, Optional
+
+# ลำดับความรุนแรงมาตรฐานของทั้งระบบ (ใช้จัดอันดับ/หา max) — แหล่งความจริงเดียว
+SEV_RANK: Dict[str, int] = {"CRITICAL": 3, "ERROR": 2, "WARNING": 1, "INFO": 0}
+
+__all__ = ["SEV_RANK", "max_severity", "bill_key", "parse_llm_json"]
+
+
+def max_severity(issues, key=None) -> str:
+    """คืน severity สูงสุดจาก list ของ issue/finding (ค่าเริ่มต้น 'INFO' ถ้าว่าง).
+
+    Args:
+        issues: iterable ของ dict (มีคีย์ 'severity') หรือ object (มี .severity)
+        key:    ฟังก์ชันดึง severity จากแต่ละ item (ดีฟอลต์: รองรับทั้ง dict และ object)
+    """
+    if key is None:
+        def key(it):
+            if isinstance(it, dict):
+                return it.get("severity", "INFO")
+            return getattr(it, "severity", "INFO")
+    best = "INFO"
+    best_rank = SEV_RANK.get(best, 0)
+    for it in issues or []:
+        s = key(it) or "INFO"
+        r = SEV_RANK.get(s, 0)
+        if r > best_rank:
+            best, best_rank = s, r
+    return best
+
+
+def bill_key(file: str, sheet: str, iv: str) -> str:
+    """กุญแจอ้างอิงบิลมาตรฐาน (file|sheet|iv) — ใช้ทุกที่ที่ correlate ข้าม agent.
+
+    ⚠️ อย่า reconstruct (file,sheet,iv) ด้วยการ split คีย์นี้กลับ — ถ้า field มี '|'
+       จะแยกผิด. ให้เก็บ tuple ต้นฉบับไว้ต่างหากแล้วใช้ key นี้เป็น index เท่านั้น.
+    """
+    return f"{file}|{sheet}|{iv}"
+
+
+def parse_llm_json(text: str, raw_fallback_key: Optional[str] = None) -> Dict[str, Any]:
+    """แกะ JSON จากคำตอบ LLM แบบกันพัง (LLM อาจห่อด้วย ```json หรือมีข้อความนำ/ตาม).
+
+    ลำดับการพยายาม:
+      1) ตัด code fence แล้ว json.loads ทั้งก้อน
+      2) จับ {...} ก้อนแรกที่ครอบคลุมแล้ว json.loads
+      3) แกะไม่ได้เลย → ถ้ามี raw_fallback_key คืน {key: text[:500]} เพื่อคง "เนื้อความดิบ"
+         (พฤติกรรมเดิมของ ai_review/synthesis), ไม่งั้นคืน {}.
+
+    คืน dict เสมอ (ผู้เรียกใช้ .get() ต่อได้ปลอดภัย — ไม่ต้องกลัว None).
+    """
+    if not text:
+        return {}
+    t = text.strip()
+    t = re.sub(r"^```(?:json)?\s*", "", t)
+    t = re.sub(r"\s*```$", "", t)
+    try:
+        obj = json.loads(t)
+        return obj if isinstance(obj, dict) else {}
+    except Exception:
+        pass
+    m = re.search(r"\{.*\}", t, re.DOTALL)
+    if m:
+        try:
+            obj = json.loads(m.group(0))
+            return obj if isinstance(obj, dict) else {}
+        except Exception:
+            pass
+    if raw_fallback_key:
+        return {raw_fallback_key: t[:500]}
+    return {}
--- a/agents/vat_agent.py
+++ b/agents/vat_agent.py
@@ -17,6 +17,7 @@
 from decimal import Decimal
 
 from . import core_access as core
+from ._shared import max_severity as _max_sev
 from .base import Agent, bill_ref, issues_with_prefix
 from .contracts import AgentResult, Finding, PipelineContext, Severity, Status
 
@@ -75,15 +76,3 @@
             "false_clean_candidates": n_false_clean,
         }
         return AgentResult(self.name, Status.OK.value, summary=summary, findings=findings)
-
-
-_SEV_RANK = {"CRITICAL": 3, "ERROR": 2, "WARNING": 1, "INFO": 0}
-
-
-def _max_sev(issues):
-    best = "INFO"
-    for i in issues:
-        s = i.get("severity", "INFO")
-        if _SEV_RANK.get(s, 0) > _SEV_RANK.get(best, 0):
-            best = s
-    return best
--- a/agents/taxid_agent.py
+++ b/agents/taxid_agent.py
@@ -15,6 +15,7 @@
 from collections import Counter, defaultdict
 
 from . import core_access as core
+from ._shared import max_severity as _max_sev
 from .base import Agent, bill_ref, issues_with_prefix
 from .contracts import AgentResult, Finding, PipelineContext, Severity, Status
 
@@ -91,15 +92,3 @@
             "tax_id_multi_company": n_id_multi_company,
         }
         return AgentResult(self.name, Status.OK.value, summary=summary, findings=findings)
-
-
-_SEV_RANK = {"CRITICAL": 3, "ERROR": 2, "WARNING": 1, "INFO": 0}
-
-
-def _max_sev(issues):
-    best = "INFO"
-    for i in issues:
-        s = i.get("severity", "INFO")
-        if _SEV_RANK.get(s, 0) > _SEV_RANK.get(best, 0):
-            best = s
-    return best
--- a/agents/ai_review_agent.py
+++ b/agents/ai_review_agent.py
@@ -17,8 +17,8 @@
 from __future__ import annotations
 
 import json
-import re
 
+from ._shared import SEV_RANK as _RANK, parse_llm_json
 from .base import Agent
 from .contracts import AgentResult, Finding, PipelineContext, Severity, Status
 from .llm_provider import make_provider
@@ -94,32 +94,14 @@
             })
 
     # จัดลำดับ: cross_validated ก่อน, แล้ว CRITICAL > ERROR > WARNING > INFO, แล้วตัดที่ max_items
-    rank = {"CRITICAL": 3, "ERROR": 2, "WARNING": 1, "INFO": 0}
     cands.sort(key=lambda c: (c.get("cross_validated", False),
-                              rank.get(c.get("severity"), 0)), reverse=True)
+                              _RANK.get(c.get("severity"), 0)), reverse=True)
     return cands[:max_items]
 
 
 def _parse_llm_json(text: str) -> dict:
-    """แกะ JSON จากคำตอบ LLM แบบกันพัง (LLM อาจห่อด้วย ```json หรือมีข้อความนำ)."""
-    if not text:
-        return {}
-    # ตัด code fence
-    t = text.strip()
-    t = re.sub(r"^```(?:json)?\s*", "", t)
-    t = re.sub(r"\s*```$", "", t)
-    try:
-        return json.loads(t)
-    except Exception:
-        pass
-    # หา {...} ก้อนแรกที่ balance พอประมาณ
-    m = re.search(r"\{.*\}", t, re.DOTALL)
-    if m:
-        try:
-            return json.loads(m.group(0))
-        except Exception:
-            return {"summary": t[:500], "prioritized": []}
-    return {"summary": t[:500], "prioritized": []}
+    """แกะ JSON จากคำตอบ LLM (คง raw text เป็น summary ถ้าแกะไม่ได้ — พฤติกรรมเดิม)."""
+    return parse_llm_json(text, raw_fallback_key="summary")
 
 
 class AiReviewAgent(Agent):
@@ -151,7 +133,15 @@
             + json.dumps(candidates, ensure_ascii=False, indent=0)
         )
 
-        raw = provider.chat(_SYSTEM_PROMPT, user_prompt)
+        # LLM call เป็น I/O ที่อาจล้มชั่วคราว (timeout/เน็ตหลุด) แม้ available() ผ่านแล้ว.
+        # advisory → ล้ม = degrade เป็น SKIPPED (ไม่ใช่ ERROR ที่จะทำ SuperAgent มองว่าระบบพัง).
+        try:
+            raw = provider.chat(_SYSTEM_PROMPT, user_prompt)
+        except Exception as e:
+            return AgentResult(self.name, Status.SKIPPED.value,
+                               summary={"reason": f"เรียก Local LLM ไม่สำเร็จ (degrade graceful): "
+                                                  f"{type(e).__name__}: {str(e)[:120]}",
+                                        "provider": provider.info()})
         parsed = _parse_llm_json(raw)
 
         findings = []
--- a/agents/synthesis_agent.py
+++ b/agents/synthesis_agent.py
@@ -25,10 +25,10 @@
 from __future__ import annotations
 
 import json
-import re
 from collections import Counter, defaultdict
 from typing import Dict, List
 
+from ._shared import parse_llm_json
 from .base import Agent
 from .contracts import AgentResult, Finding, PipelineContext, Severity, Status
 from .llm_provider import make_provider
@@ -50,23 +50,8 @@
 
 
 def _parse_llm_json(text: str) -> dict:
-    """แกะ JSON จากคำตอบ LLM แบบกันพัง (เหมือน ai_review — LLM อาจห่อ ```json)."""
-    if not text:
-        return {}
-    t = text.strip()
-    t = re.sub(r"^```(?:json)?\s*", "", t)
-    t = re.sub(r"\s*```$", "", t)
-    try:
-        return json.loads(t)
-    except Exception:
-        pass
-    m = re.search(r"\{.*\}", t, re.DOTALL)
-    if m:
-        try:
-            return json.loads(m.group(0))
-        except Exception:
-            return {"overview": t[:500]}
-    return {"overview": t[:500]}
+    """แกะ JSON จากคำตอบ LLM (คง raw text เป็น overview ถ้าแกะไม่ได้ — พฤติกรรมเดิม)."""
+    return parse_llm_json(text, raw_fallback_key="overview")
 
 
 def _build_overview(ctx: PipelineContext) -> Dict:
@@ -145,7 +130,14 @@
             "ช่วยเขียนบทสรุปเชิงบริหารตาม schema ที่กำหนด:\n\n"
             + json.dumps(overview, ensure_ascii=False, indent=0)
         )
-        raw = provider.chat(_SYSTEM_PROMPT, user_prompt)
+        # LLM call อาจล้มชั่วคราวแม้ probe ผ่าน → ตกไปใช้สรุปสถิติ (degrade graceful) ไม่ใช่ ERROR
+        try:
+            raw = provider.chat(_SYSTEM_PROMPT, user_prompt)
+        except Exception as e:
+            return self._statistical_synthesis(
+                ctx, overview,
+                reason=f"เรียก Local LLM ไม่สำเร็จ ({type(e).__name__}) — ใช้สรุปสถิติแทน",
+                provider=provider.info())
         parsed = _parse_llm_json(raw)
 
         findings: List[Finding] = []
--- a/agents/super_agent.py
+++ b/agents/super_agent.py
@@ -29,16 +29,15 @@
 from __future__ import annotations
 
 import json
-import re
 from collections import defaultdict
 from typing import Dict, List, Optional
 
+from ._shared import SEV_RANK as _SEV_RANK, bill_key as _bkey, parse_llm_json
 from .base import Agent
 from .contracts import AgentResult, Finding, PipelineContext, Severity, Status
 from .llm_provider import make_provider
 
-# ลำดับความรุนแรง (ใช้จัดอันดับ + ให้น้ำหนัก) — ตรงกับทั้งระบบ
-_SEV_RANK = {"CRITICAL": 3, "ERROR": 2, "WARNING": 1, "INFO": 0}
+# น้ำหนักความรุนแรงเฉพาะของ super (สเกลคนละชุดกับ ConfidenceAgent โดยตั้งใจ — ใช้รวมคะแนนข้าม tier)
 _SEV_WEIGHT = {"CRITICAL": 40, "ERROR": 25, "WARNING": 12, "INFO": 4}
 
 # ผู้ตรวจอิสระชั้นต้น (การธงพร้อมกันหลายตัว = หลักฐานข้ามมุมมอง)
@@ -50,28 +49,9 @@
 _META = ("confidence", "crosscheck", "synthesis", "super")
 
 
-def _bkey(file: str, sheet: str, iv: str) -> str:
-    return f"{file}|{sheet}|{iv}"
-
-
 def _parse_llm_json(text: str) -> dict:
-    """แกะ JSON จากคำตอบ LLM แบบกันพัง (รูปแบบเดียวกับ synthesis/ai_review)."""
-    if not text:
-        return {}
-    t = text.strip()
-    t = re.sub(r"^```(?:json)?\s*", "", t)
-    t = re.sub(r"\s*```$", "", t)
-    try:
-        return json.loads(t)
-    except Exception:
-        pass
-    m = re.search(r"\{.*\}", t, re.DOTALL)
-    if m:
-        try:
-            return json.loads(m.group(0))
-        except Exception:
-            return {}
-    return {}
+    """แกะ JSON จากคำตอบ LLM แบบกันพัง (super ไม่คง raw text — ต้องการเฉพาะ schema)."""
+    return parse_llm_json(text)
 
 
 class SuperAgent(Agent):
@@ -323,7 +303,10 @@
             "ai_only_bills": summary["ai_only_bills"],
             "top_priorities": summary["top_priorities"][:5],
         }
-        raw = provider.chat(sys_prompt, json.dumps(payload, ensure_ascii=False, indent=0))
+        try:
+            raw = provider.chat(sys_prompt, json.dumps(payload, ensure_ascii=False, indent=0))
+        except Exception:
+            return None      # LLM ล้มชั่วคราว → ข้ามบทสรุป LLM (super ยังคืนผล deterministic ครบ)
         parsed = _parse_llm_json(raw)
         rec = str(parsed.get("recommendation", "")).strip()
         if not rec:
--- a/agents/mesh.py
+++ b/agents/mesh.py
@@ -23,12 +23,13 @@
 from collections import defaultdict
 from typing import Callable, Dict, List, Optional
 
+from ._shared import SEV_RANK as _RANK, bill_key
 from .contracts import Finding
 
 
 def _bill_key(f: Finding) -> str:
     """กุญแจอ้างอิงบิลของ finding (file|sheet|iv) — ใช้จับ cross-agent correlation."""
-    return f"{f.file}|{f.sheet}|{f.iv}"
+    return bill_key(f.file, f.sheet, f.iv)
 
 
 class FindingsMesh:
@@ -42,6 +43,8 @@
         self._findings: List[Finding] = []          # ลำดับ publish (deterministic by orchestrator)
         self._by_agent: Dict[str, List[Finding]] = defaultdict(list)
         self._by_bill: Dict[str, List[Finding]] = defaultdict(list)
+        # key -> (file, sheet, iv) ต้นฉบับ — เก็บไว้แทนการ split คีย์กลับ (กัน field มี '|' แล้วเพี้ยน)
+        self._bill_ref: Dict[str, tuple] = {}
 
     # ---------- publish (producer side) ----------
     def publish(self, findings: List[Finding]) -> int:
@@ -55,7 +58,9 @@
                 continue
             self._findings.append(f)
             self._by_agent[f.agent].append(f)
-            self._by_bill[_bill_key(f)].append(f)
+            k = _bill_key(f)
+            self._by_bill[k].append(f)
+            self._bill_ref.setdefault(k, (f.file, f.sheet, f.iv))
             n += 1
         return n
 
@@ -88,7 +93,7 @@
 
     def by_bill(self, file: str, sheet: str, iv: str) -> List[Finding]:
         """findings ทั้งหมดที่ชี้ไปยังบิลใบเดียวกัน (file/sheet/iv)."""
-        return list(self._by_bill.get(f"{file}|{sheet}|{iv}", []))
+        return list(self._by_bill.get(bill_key(file, sheet, iv), []))
 
     def since(self, seq: int) -> List[Finding]:
         """findings ตั้งแต่ index `seq` เป็นต้นไป (ให้ agent อ่าน 'ของใหม่หลังจุดหนึ่ง')."""
@@ -119,15 +124,14 @@
             }
         ใช้โดย CrossCheckAgent/ConfidenceAgent เพื่อยก/ลดความสำคัญแบบ cross-validated.
         """
-        rank = {"CRITICAL": 3, "ERROR": 2, "WARNING": 1, "INFO": 0}
         rows: List[Dict] = []
         for bkey, fs in self._by_bill.items():
             agents = sorted({f.agent for f in fs})
             if len(agents) < min_agents:
                 continue
-            file, sheet, iv = bkey.split("|", 2)
+            file, sheet, iv = self._bill_ref.get(bkey, (bkey, "", ""))
             codes = sorted({f.code for f in fs})
-            max_sev = max((f.severity for f in fs), key=lambda s: rank.get(s, 0))
+            max_sev = max((f.severity for f in fs), key=lambda s: _RANK.get(s, 0))
             rows.append({
                 "file": file, "sheet": sheet, "iv": iv,
                 "agents": agents, "n_agents": len(agents),
@@ -135,7 +139,7 @@
                 "findings": list(fs),
             })
         # เรียง deterministic: agent มากก่อน, แล้ว severity สูงก่อน, แล้ว bill key
-        rows.sort(key=lambda r: (-r["n_agents"], -rank.get(r["max_severity"], 0),
+        rows.sort(key=lambda r: (-r["n_agents"], -_RANK.get(r["max_severity"], 0),
                                  r["file"], r["sheet"], r["iv"]))
         return rows
 
--- a/agents/confidence_agent.py
+++ b/agents/confidence_agent.py
@@ -23,6 +23,7 @@
 
 import mesh_contract
 
+from ._shared import bill_key as _bkey
 from .base import Agent
 from .contracts import AgentResult, Finding, PipelineContext, Severity, Status
 
@@ -49,20 +50,22 @@
         low_conf_bills = set()
         for b in (ctx.bills or []):
             if b.get("amount_confidence") == "low":
-                k = f"{b.get('file','')}|{b.get('sheet','')}|{b.get('iv_number') or '-'}"
+                k = _bkey(b.get("file", ""), b.get("sheet", ""), b.get("iv_number") or "-")
                 low_conf_bills.add(k)
 
         # รวมหลักฐานต่อบิลจาก mesh (ไม่นับ finding ของ confidence เอง กัน feedback loop)
         per_bill_raw: Dict[str, float] = defaultdict(float)
         per_bill_agents: Dict[str, set] = defaultdict(set)
         per_bill_codes: Dict[str, set] = defaultdict(set)
+        per_bill_ref: Dict[str, tuple] = {}    # key -> (file,sheet,iv) ต้นฉบับ (กัน split เพี้ยน)
         for f in mesh.all():
             if f.agent == self.name:
                 continue
-            bkey = f"{f.file}|{f.sheet}|{f.iv}"
+            bkey = _bkey(f.file, f.sheet, f.iv)
             per_bill_raw[bkey] += _SEV_WEIGHT.get(f.severity, 1)
             per_bill_agents[bkey].add(f.agent)
             per_bill_codes[bkey].add(f.code)
+            per_bill_ref.setdefault(bkey, (f.file, f.sheet, f.iv))
 
         # บวกน้ำหนัก: จำนวน Tier-1 อิสระที่ธง (ยิ่งหลาย = ยิ่งมั่นใจ) + parser low-conf
         for bkey in list(per_bill_raw.keys()):
@@ -94,7 +97,7 @@
         for score, bkey, raw in scored:
             if score < top_pct:
                 continue
-            file, sheet, iv = bkey.split("|", 2)
+            file, sheet, iv = per_bill_ref.get(bkey, (bkey, "", ""))
             agents = sorted(per_bill_agents[bkey])
             sev = (Severity.CRITICAL.value if score >= 80 else
                    Severity.ERROR.value if score >= 60 else
--- a/agents/notepad_report.py
+++ b/agents/notepad_report.py
@@ -13,6 +13,8 @@
 from datetime import datetime
 from typing import Dict, List
 
+from ._shared import SEV_RANK as _SEV_RANK
+
 # ลำดับการแสดง (ตามลำดับ pipeline) + ข้อมูลกำกับราย agent
 _ORDER = ["import", "formula", "vat", "wht", "taxid",
           "crosscheck", "confidence", "ai_review", "synthesis", "super",
@@ -45,7 +47,6 @@
                    "role": "สรุปการทำงานของทุก agent เป็นไฟล์ .txt (รายงานนี้)"},
 }
 
-_SEV_RANK = {"CRITICAL": 3, "ERROR": 2, "WARNING": 1, "INFO": 0}
 _LINE = "=" * 70
 _SUB = "-" * 70
 
@@ -153,7 +154,10 @@
         out.append("   ▶ บิลที่ควรตรวจก่อน (priority รวมจากทุก tier):")
         for t in tp:
             ref = "/".join(x for x in (t.get("file", ""), t.get("sheet", ""), t.get("iv", "")) if x)
-            out.append(f"        อันดับ {t.get('rank'):>2}  [{t.get('priority'):>5}/100]  {ref}")
+            # ป้องกัน None ทำ format crash (latent): cast เป็น str ก่อนจัดความกว้าง — ผลปกติเหมือนเดิม
+            rank = t.get("rank", "-")
+            prio = t.get("priority", "-")
+            out.append(f"        อันดับ {str(rank):>2}  [{str(prio):>5}/100]  {ref}")
 
     if s.get("verdict"):
         out.append("")
--- a/config.py
+++ b/config.py
@@ -517,7 +517,9 @@
 )
 
 # v8.1: คำนำหน้าที่พิมพ์ผิดบ่อย (typo prefixes)
-COMPANY_TYPO_PREFIXES: tuple = ('บริษัส', 'บริสัท', 'บริษาท', 'บริษัส')
+# หมายเหตุ: ใช้เชิง membership/startswith เท่านั้น → ลำดับ/ตัวซ้ำไม่มีผลต่อผลตรวจ
+#   (เดิมมี 'บริษัส' ซ้ำ 2 ครั้ง — ตัดตัวซ้ำออก ผลเหมือนเดิมทุกประการ)
+COMPANY_TYPO_PREFIXES: tuple = ('บริษัส', 'บริสัท', 'บริษาท')
 
 # pre-compiled regex: ขึ้นต้นด้วย prefix ตัวใดตัวหนึ่ง
 # - เรียงจากยาวไปสั้น กัน 'บจก' แย่ง match ก่อน 'บจก.'
--- a/run_agents.py
+++ b/run_agents.py
@@ -116,7 +116,9 @@
         "llm_base_url": args.llm_base_url,
         "llm_model": args.llm_model,
         "ai_max_items": args.ai_max_items,
-        "source": "drive",
+        # หมายเหตุ: ตัวรันนี้ resolve file_list เองเสมอ (จาก --files/--data) แล้วส่งให้ pipeline
+        #   → ImportAgent ใช้ file_list ตรง ๆ ไม่แตะสาขา 'source' (เดิมตั้ง source='drive'
+        #     ซึ่งทั้ง dead และทำให้เข้าใจผิดว่าดึงจาก Google Drive). ละไว้เพื่อความตรงไปตรงมา.
     }
 
     logger = (lambda msg: None) if args.quiet else (lambda msg: print(msg))
