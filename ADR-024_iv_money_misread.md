# ADR-024 — กัน "ยอดเงินบนบิล" ถูกอ่านเป็นเลขที่เอกสาร (F-MONEYIV v9.3.1)

สถานะ : ACCEPTED — 17.06.2026
บริบท : ผู้ใช้พบ false positive 4 จุดในรายงานสรุปบริษัท "ไทย ยู คิง 69.05" (ไฟล์ SHS, 21 บิล)
ทั้งหมดสืบกลับได้เป็น **อาการปลายน้ำของรากเดียว** — เลขที่เอกสารถูกอ่านพลาดเป็นยอดเงิน

---

## อาการที่ผู้ใช้รายงาน (4 จุด — false positive ทั้งหมด)

1. "เลขที่เอกสาร 200500 ใช้ซ้ำ 04/05 & 12/05" — ผิด: 200,500 คือยอดก่อน VAT ไม่ใช่เลขเอกสาร
   เลขจริง = `05070` (วันที่ 4) และ `05309` (วันที่ 12) ซึ่งคนละเลขกัน
2. "202000 ฝังงวด 2020" — ผิด: 202,000 คือยอดก่อน VAT ของวันที่ 7 เลขจริง = `05171`
3. "204000 ฝังงวด 2040" — ผิด: 204,000 คือยอดก่อน VAT ของวันที่ 13 เลขจริง = `05351`
4. "05380/05414/05642/05677/05715 รีเช็ค" — ผิด: เป็นเลขเรียงปกติ ไม่มีอะไรผิด
   (เป็น collateral จากการที่เลขอื่นในชุดถูกแทนด้วยยอดเงิน ทำให้ลำดับดู "ผิด")

---

## Current risk (ความเสี่ยงปัจจุบัน ก่อนแก้)

ผู้ตรวจได้รายงานที่มี **เลขเอกสารปลอม (ยอดเงิน)** ปนกับเลขจริง → ส่งบัญชีต่อไม่ได้, เสียเวลา
ไล่เช็คด้วยมือทุกใบเพื่อแยกว่าอันไหน flag จริงอันไหนปลอม. กระทบความน่าเชื่อถือของระบบทั้งตัว
(ผู้ใช้เริ่มไม่เชื่อ output → ตรวจซ้ำเองหมด = ระบบหมดประโยชน์)

## Root cause (รากของปัญหา — สืบจาก source จริง ไม่เดา)

โครงไฟล์ SHS: `[4,20]` = เลขเอกสารจริง (string 5 หลัก เช่น '05070'),
`[16,22]/[17,22]/[18,22]` = ยอด subtotal/VAT/total (pandas อ่านยอดที่ลงตัวเป็น **int**)

ลำดับที่ทำให้พลาด (ยืนยันด้วยการรัน parser จริงทีละชั้น):
- `_parse_block` ตั้ง `header_end = min(row_start+20, row_end+1)` → ชีต 19 แถวถูกสแกน
  **ทั้งชีตรวมแถวยอดรวม** เข้าโซนหา iv
- `_pick_best_iv` (parser_p1.py) รับ candidate เลขล้วน ≥5 หลัก, score = len×2 + 15
  → subtotal 6 หลัก '200500' (score 27) **ชนะ** เลขเอกสารจริง 5 หลัก '05070' (score 25)
- money-guard เดิม (`_is_money`) จับเฉพาะเมื่อมี **จุดทศนิยมตามหลัง** (`\.\d`) แต่ pandas
  อ่านยอดที่ลงตัวเป็น int → สตริง '200500' ไม่มี '.0' → guard เดิม **ไม่ทำงาน**

จุดที่ "บังเอิญรอด" (ชีต 11–15,22): ยอด VAT/total มีหางทศนิยม float (เช่น 13535.900000000001)
→ ผลิต candidate ขยะคะแนนสูงกว่า → trip `apply_iv_lastresort` → คว้าเลขจริง 5 หลักได้พอดี
(เป็น **อุบัติเหตุ** ไม่ใช่ logic ที่ออกแบบ — เปราะมาก)

ทั้ง 4 false positive เป็นปลายน้ำของ misread เดียวนี้: เมื่อ iv = ยอดเงิน →
`_iv_check_cross_day` (IV003), `detect_iv_period_mismatch` (DT004/ฝังงวด), `_iv_check_ascending`
(IV004) ฟ้องตามกันหมด. **แก้รากเดียว = หายทั้ง 4**

## Long-term impact (ผลระยะยาวถ้าไม่แก้)

ทุกไฟล์ที่ยอดเงินเป็นเลขจำนวนเต็ม 5–6 หลัก และเลขเอกสารสั้นกว่า/อยู่ในโซน header เดียวกัน
จะเสี่ยงอาการนี้ในอนาคต (ขึ้นกับ layout ผู้ขายแต่ละราย) — เป็น **time bomb เชิง layout** ที่
จะโผล่เป็นระยะ ๆ ทุกครั้งที่รับผู้ขายรายใหม่ที่จัดหน้าตาราง "ยอดใกล้โซนเลขเอกสาร"

---

## Decision — Recommended solution (ทางแก้ที่เลือก)

บังคับ **business invariant**: "เลขที่เอกสาร ≠ ยอดเงินใด ๆ บนบิลเดียวกัน" (จริงเสมอเชิงธุรกิจ)

ทำเป็น post-guard `reject_iv_equal_amount(df, result, row_start, header_end, ncols)` ใน
`parser_guards.py` เรียกใน `_parse_block` **หลัง** `_pb_finalize_amounts` (step 8b):
เมื่อ iv (เฉพาะตัวเลข) == subtotal/vat/total หรือยอดรายการสินค้าใด → ล้าง iv แล้วเรียก
`apply_iv_lastresort_if_needed` ซึ่งกู้เลขจริงด้วยการให้คะแนนตามตำแหน่ง: '05070' ได้ +20
near-date bonus จาก cell วันที่ที่อยู่ติดกัน → ชนะยอดรายการใด ๆ

ทางเลือกที่ **ไม่** เลือก และเหตุผล:
- แก้ `_pick_best_iv` ให้ปฏิเสธเลขล้วน 6 หลัก → เสี่ยง golden ขยับ + อาจตัดเลขเอกสารจริง
  6 หลักของผู้ขายรายอื่น (เป็นการแก้ที่ต้นน้ำของ scorer ที่ corpus พึ่งพา)
- แก้ `header_end` ให้แคบลง → กระทบทุกไฟล์ที่ corpus พิสูจน์แล้ว → golden ขยับแน่
- ขยาย money-guard ให้จับ int ด้วย → ต้นน้ำ scorer เปลี่ยน → golden เสี่ยง

post-guard ที่เลือก **ไม่แตะ scorer/header เดิมเลย** ทำงานเป็นชั้นกรองท้ายสุดเฉพาะเมื่อ
invariant ถูกละเมิด → ความเสี่ยง golden = ศูนย์โดยการพิสูจน์ (ดูด้านล่าง)

## Migration risk (ความเสี่ยงการย้าย — พิสูจน์เป็นศูนย์)

สแกน corpus จริงทั้ง 834 บิล: **ไม่มีบิลใดที่ iv == ยอดเงินของตัวเอง (0 ราย)** →
เงื่อนไข `if` ของ guard **ไม่เคยเป็นจริงบน corpus** → guard dormant 100% → golden ไม่ขยับ
โดยโครงสร้าง (ไม่ใช่โดยบังเอิญ)

ยืนยันด้วยการรันจริงหลังแก้:
- `golden_master.py . /mnt/project` → `d6b23d127999e62c2a898554c012e60c1d8731dffec212770569fa6ec8818173`
  (ตรง canonical เป๊ะ) — stats เดิมทุกตัว: BILLS=834 FILES=106 fn=9 dup=1 iv_seq=28 iv_date=3 typos=42
- `regression_full.py . /mnt/project` → engine == agent == baseline ทั้งสาม = `d6b23d12…` ✅

## Priority level

**P1 (สูง)** — เป็น correctness bug ที่กระทบ output ที่ผู้ใช้เห็นโดยตรงและความเชื่อถือทั้งระบบ
แต่แก้แบบ golden-neutral พิสูจน์ได้ → ความเสี่ยงในการ ship ต่ำ

---

## การพิสูจน์ผล (verification ครบวงจร)

| ขั้น | คำสั่ง | ผล |
|------|--------|-----|
| ผลถูกต้อง | parse SHS 69.05 เพิ่ม (21 บิล) | เลขเรียง `05029…05746` ครบ, iv==ยอด = 0 |
| false positive หาย | check_invoice_sequence + check_iv_date_sequence + detect_iv_period_mismatch | 0 issue ทั้งหมด (เดิม 4) |
| regression unit | `test_iv_money_misread.py` | ✅ 9/9 (ลงทะเบียน CI gate [3b4]) |
| golden (master) | `golden_master.py . /mnt/project` | `d6b23d12…8173` ไม่ขยับ |
| golden (full) | `regression_full.py . /mnt/project` | engine==agent==baseline ✅ |
| CI gates | `run_ci.sh /mnt/project` | ผ่านทุก gate (0 ล้มเหลว) |

## ไฟล์ที่แก้ (surgical — 3 จุด)

- `parser_guards.py` : เพิ่ม `_bill_amount_strings()` + `reject_iv_equal_amount()` (leaf module ไม่มี cycle)
- `parser_p2.py` : (1) เพิ่ม import `reject_iv_equal_amount` (2) เรียกใน `_parse_block` step 8b
- `test_iv_money_misread.py` : เทสล็อก invariant (paste-and-verify + integration presence-gated)
- `run_ci.sh` : ลงทะเบียน gate [3b4]

## เงื่อนไขเปิด ADR ใหม่

หากอนาคตพบผู้ขายที่ใช้ "เลขเอกสารที่ตรงกับยอดเงินจริง" (กรณีพิเศษมาก) guard นี้จะกู้ผิด —
ให้เปิด ADR ใหม่ทบทวน invariant ก่อนแก้ guard เสมอ (ปัจจุบันยังไม่พบกรณีนี้ใน corpus)
