# ล่าบั๊ก ปุ้มปุ้ย v9.3.5 — พร้อมท์สำหรับ Claude Code

> วางไฟล์นี้ไว้ root ของโปรเจกต์ แล้วสั่ง Claude Code ว่า **"อ่าน `CLAUDE_CODE_BUGHUNT.md` แล้วเริ่มทำงานตามนั้น"**
>
> เอกสารนี้เขียนให้ AI อ่านเป็นคำสั่ง ไม่ใช่ให้คนอ่านเป็นสรุป · ทุกหัวข้อคือสิ่งที่ต้องทำจริง ไม่ใช่คำแนะนำ

---

## 0. บริบทที่ต้องรู้ก่อนแตะโค้ด

**ระบบนี้คืออะไร:** ตรวจสอบใบกำกับภาษีไทยจากไฟล์ Excel ที่ผ่าน OCR มา — ตรวจ VAT, ยอดเงิน, ข้อมูลบริษัท, ชื่อสินค้า, ลำดับเอกสาร แล้วออกรายงานให้ฝ่ายบัญชีใช้ส่งงานจริง

**เดิมพัน:** ถ้าระบบบอกว่า "ตรง" ทั้งที่ผิด → ผู้ใช้ส่งงานผิดให้ลูกค้า/สรรพากร → ตกงาน
ดังนั้น **false-clean (บอกว่าถูกทั้งที่ผิด) ร้ายแรงกว่า false-positive เสมอ**

**ขนาด:** Python 3.12 · ~42,000 บรรทัด · 234 ไฟล์ .py · offline 100% ไม่ต่อเน็ตขณะทำงาน

**สถานะ ณ ตอนส่งมอบ:** golden `814c7cf0…` · fixture `d05e5de3…` · corpus 146 ไฟล์ / 1,085 บิล
ผ่านครบ: pytest 131 สคริปต์ · regression · coverage (parser branch 85.3% / rules 86.6%) · invariants · perf ratio 1.91

---

## 1. กฎเหล็ก — ห้ามละเมิดเด็ดขาด

| # | กฎ | เหตุผล |
|---|---|---|
| 1 | **ห้ามแต่งค่าที่หายไป** — ไม่มีข้อมูล ให้คืน `None`/`UNKNOWN` แล้วฟ้อง ห้ามเดา ห้ามเติมค่าเฉลี่ย | ตัวเลขที่ระบบแต่งเอง = ตัวเลขปลอมในเอกสารภาษี |
| 2 | **ห้ามแก้ตัวเลขการเงินให้ "ตรง"** — เจอยอดไม่ลงตัวต้องฟ้อง ห้าม normalize ให้เท่ากัน | การซ่อมยอดเงินอัตโนมัติ = ทำลายหลักฐานการตรวจ |
| 3 | **ห้ามซ่อน finding เพื่อลด false positive** — ต้องแก้ที่ตัวกฎ (ต้นเหตุ) เท่านั้น | มี ADR-185 เป็นบทเรียน: `_HIDE_IN_SUMMARY` เคยทำให้รีพอร์ตขึ้น "ตรง" ทั้งที่มี 254 จุดค้าง |
| 4 | **ห้ามลด threshold / ปิดกฎ / ใส่ whitelist เพื่อให้เทสเขียว** | เกตที่ผ่านเพราะถูกลดมาตรฐาน = ไม่ได้ตรวจอะไรเลย |
| 5 | **ห้ามลบไฟล์ต้นฉบับ / เขียนทับหลักฐาน** — `_evidence/` เป็น append-only | |
| 6 | **หา root cause ก่อนแก้เสมอ** — ไล่เส้นทางความล้มเหลวให้เจอ แล้วอธิบายว่าซับซิสเต็มไหนทำหน้าที่พลาด | แก้อาการโดยไม่รู้ต้นเหตุ = บั๊กกลับมาในรูปอื่น |
| 7 | **ห้าม rebaseline golden เอง** — ต้องขออนุญาตเจ้าของระบบก่อนทุกครั้ง | golden คือ invariant ที่ตรึงพฤติกรรมทั้งระบบ |
| 8 | **แก้ทีละจุด → รันเกตครบ → ยืนยัน → ค่อยจุดถัดไป** ห้ามแก้รวดเดียวหลายจุดแล้วค่อยรัน | ถ้าเทสแดง จะแยกไม่ออกว่าจุดไหนทำ |
| 9 | **ผลลัพธ์ที่ไม่มีหลักฐานการรันแนบ = ถือว่าไม่เกิดขึ้น** — ห้ามเล่าว่า "แก้แล้วน่าจะผ่าน" | |
| 10 | **ห้ามรีไรต์ไฟล์ทั้งไฟล์** — แก้เฉพาะฟังก์ชัน/บล็อกที่เกี่ยว | |

---

## 2. ตั้งค่าสภาพแวดล้อม (ทำก่อนทุกอย่าง)

```bash
python3 --version                      # ต้องเป็น 3.12.x
pip install -r requirements.txt --break-system-packages
pip install pytest pytest-cov ruff black mypy --break-system-packages

export PYTHONHASHSEED=0                # บังคับ — ไม่ตั้ง = ผลไม่ deterministic
export PUOPUY_AUDIT_DATE=2026-06-02    # บังคับ — ตรึงวันที่ฐานของกฎ DT002
```

**เวอร์ชันไลบรารีต้องตรง pin เป๊ะ** (`pandas==2.2.2`, `numpy==2.2.6`, `xlrd==2.0.1`,
`openpyxl==3.1.5`, `rapidfuzz==3.10.1`) — ถ้าไม่ตรง golden จะไม่ reproduce แล้วจะไล่บั๊กผิดตัว

**ยืนยันว่าฐานเขียวก่อนเริ่มหาบั๊ก:**

```bash
python3 -m pytest -q                                    # ต้องผ่านทั้งหมด
python3 regression_full.py . /mnt/project               # engine == agent == baseline
python3 INVARIANTS/check_invariants.py                  # invariant ครบ
```

ถ้าด่านไหนแดงตั้งแต่ยังไม่แตะอะไร → **นั่นคือบั๊กแรกที่ต้องแก้ อย่าเพิ่งไปหาอย่างอื่น**

---

## 3. วิธีล่าบั๊กที่ได้ผลจริงกับระบบนี้

รอบก่อนหน้าเจอบั๊กจริง 13 จุดด้วยวิธีเหล่านี้ **เรียงตามอัตราการเจอจากมากไปน้อย**

### 3.1 เขียน oracle อิสระ แล้วเทียบกับที่ระบบฟ้อง ★★★★★

**หลักการ:** อย่าอ่านโค้ดแล้วเดาว่ามันถูก — เขียนโปรแกรมแยกที่อ่านไฟล์ Excel ดิบเอง
คำนวณเอง แล้วเทียบกับผลของระบบ **จุดที่สองฝั่งไม่ตรง คือบั๊ก**

```python
# โครงที่ใช้ได้จริง
import pandas as pd
df = pd.read_excel(path, sheet_name=sheet, header=None)   # header=None สำคัญ
M = df.to_numpy(dtype=object)
# → คำนวณสิ่งที่ต้องการเอง แล้วเทียบกับ bill dict ที่ engine ให้
```

โหลดผลของ engine มาเทียบ:
```python
import importlib, io, contextlib, glob
app = importlib.import_module('ปุ้มปุ้ย_ultimate_v9_modular')
with contextlib.redirect_stdout(io.StringIO()):
    app.reset_run_state()
    bills, fi = app.parse_all_files(sorted(glob.glob('/mnt/project/*.xls*')))
    for b in bills: app.compute_bill_confidence(b)
    app.run_audit_core(bills, {}, isolate=True)
```

**oracle ที่ควรเขียน (ทำครบทุกข้อ):**

| oracle | ตรวจอะไร | เคยเจอบั๊กอะไร |
|---|---|---|
| ยอด footer | `subtotal/vat/total` ที่ engine อ่าน ต้องตรงกับที่พิมพ์บนเอกสาร | ADR-179: อ่านผิดคอลัมน์ ได้ยอดรวมทั้งไฟล์มาเป็นยอดบิลเดียว |
| provenance | ทุกบิลต้องอ่านยอดจาก **เอกสาร** ไม่ใช่คำนวณเอง (`amount_source`) | ADR-182: 384 บิลใช้ผลรวมรายการเป็น subtotal → VAT001 เทียบตัวเอง = "ตรงหลอก" |
| บรรทัดรายการ | `qty × price = amount` ทีละบรรทัด ; ถ้าไม่ตรงต้องมีคอลัมน์ส่วนลดจริงที่กระทบยอดลงตัว | ADR-189: กฎ "เดา" ว่าเป็นส่วนลดแล้วข้ามเงียบ → ยอดผิด 1–50% ถูกกลืนทั้งแถบ |
| ความซื่อสัตย์ค่า | ทุกค่า (qty/price/amount/ชื่อ/หน่วย/เลขภาษี) ต้องมีอยู่จริงในเซลล์ของชีตนั้น | จับ parser ที่แต่งค่าหรือหยิบข้ามชีต |
| ครบชีต | ทุกชีตที่มีลายเซ็นใบกำกับ (เลขภาษี / เลข IV / อัตรา 7%) ต้องออกบิล | จับบิลหายเงียบทั้งใบ |
| เลขซ้ำ | เลข IV ซ้ำ — แยก "ผู้ขาย+ยอดเดียวกัน" (= นับซ้ำจริง) ออกจาก "เลขชนข้ามผู้ขาย" (= ปกติ) | ADR-191: นับเงินซ้ำ 7.46 ล้าน รีพอร์ตขึ้น "ตรง" ทุกช่อง |
| normalize เงียบ | ข้อความไหนที่ `normalize_text(raw) != raw` แบบไม่ใช่แค่ยุบช่องว่าง → ต้องมีกฎแจ้ง | ADR-190: อักขระไทยผิดรูปถูกกลบ 57 เซลล์ |
| เลขคณิต | `ผลรวมรายการ = subtotal` · `subtotal × 7% = vat` · `subtotal + vat = total` · บาทตัวอักษร = ตัวเลข | |
| เลขภาษี | checksum 13 หลัก (mod 11) · เลขเดียวกันชื่อบริษัทต่างกัน | |

### 3.2 ตามล่า "รหัสกฎที่ฟ้อง 0 ครั้ง" ★★★★☆

```bash
# นับว่ารหัสไหนไม่เคยฟ้องเลย แล้วถามว่า "เพราะข้อมูลสะอาด หรือเพราะกฎพัง"
```

สำหรับแต่ละรหัสที่เป็น 0 ต้องตอบให้ได้ว่า:
- ข้อมูลไม่มีเคสนั้นจริง (พิสูจน์ด้วย oracle) → **ปกติ**
- กฎมีเงื่อนไขที่ทำให้ไม่มีวันเข้า (เช่น ต้องมี ≥5 รายการ แต่บิลส่วนใหญ่มี 3) → **ข้อจำกัดโครงสร้าง ต้องบันทึก**
- กฎ `return []` ตั้งแต่ต้นเพราะ field เป็น `None` → **fail-open = บั๊ก ต้องแก้**

> เคสจริง: `ITM001` ฟ้อง 0 ทั้งที่มี 18 บรรทัดที่ `qty×price ≠ amount` — เพราะกฎเดาว่าเป็นส่วนลด

### 3.3 ตามล่า "เกตที่เขียวเพราะไม่ได้ตรวจ" (false-green) ★★★★☆

```bash
grep -rn "skip\|SKIP\|ImportError\|except Exception:\s*pass\|return \[\]" --include=*.py . | head -50
```

หาเทส/เกตที่ **ข้ามเงียบเมื่อไม่มีเครื่องมือ** แล้วติดตั้งเครื่องมือนั้นให้ครบ แล้วรันใหม่

> เคสจริง ADR-188: `test_typing_leaf.py` เขียวมาตลอดเพราะเครื่องไม่มี `mypy` — พอติดตั้งแล้วแดงทันที

ตรวจเพิ่ม: `except Exception: return []` ในกฎ — กลืน error แล้วรายงานว่า "ผ่าน"
ต้องแยกให้ได้ว่า "ไม่มีปัญหา" กับ "ตรวจไม่สำเร็จ" (ต้องเป็น `SYS-*` ไม่ใช่เงียบ)

### 3.4 ตรวจว่ารีพอร์ตพูดตรงกับข้อมูลจริง ★★★★☆

รีพอร์ตมีหลายฉบับ (`company_summary.txt`, `company_summary_ultra.txt`, `audit_v58_*.xlsx`,
`agent_report.txt`) — **ทุกฉบับต้องเล่าเรื่องเดียวกัน**

```bash
python3 main.py     # PUKPUI_AUTO=1 PUKPUI_DATA_DIR=... PUKPUI_REPORT_DIR=...
```

แล้วเช็ค:
- นับ finding ในบิล (lane `fix`/`check`) เทียบกับที่ขึ้นรีพอร์ต → **ต้องไม่ตกหล่นแม้จุดเดียว**
- ช่องไหนขึ้นคำว่า "ตรง" ต้องไม่มี finding ค้างในช่องนั้น
- ตัวเลขสรุปต้องบวกกันครบ (เช่น `ต้องแก้ + ควรตรวจ + ตรง = จำนวนกลุ่มทั้งหมด`)
- หน่วยนับต้องไม่ปน (จำนวน "บริษัท" กับ จำนวน "จุดที่ฟ้อง" ห้ามอยู่บรรทัดเดียวกันแบบไม่ระบุ)
- ข้อมูลที่มีเฉพาะในชีต Excel แต่ไม่มีในรีพอร์ตหลัก → **ถือว่าไม่ได้แจ้ง** (บทเรียน ADR-191)

### 3.5 ทดสอบขอบและความทนทาน ★★★☆☆

```bash
# ไฟล์พัง / ชีตว่าง / ค่า NaN / ยอดติดลบ / วันที่ไม่มีจริง / ข้อความยาวผิดปกติ
```

เกณฑ์: **ไฟล์เสียหาย 1 ไฟล์ ต้องไม่ล้มทั้ง batch** — ต้อง log แล้วไปต่อ (soft failure)
และต้องมี `SYS-*` บอกว่าตรวจไฟล์นั้นไม่สำเร็จ ไม่ใช่เงียบแล้วนับว่าผ่าน

### 3.6 ตรวจ determinism และ concurrency ★★★☆☆

```bash
python3 verify_report_det.py            # รัน 2 รอบ hash ต้องเท่ากัน
python3 verify_parallel.py /mnt/project 8   # serial == parallel
for s in 0 1 42; do PYTHONHASHSEED=$s python3 golden_master.py; done  # ต้องได้ hash เดียวกัน
```

---

## 4. เกตที่ต้องผ่านก่อนบอกว่า "เสร็จ"

รันตามลำดับนี้ ทุกครั้งหลังแก้แต่ละจุด:

```bash
export PYTHONHASHSEED=0 PUOPUY_AUDIT_DATE=2026-06-02

python3 -m pytest -q                                       # 1. เทสทั้งหมด
python3 regression_full.py . /mnt/project                  # 2. golden ข้อมูลจริง
python3 regression_full.py . tests/fixtures tests/fixtures/baseline_fixture.json   # 3. fixture
python3 verify_corpus_manifest.py                          # 4. แยก data drift ↔ code drift
python3 test_golden_single_source.py                       # 5. เอกสาร sync กับ baseline
python3 INVARIANTS/check_invariants.py                     # 6. invariant
python3 _audit_iv_truth.py /mnt/project                    # 7. เลขเอกสารตรงเซลล์จริง
python3 verify_parallel.py /mnt/project 8                  # 8. serial == parallel
PUOPUY_COV_BRANCH_MIN=85 python3 coverage_gate.py          # 9. line ≥90% branch ≥85%
python3 -m mypy agents/contracts.py agents/base.py offline_guard.py file_guard.py
python3 -m black --check <LINT_SCOPE ใน run_ci.sh>
bash run_ci.sh /mnt/project                                # 10. รวดเดียวทั้งชุด
```

**ถ้าเกตแดงเพราะโค้ดที่เพิ่งเพิ่มยังไม่มีเทสคลุม → เขียนเทสเพิ่ม ห้ามลดเพดาน**

---

## 5. ขั้นตอนเมื่อเจอบั๊ก (ทำตามลำดับนี้เท่านั้น)

1. **พิสูจน์ว่าเป็นบั๊กจริง** — เปิดไฟล์ Excel ดิบดูด้วยตาเอง คำนวณมือ ยืนยันว่าระบบผิด ไม่ใช่ข้อมูลผิด
2. **หา root cause** — ไล่ตั้งแต่ parser → rule → report ว่าจุดไหนทำหน้าที่พลาด
3. **วัดผลกระทบ** — บั๊กนี้กระทบกี่บิล กี่บาท เป็น false-clean หรือ false-positive
4. **แก้ให้แคบที่สุด** — เฉพาะฟังก์ชันที่เป็นต้นเหตุ ห้ามรีไรต์
5. **เขียนเทสที่ fail ก่อนแก้ / pass หลังแก้** — ต้องมีทั้งเคสบวกและเคสลบ (กันแก้เกิน)
6. **รันเกตข้อ 4 ให้ครบ**
7. **วัด diff** — เทียบรหัสที่ฟ้องก่อน/หลัง: อะไรหายไป (ควรเป็น FP) อะไรเพิ่มมา (ต้องพิสูจน์ว่าเป็น error จริง)
8. **บันทึก ADR** ต่อท้าย `INVARIANTS/DECISIONS.md` — อาการ / root cause / หลักฐานเชิงตัวเลข / การตัดสินใจ / ผล / ขอบเขตไฟล์
9. **ถ้า golden ขยับ → หยุด แล้วรายงานเจ้าของระบบก่อน** พร้อมแยกให้ชัดว่าเป็น data drift หรือ code drift

---

## 6. แผนที่ระบบ (ไปให้ถูกที่)

```
main.py                          จุดเข้า
ปุ้มปุ้ย_ultimate_v9_modular.py    orchestrator หลัก
pukpui_modular_funcs.py          ฟังก์ชันประกอบ + สรุป console

parser_p0.py / p0a / p1 / p2     ชั้นอ่านไฟล์  ← บั๊กเรื่อง "อ่านค่าผิด" อยู่ตรงนี้
  p0a: detect_item_columns, merge_continuation_bills
  p1 : _pb_scan_header, _pb_build_item
  p2 : _pb_money_col, _pb_footer_triple, _pb_amounts_from_vatrow
parser_guards.py                 ชั้นกันไฟล์พัง

rules_engine.py                  ทะเบียนกฎทั้งหมด (RULES dict)
rules_engine_rules_a/b/c.py      ตัวกฎ         ← บั๊กเรื่อง "ตรวจไม่เจอ/ฟ้องผิด" อยู่ตรงนี้
validators.py                    ตรวจความถูกต้องข้อมูล
puopuy_units.py                  หน่วยสินค้า + บาทตัวอักษร
puopuy_core.py                   normalize_text, has_hidden_chars, clean_tax_id

super_ultra_viewer.py            รีพอร์ตหลัก company_summary.txt  ← บั๊ก "ไม่แจ้ง" อยู่ตรงนี้
ultra_agent.py                   รีพอร์ตตรวจทานซ้ำ
reporting_p*.py                  Excel export
code_labels.py                   MAP รหัส→ช่อง/ป้าย/เลน
config.py / config_base.py       ค่าคงที่ + FIELD_CODES

agents/                          ชั้น AI (advisory เท่านั้น — ห้ามเปลี่ยนผลตรวจ)
INVARIANTS/DECISIONS.md          ADR ทั้งหมด (อ่านก่อนแก้อะไรที่เกี่ยวกับกฎ)
_evidence/                       baseline เก่า (append-only)
```

**invariant ที่ห้ามพัง:** `engine_hash == agent_hash` — ชั้น agent ต้องไม่เปลี่ยนผลตรวจแม้แต่บิตเดียว

---

## 7. บั๊กที่แก้ไปแล้ว — อย่าแก้ซ้ำ อย่าถอยกลับ

| ADR | บั๊ก | อย่าทำอะไร |
|---|---|---|
| 179 | `_dic_find_amt` เลือกคอลัมน์ขวาสุด → คว้ายอดรวมทั้งไฟล์ | อย่าถอด `_pb_money_col` |
| 180 | console นับ 2 ถังจาก 3 | |
| 181 | หัวรีพอร์ต Ultra ปนหน่วยนับ | |
| 182 | เอกสารไม่พิมพ์อัตรา 7% → footer ไม่ถูกอ่าน → VAT001 เทียบตัวเอง | อย่าถอด `_pb_footer_triple` |
| 183 | merge บิลข้ามหน้าเขียนทับยอดที่พิมพ์บนเอกสาร | ยอดจากเอกสารต้องชนะยอดที่คำนวณเองเสมอ |
| 184 | ITM015 ฟ้อง `Pcs.` ↔ `ท่อน` (หน่วยเดียวกันคนละภาษา) | อย่าถอด synonym อังกฤษออกจาก `_UNIT_SYNONYMS` |
| 185 | `_HIDE_IN_SUMMARY` ซ่อน 3 รหัส → รีพอร์ตขึ้น "ตรง" หลอก | **ห้ามใส่รหัสกลับเข้า `_HIDE_IN_SUMMARY` เด็ดขาด** |
| 186 | เลนก้ำกึ่ง "ตรวจตาเพิ่ม" ทำให้ช่องขึ้น "ตรง" | ห้ามเอาเลน soft กลับมา |
| 187 | rebaseline + แยก data drift ↔ code drift | |
| 188 | `test_typing_leaf` เขียวเพราะไม่มี mypy | ต้องติดตั้ง mypy ก่อนเชื่อว่าเขียว |
| 189 | ITM001 เดาว่าเป็นส่วนลดแล้วข้ามเงียบ | ส่วนลดต้องพิสูจน์จากเซลล์จริง (`it['discount']`) ห้ามกลับไปใช้ช่วง % |
| 190 | อักขระไทยผิดรูปถูก normalize กลบเงียบ | normalize ต่อได้ แต่ต้องแจ้ง (`CMP007`/`ADDR011`/`ITM004`) |
| 191 | ใบกำกับซ้ำข้ามไฟล์ → นับเงินซ้ำ 7.46 ล้าน | ห้ามจำกัด `r_doc003` กลับเป็น "ไฟล์เดียวกัน" |
| 192 | DOC003 ข้ามไฟล์ทำให้ cross-bill เป็น O(n²) | กฎ cross-bill ต้องเดินผ่านดัชนี ห้าม scan เต็ม batch |

รายละเอียดเต็มของทุก ADR อยู่ใน `INVARIANTS/DECISIONS.md` — **อ่านก่อนแตะกฎที่เกี่ยวข้อง**

---

## 8. จุดที่ยังค้าง (งานที่รอทำ)

1. **`ruff` แจ้ง 73 errors** — เท่ากันทั้งโค้ดเดิมและใหม่ เกิดจาก `ruff 0.16.1` ใหม่กว่าที่โปรเจกต์เคย lint ผ่าน
   ส่วนใหญ่เป็น `datetime.now()` ไม่ระบุ timezone และลำดับ import
   → เลือกทาง: pin ruff เวอร์ชันใน `requirements.txt` **หรือ** ไล่แก้ทีละข้อ · ไม่กระทบผลตรวจ

2. **`verify_parallel.py` ยังไม่เคยรันที่ `workers=8` บนเครื่องจริง** — sandbox ที่ทดสอบมี 1 CPU
   → รัน `python3 verify_parallel.py /mnt/project 8` บนเครื่องที่มีหลายคอร์

3. **ไม่มี `master_companies.json` จริง** — 99 กลุ่มจึงขึ้น "ไม่มีใน master (ตรวจไม่ได้)"
   ในช่อง ชื่อบจ./ที่อยู่/เลขภาษี/สาขา
   → ใส่ master จริงแล้วกฎตระกูล `CMP*` / `ADDR*` / `TAX*` / `BR*` จะทำงานเต็มความสามารถ

4. **`ITM015` เหลือ 100 จุด** — เป็นคู่หน่วยต่างจริง (`ม้วน↔แพ็ค`, `คิว↔ตัน`, `M↔กล่อง`, `ขวด↔ซอง`)
   → ถ้าตรวจแล้วพบว่าคู่ไหนเป็นเรื่องปกติของผู้ขาย ให้เพิ่มเข้า `_UNIT_SYNONYMS`
   **แก้ที่ต้นเหตุเท่านั้น ห้ามกลับไปใช้ `_HIDE_IN_SUMMARY`**

5. **`ITM008` (ราคาต่อหน่วยผิดปกติ) ครอบแค่ 192/1085 ใบ** เพราะต้องมี ≥5 รายการในใบเดียว
   → พิจารณาเพิ่มการตรวจ outlier **ข้ามบิล** แต่ต้องระวัง false positive จากสินค้าชื่อกว้างต่างสเปก
   (เคยลองแล้วพบ 13 จุด ล้วนเป็น `Distribution frame` / `ผ้า` / `Flexible fiber` ที่ต่างสเปกจริง)

6. **`vendor/wheels` ถูกถอดออกแล้ว** (73 MB) — ถ้าต้องติดตั้ง offline ให้ `pip download` ตาม pin ก่อน
   วิธีอยู่หัวไฟล์ `requirements.txt`

---

## 9. รูปแบบรายงานที่ต้องส่งกลับ

ทุกครั้งที่จบงานหนึ่งรอบ ให้รายงานตามนี้ **ห้ามข้ามข้อไหน**

```
## บั๊กที่เจอ N จุด

### [ความรุนแรง] ชื่อบั๊ก — ไฟล์ที่เกี่ยวข้อง
อาการ    : ระบบทำอะไร / ควรทำอะไร
หลักฐาน  : ไฟล์ + ชีต + เซลล์ + ตัวเลขที่คำนวณมือแล้วเทียบ
Root cause: ฟังก์ชันไหน บรรทัดไหน ทำไมถึงพลาด
ผลกระทบ  : กี่บิล กี่บาท เป็น false-clean หรือ false-positive
การแก้    : แก้อะไร ทำไมเลือกวิธีนี้ ทางเลือกอื่นที่ไม่เลือกเพราะอะไร
เทส      : เทสที่เพิ่ม ครอบเคสอะไรบ้าง
diff     : รหัสที่หายไป / รหัสที่เพิ่มมา พร้อมเหตุผลรายตัว

## ผลการรันเกต
[ตารางผลทุกเกตในข้อ 4 พร้อมสถานะ]

## golden
ก่อน = ... / หลัง = ...  (ถ้าขยับ ต้องอธิบายว่าเพราะอะไร และขออนุญาตก่อน)

## ที่ยังแก้ไม่ได้
[บอกตรง ๆ พร้อมเหตุผล ห้ามปิดบัง]
```

---

## 10. สิ่งที่จะทำให้งานนี้ล้มเหลว (อย่าทำ)

- อ่านโค้ดแล้วสรุปว่า "ดูถูกต้องดี" โดยไม่รันเทียบกับไฟล์จริง
- แก้ให้เทสเขียวโดยไม่เข้าใจว่าทำไมมันแดง
- เพิ่ม `try/except` ครอบเพื่อไม่ให้ error โผล่
- ใส่ค่า default แทนค่าที่อ่านไม่ได้
- ลด threshold / ปิดกฎ / ใส่ whitelist เพื่อลดจำนวน finding
- rebaseline golden เองโดยไม่ขออนุญาต
- รายงานว่า "แก้แล้ว" โดยไม่แนบผลรัน
- แก้หลายจุดพร้อมกันแล้วรันเกตครั้งเดียว

---

## 11. คำสั่งเริ่มงาน

> อ่าน `CLAUDE_CODE_BUGHUNT.md` ทั้งไฟล์
> ตั้งสภาพแวดล้อมตามข้อ 2 แล้วยืนยันว่าฐานเขียว
> เขียน oracle อิสระตามข้อ 3.1 ให้ครบทุกตัว รันกับ corpus จริงใน `/mnt/project`
> รายงานจุดที่ระบบ **ผิดจริงแต่ไม่แจ้ง** ก่อนเป็นอันดับแรก
> แล้วไล่แก้ทีละจุดตามข้อ 5 พร้อมรันเกตข้อ 4 ครบทุกครั้ง
> ห้าม rebaseline golden เอง — ถ้า golden ขยับให้หยุดแล้วรายงาน
