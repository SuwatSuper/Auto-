# ADR-041 — กัน "หางทศนิยม floating-point ของยอดเงิน" ถูกอ่านเป็นเลขที่เอกสาร (F-IV-FLOATTAIL v9.3.2)

สถานะ : ACCEPTED — 17.06.2026
บริบท : ผู้ใช้ส่งไฟล์จริง `TNT_69.041.xls` (บริษัท ซิ่น ด๋า จำกัด, 75 ชีต = 75 บิล) มาถามว่า
ระบบอ่านเลขที่เอกสารได้ไหม. รัน parser จริงพบว่าอ่านถูก **72/75** ใบ แต่ **3 ใบอ่านพลาด**
เป็นเลขขยะ — สืบกลับได้เป็นอาการที่ ADR-024 เคยเตือนไว้ว่าเป็น "อุบัติเหตุเปราะ ๆ"

---

## อาการ (3 ใบจาก 75 — อ่านเลขที่เอกสารผิด)

| ชีต      | เลขจริง `[4,20]` | ระบบอ่านได้ผิดเป็น | ที่มาของเลขขยะ           |
|----------|------------------|---------------------|--------------------------|
| `17 (3)` | `04496`          | `04000000001`       | total `75191.04000000001`|
| `17 (5)` | `04498`          | `08000000002`       | total `239941.08000000002`|
| `25 (2)` | `04723`          | `08000000002`       | total `239941.08000000002`|

72 ใบที่เหลืออ่านถูกหมด (`04085`…`04900`).

## Root cause (รากของปัญหา — ยืนยันด้วยการรัน parser จริงทีละชั้น)

โครงไฟล์: `[4,20]` = เลขเอกสารจริง (string 5 หลัก เช่น '04496'),
`[18,22]` = ยอดรวม — pandas/xlrd อ่านเป็น **float** ที่มีเศษ floating-point เช่น
`75191.04000000001` (ค่าจริงคือ 75191.04 แต่ float แทนเป๊ะไม่ได้).

ลำดับที่ทำให้พลาด:
- `_parse_block` ตั้ง `header_end = min(row_start+20, row_end+1)` → แถวยอดรวม (row 18) อยู่ในโซนหา iv
- `normalize_text(75191.04000000001)` → สตริง `'75191.04000000001'`
- `_pick_best_iv` (parser_p1.py) regex `\b(...\d{2,}...)\b` แตกสตริงเป็น 2 candidate:
  `'75191'` (หน้าจุด) และ `'04000000001'` (หลังจุด, 11 หลัก)
  - `'75191'` → ติด money-guard เดิม (`_is_money`: มี `.\d` ตามหลัง) → −40 → ตก ✓
  - `'04000000001'` → **ไม่ติด** money-guard เดิม (มันอยู่ "หลังจุด" ไม่ใช่ "หน้าจุด")
    score = 11×2 + 15(pos) = **37** > เลขเอกสารจริง 5 หลัก '04496' (score 25) → **ชนะ**
  - หมายเหตุ: หาง 12 หลัก (เช่น `'000000000001'`) จะติดโทษ `\d{12,13}` (−30) จึงรอด —
    เฉพาะหาง **11 หลัก** ที่หลุดทุกการ์ด (นี่คือ 3 ใบที่พลาด)
- `reject_iv_equal_amount` (F-MONEYIV, ADR-024) ก็ไม่จับ เพราะ `'04000000001'` ไม่ "เท่ากับ"
  ยอดเงินใด ๆ บนบิล (มันเป็นแค่ "เศษหาง" ไม่ใช่ตัวยอดเต็ม) → เลขขยะรอดออกมาเป็น `iv_number`

ADR-024 บันทึกไว้ตรงตัวว่า การที่หางทศนิยม float ผลิต "candidate ขยะคะแนนสูง" เป็น
**"อุบัติเหตุ ไม่ใช่ logic ที่ออกแบบ — เปราะมาก"**. ไฟล์ SHS หางช่วยให้ trip lastresort คว้าเลขจริง;
ไฟล์ TNT นี้หาง 11 หลักกลับชนะเลขจริงโดยตรง → อุบัติเหตุเดียวกัน "กลับด้าน" เป็น false read.

## Long-term impact (ถ้าไม่แก้)

ทุกไฟล์ที่ยอด VAT/total มีเศษทศนิยมที่ float แทนไม่ลงตัว (พบบ่อยมากกับยอด × 0.07) และเลขเอกสาร
สั้นกว่า/อยู่โซน header เดียวกัน เสี่ยงอ่านเลขเอกสารพลาดเงียบ ๆ — เป็น layout time-bomb แบบเดียว
กับ ADR-024 แต่ผ่านช่องทาง "หางทศนิยม" ที่ guard เดิมยังไม่ครอบ.

---

## Decision — ทางแก้ที่เลือก (minimal, golden-neutral)

ขยายหลักการเดิมของ FIX-IV-MONEY (ADR-024): **"เลขที่เอกสาร ≠ ส่วนหนึ่งของจำนวนเงิน"**
ให้ครอบ "หางทศนิยม" ด้วย. ใน `_pick_best_iv` (parser_p1.py) และ twin `_pick_best_iv_safe`
(parser_p0.py) เพิ่มเงื่อนไข `_is_dec_tail`:

```python
_is_dec_tail = (cand[:1].isdigit() and pos >= 2 and s[pos-1] == '.' and s[pos-2].isdigit())
```

ตีความ: candidate ที่ขึ้นต้นด้วยตัวเลข และ "ตัวอักษรหน้ามัน = จุด" และ "ก่อนจุดเป็นตัวเลข"
→ คือเศษหลังจุดทศนิยมของ *จำนวนเลข* (ยอดเงิน) → หักเท่ากับ money (−40) → ตกเกณฑ์ → ถูกทิ้ง.

เงื่อนไข `s[pos-2].isdigit()` สำคัญ: กันเคสเลขเอกสารที่มี "อักษร" นำหน้าจุด เช่น `DOC.04085`
(หน้าจุดเป็น 'C' ไม่ใช่ตัวเลข) → **ไม่โดนหัก** ตามเจตนา.

ทำไมไม่แก้ `normalize_text`/รอบอ่านเซลล์: `normalize_text` เป็น leaf hot-loop (35k+ ครั้ง/ชุด)
ใช้ทั่วทั้งระบบ — เปลี่ยน format float เสี่ยงทำ golden hash ขยับเป็นวงกว้าง. แก้ที่ตัวให้คะแนน iv
จุดเดียว = surgical + golden-neutral.

## Verification (พิสูจน์จริง ไม่เดา)

- ไฟล์ `TNT_69.041.xls`: ก่อนแก้ 72/75 → **หลังแก้ 75/75** (iv set == raw `[4,20]` set ทุกใบ,
  ไม่มี missing / ไม่มีเลขขยะ)
- Golden-neutral (รันในเครื่องนี้ env เดียวกัน ก่อน/หลังแก้ byte-identical):
  - `tests/real_cases` hash = `95852c68…84b4` (ไม่ขยับ)
  - `tests/fixtures`  hash = `269ddaed…34c1` (ไม่ขยับ)
- เทสครบชุด `test_*.py` : **76/76 ผ่าน** (รวม `test_parser_branch` money-penalty,
  `test_file_size_ceiling` ≤600 LOC, `test_package_integrity`)

หมายเหตุ env: เครื่องตรวจนี้เป็น Python 3.11 / pandas 3.0 (ไม่ตรง pin 3.12 / pandas 2.2.2)
จึง reproduce **official** golden hash ไม่ได้ (version gate บล็อกตามดีไซน์). การพิสูจน์ golden-neutral
ข้างต้นจึงใช้ baseline ที่ snapshot ในเครื่องนี้ก่อนแก้ แล้วเทียบ byte-identical หลังแก้.
แนะนำให้เจ้าของระบบรัน `bash run_ci.sh /path/to/data` บน env ที่ pin ตรง เพื่อยืนยัน official hash อีกชั้น.
