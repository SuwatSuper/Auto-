# AUDIT v9.2 — รายงานตรวจคุณภาพ + แก้บั๊ก (ปุ้มปุ้ย invoice audit)

> ⏮️ **เอกสารนี้เป็นบันทึกอดีต (ลงวันที่ 2026-06)** — ค่า hash ที่อ้างในนี้ (เช่น `73f5bf87`, `f1ac8421`)
> ถูกต้อง *ณ เวลาที่เขียน* แต่ **ไม่ใช่ golden ปัจจุบัน**. golden จริงตอนนี้ = `baseline.json._sha256`
> (ปัจจุบัน `35b2f7c8…`). เห็นค่าไม่ตรงในเอกสารเก่า = ปกติ ไม่ใช่บั๊ก — สรุป hash ทุกตัวที่ **`GOLDEN.md`**.

> บทบาท: Principal Software Architect / Production Reliability Engineer / Code Auditor
> ขอบเขต: **ไม่เพิ่มฟีเจอร์** — เพิ่มความเสถียร/เชื่อถือได้/ดูแลรักษาได้ และแก้บั๊กที่ทำให้ผลตรวจผิด
> วันที่: 2026-06 · ชุดข้อมูลพิสูจน์: 106 ไฟล์จริง (/mnt/project), 834 บิล

---

## บทสรุปผู้บริหาร

ระบบ **ผ่าน CI ทุกขั้น (30+ เทส) และ golden hash เดิม `f1ac8421…` ตรง** — แต่ผลตรวจบน
ไฟล์จริงกลับ **ผิด** ตามที่ลูกค้าแจ้ง. นี่คืออาการคลาสสิกของ *"เทสและ golden ล็อกพฤติกรรมที่ผิดไว้"*:
ชุดทดสอบ (fixture 3 บิล) ไม่ครอบคลุมเคสจริง (บิลหลายหน้า / จำนวนสินค้า = 7 / ยอดเป็นตัวอักษร)
จึงไม่มีอะไรจับบั๊กได้เลย.

แก้ **4 บั๊ก** (ราก 3 + ปรับ noise 1), วัด blast radius บนไฟล์จริงทั้งหมด, ยืนยันทุกการเปลี่ยน
เป็นการ "แก้ให้ถูก" (ไม่มี regression), แล้ว **สร้าง golden baseline ใหม่อย่างตั้งใจ**:

| | เดิม | ใหม่ (v9.2) |
|---|---|---|
| golden hash (106 ไฟล์) | `f1ac8421…` | **`73f5bf87…`** |
| จำนวนบิล | 836 | **834** (−2 จากการรวมหน้าต่อที่เคยนับซ้ำ) |
| ไฟล์ที่ผลเปลี่ยน | — | 3/106 (ทั้งหมดถูกต้องขึ้น) |
| เทสที่เพิ่ม | — | `test_v9_2_fixes.py` (32 เช็ค) |

`engine == agent == baseline` และ `serial == parallel` ที่ hash ใหม่ — reproduce ได้ 100%.

---

## BUG-1 (P0) — บิลหน้าต่อ parse ไม่ครบ → บิลซ้ำ / ยอดรวมเกินจริง

ตรงกับที่ลูกค้าแจ้ง: *"ยอดเงินรวมก็ไม่ตรง 10266200 แต่จริง ๆ มัน 10 ล้านถ้วน"* และ *"เอกสารนับเป็นบิล เท่านั้น"*

- **Current risk (สูงสุด):** ยอดรวมต่อผู้ขาย/เดือน **ผิด** และจำนวนบิล **ผิด** — ข้อมูลที่ก๊อปให้ลูกค้าใช้ไม่ได้.
  พิสูจน์กับ `TKH_69_0462.xls`: บิล IV `CB69040146` ที่ถูกต้องคือ 1 ใบ (รายการ #1-12, subtotal 1,530,235.72)
  แต่ระบบเดิมแตกเป็น **2 บิล** — ใบหนึ่งรายการขาด (เหลือ #1-8) อีกใบเป็น "บิลผี" (0 รายการ แต่มี subtotal 1,530,236)
  → ถ้ารวมยอดจะ **นับซ้ำ** (1.44M + 1.53M).
- **Root cause:** `parser_p0._dic_find_seq` ใช้เงื่อนไข `ints[0] == 1` — บังคับว่าคอลัมน์ "ลำดับสินค้า"
  ต้อง **เริ่มที่ 1**. หน้าต่อของบิล (ลำดับเริ่ม #9,10,11,12) จึงถูกปฏิเสธ; ระบบไป lock
  คอลัมน์ `qty` (ที่บังเอิญเป็น 1,1,1,1) เป็น seq แทน ซึ่งอยู่ **ขวา** ของคอลัมน์ชื่อ →
  `_dic_find_name` (หาเฉพาะขวาของ seq) หาไม่เจอ → ดึงรายการได้ 0 → `merge_continuation_bills`
  (ต้องมีลำดับต่อเนื่อง #8→#9) **รวมไม่ได้** เพราะหน้าต่อไม่มีรายการให้เทียบ.
- **Long-term impact:** ทุกบิลที่ยาวเกิน 1 หน้า (พบมากในผู้ขายรายใหญ่ ยอดสูง) จะผิดเงียบ — ยิ่งบิลใหญ่ ยอดยิ่งเพี้ยนมาก.
- **Recommended solution (ทำแล้ว):** เพิ่ม `_is_seq_run(ints)` ที่รับลำดับ **ไม่ต้องเริ่มที่ 1**
  โดยใช้เกณฑ์ *non-decreasing + มี span จริง (ปลาย>ต้น) + ช่วงแน่น (max-min+1 ≤ n+2)*:
  - รับ `[9,10,11,12]` (หน้าต่อ) และทน `[1,2,3,4,5,5,6,7]` (คนคีย์ลำดับซ้ำ — เคสจริงใน KTV_68_041)
  - กัน `[1,1,1,1]` (qty ค่าเดียวซ้ำ — เดิมหลุดเป็น seq ผิด เป็นบั๊กแฝงอยู่แล้ว) และ `[20,10,…]`
  - คงพฤติกรรมเดิมของลำดับเริ่ม 1 ทุกกรณี (รวม gap เล็ก `[1,2,4]`)
- **Migration risk (ต่ำ):** กระทบ 3/106 ไฟล์ ทั้งหมดเป็น "แก้ให้ถูก" (TKH_69_0462: 12 รายการ subtotal ตรงเอกสาร;
  TKH_69_06: รวม 16 รายการ; KNT_69_051 ช.23: 2→4 รายการ subtotal 15,102→60,428). เทสเดิมไม่พังสักตัว.
- **Priority: P0 (กระทบความถูกต้องของเงิน)**

---

## BUG-2 (P0) — `_detect_vat_rows` ตัดบล็อกผิด (จำนวนสินค้า = 7 ถูกเข้าใจเป็นแถว VAT)

บั๊กรากที่ "ซ่อนใต้" BUG-1 — ถูกเปิดโปงตอน BUG-1 ดึงรายการหน้าต่อกลับมาได้

- **Current risk (สูง):** บิลใบเดียวถูก **หั่นเป็นหลายบล็อก** กลางรายการ ทุกครั้งที่มีรายการจำนวน = 7
  → รายการหาย / บิลซ้ำ. พิสูจน์: `KNT_69_051.xls` ช.23 (บิลเดียว 4 รายการ subtotal 60,428)
  มีรายการ #2,#3 ที่ qty = 7 → ถูกตัดเป็นหลายบล็อก.
- **Root cause:** ลิสต์ VAT marker เดิมคือ `['0.07','7%','.07','7.00','7','7%','7 %']` — มี `'7'` และ `'7.00'`
  ซึ่ง **ชนกับ "จำนวนสินค้า = 7"**. โค้ดสแกนทุกเซลล์ทุกแถวหา `'7'` โดยไม่ดูบริบท →
  แถวรายการที่ qty=7 ถูกตีว่าเป็นแถว VAT. (พิสูจน์เชิงประจักษ์: เซลล์ที่เป็น `'7'`/`'7.00'` เป๊ะ
  ในคอร์ปัส = **49 เซลล์ และอยู่ใน "แถวรายการ" ทั้งหมด ไม่ใช่แถว VAT สักเซลล์**; VAT จริง 684 เซลล์
  เก็บเป็น numeric 0.07).
- **Long-term impact:** กระทบเงียบทุกไฟล์ที่มีจำนวนสินค้า = 7 (พบบ่อยมาก) — เป็นต้นเหตุร่วมของ
  ทั้ง "ยอดเพี้ยน" และ "บิลซ้ำ".
- **Recommended solution (ทำแล้ว):** ทำ `_detect_vat_rows` ให้ **รู้บริบท** — รับ `'7'`/`'7.00'`
  เฉพาะเมื่อแถวนั้นมี "คำบ่งบอกภาษี" (ภาษีมูลค่าเพิ่ม/VAT/ร้อยละ/มูลค่าเพิ่ม); ส่วน 0.07 (numeric)
  และรูปแบบ `%`/`.07` ยังจับเสมอ (ไม่ต้องมี label). วิธีนี้ **คงเจตนาเทสเดิม** (แถว `ภาษีมูลค่าเพิ่ม | 7`
  ยังตรวจเป็น VAT) แต่ตัดการชนกับจำนวนสินค้า.
- **Migration risk (ต่ำมาก):** ไม่มี regression (49 เซลล์ที่เคยฟ้องผิดล้วนเป็นแถวรายการ); เทส `test_parser_helpers`
  ที่ตั้งใจทดสอบ "label ภาษี + 7 = VAT" ยังผ่าน.
- **Priority: P0**

---

## BUG-3 (P1) — บาทตัวอักษรถูกนับเป็น "รายการสินค้า" + ฟ้อง ITM016 ผิด

ตรงกับที่ลูกค้าแจ้ง: *"ITM01 พบรายการ 'สองแสนแปดหมื่นสี่พันแปดร้อยสาม' แต่คนคีย์ข้อมูลไม่ระบุเลขลำดับ —
อันนี่มันเป็นยอดตัวอักษรไทยที่มาจากยอดเงินรวม ... มันไม่ใช่รายการสินค้า"*

- **Current risk (กลาง):** เกิด false positive (ITM016 "เลขลำดับหาย") และอาจทำให้ยอด/จำนวนรายการเพี้ยน
  ถ้าบาทตัวอักษรตกในบล็อกที่มีตัวเลขด้านขวา. (คอร์ปัสนี้มีเซลล์บาทตัวอักษร **723 เซลล์** — ยังไม่ระเบิด
  เพราะอยู่แถวท้ายบิล แต่เป็นบั๊กแฝงที่พร้อมเกิดในไฟล์อื่นของลูกค้า ตามที่แจ้งมา).
- **Root cause:** `parser_p2._pb_extract_items` — เมื่อ `seq` ว่างแต่มีข้อความยาว ≥3 และมียอด > 0
  จะสร้าง item ทันทีโดย **ไม่ตรวจว่าข้อความนั้นเป็น "ยอดเงินเป็นตัวอักษร"** (เช่น
  "สองแสนแปดหมื่นสี่พันแปดร้อยสาม"). หมายเหตุ: ตัวแปลงเดิม `puopuy_ingest.thai_words_to_number`
  บังคับต้องมีคำว่า "บาท" → จับเคสยอดเป็นคำล้วนไม่ได้.
- **Long-term impact:** noise ใน Error Report ที่ลูกค้าต้องมานั่งกรองเองทุกบิล — ขัดกับเป้าหมาย
  "ก๊อปข้อมูลไปให้ลูกค้าได้เลย 100%".
- **Recommended solution (ทำแล้ว):** เพิ่ม `_is_thai_amount_words(s)` ที่จับ "จำนวนเป็นคำไทย" แม้ไม่มี "บาท"
  (เกณฑ์: พบคำเลขไทย ≥3 คำ และตัดคำเลข/หน่วยเงินออกแล้วเหลือเศษ ≤2 อักขระ). ใน `_pb_extract_items`
  ถ้า name เป็นบาทตัวอักษร → **ข้าม** (ไม่นับเป็นรายการ ไม่ฟ้อง ITM016). ทดสอบแล้ว: ยอดเป็นคำ 3 แบบ → จับได้;
  ชื่อสินค้า (รวม "...1500 บาร์", "ตราว่าว แป้ง 1 กก.") → ไม่โดน.
- **Migration risk (ต่ำ):** คอร์ปัสปัจจุบันผล parse ไม่เปลี่ยน (ITM016 ยังคงที่). guard ทำงานเฉพาะ
  branch "seq ว่าง" เท่านั้น.
- **Priority: P1**

---

## BUG-4 (P2) — ITM008 "ราคา outlier" สร้าง false positive (ปิดตามคำขอ)

ตรงกับที่ลูกค้าแจ้ง: *"#3: ราคา/หน่วย 4,590.00 ผิดปกติ (median=58.00, 79.1×) เนี่ยมันไม่ใช่ Agent
ทำงานกันยังไง อันนี้เคยบอกหลายรอบละ"*

- **Current risk (กลาง — noise):** รายการที่ `qty×price = amount` **ถูกต้อง** แต่ "ราคา/หน่วยสูง"
  (สินค้าแพงปกติในบิลจริง) ถูกฟ้องว่า "ผิดปกติ" — เป็น false positive ที่ลูกค้าปฏิเสธซ้ำหลายรอบ.
- **Root cause:** กฎ `r_itm008` ออกแบบมาจับ "คอลัมน์สลับ" แต่ gate `qty×price≈amount` (≥80%)
  ได้พิสูจน์ว่าคอลัมน์ถูกแล้ว → การฟ้องราคาสูงต่อจากนั้นเป็น noise ล้วน (precision ต่ำ).
- **Long-term impact:** ลดความเชื่อถือของรายงาน (alert fatigue) — ลูกค้าเริ่มเพิกเฉยทุก WARNING.
- **Recommended solution (ทำแล้ว):** ตั้ง `RULES['ITM008'].enabled = False` (แบบเดียวกับ VAT010/BR003
  ที่ปิด "ตามคำขอ"). `run_rules` ข้ามกฎที่ปิด; ฟังก์ชัน `r_itm008` ยังอยู่ครบ — **เปิดคืนได้** ถ้าต้องการ.
  ความผิดเลขจริง (qty×price ไม่ตรง amount) มี **ITM001** จับแทนอยู่แล้ว.
- **Migration risk (ต่ำมาก):** เป็น pure check ไม่มี side-effect; ลบเฉพาะ "การปล่อย issue" ไม่กระทบ parse.
- **Priority: P2 (คุณภาพรายงาน/precision)**

---

## ข้อสังเกตเชิงสถาปัตยกรรม (ยังไม่แก้ — ต้องขออนุมัติก่อน เพราะเป็นการเปลี่ยนพฤติกรรม/ฟีเจอร์)

1. **เทสไม่ครอบคลุมเคสจริง (ความเสี่ยงเชิงระบบ — P1).** *Root cause:* fixture มีแค่ 3 บิลหน้าเดียว →
   golden hash ให้ "ความมั่นใจปลอม". *แนะนำ:* เพิ่ม fixture ที่มี (ก) บิลหลายหน้า (ข) จำนวน=7
   (ค) บาทตัวอักษรในบล็อก (ง) ลำดับซ้ำ — `test_v9_2_fixes.py` เป็นจุดเริ่ม ควรย้ายเคสเหล่านี้เข้า fixture golden ด้วย.
2. **`detect_item_columns_safe` เป็น public API ที่เพิ่งถูกกู้ (เคยเรียกฟังก์ชันที่ไม่มี `_compute_col_confidence`).**
   ตอนนี้มีแล้ว แต่ยังไม่ถูกใช้ในเส้นทางหลัก — ควรพิจารณาใช้ `col_confidence` มา gate การดึงรายการ
   (เลี่ยงดึงจากคอลัมน์ที่ไม่มั่นใจ) อย่างเป็นทางการ.
3. **`merge_continuation_bills` ใช้ "ลำดับต่อเนื่อง" เป็นเงื่อนไขรวม.** ตอนนี้ทำงานถูกเมื่อ BUG-1/2 หาย
   แต่ยังเปราะถ้าหน้าต่อมี IV ตรงกันแต่ลำดับรีเซ็ตเป็น 1 ใหม่ — ควรเสริม heuristic (เช่น หน้าต่อที่ไม่มี header บริษัท).
4. **รายงานสรุป "ภาษาคน" (คำขอหลักในเอกสารตัวอย่าง) — ✅ ได้ไฟเขียวแล้ว → ทำเสร็จ/ส่งมอบ (v9.2).**
   ดูหัวข้อ **"ฟีเจอร์: รายงานลูกค้ารายผู้ขาย (ภาษาคน)"** ด้านล่าง. เป็น output เสริม (advisory/read-only)
   จึง **ไม่กระทบ golden hash** (`73f5bf87…` คงเดิม หลังเปิดใช้).

---

## ฟีเจอร์: รายงานลูกค้ารายผู้ขาย (ภาษาคน) — v9.2 (อนุมัติแล้ว)

**สิ่งที่ทำ:** ยกเครื่อง `agents/vendor_report.py` ให้ผลิตรายงาน `.txt` ต่อ **ผู้ขาย × เดือน**
ตรงตามเอกสารตัวอย่าง — ก๊อปส่งลูกค้าได้ทันที (1 ไฟล์/ผู้ขาย, เรียงยอดก่อน VAT มาก→น้อย).
รันโดย `VendorReportAgent` (advisory/read-only) → เขียนข้าง Excel **ไม่แตะ bills/ผลตรวจหลัก/golden**.

**ฟอร์แมต (ตรงสเปกเอกสาร):**
- หัว: `{ลำดับ}.{ชื่อย่อ} {YY.MM}`
- `ยอด : {รวม subtotal ก่อน VAT} บาท ตรง` — **ย้ำ: ก่อน VAT** (เดิมใช้ total = หลัง VAT → แก้แล้ว);
  ถ้าผลรวมรายการ ≠ subtotal ในบางบิล → `ยอด : ยอดเงินในบิลรวม X บาท ยอด Diff Y บาท`
- `บิล : {n} บิล ตรง` — **นับเป็น "บิล" (รวมหน้าต่อแล้ว) ไม่ใช่ "ใบ/ชีต"**; พบ IV ซ้ำ → ทักท้วง
- 10 ช่อง (ชื่อบจ./ที่อยู่/เลขภาษี/สาขา/เลขที่/วันที่/เลขที่ iv/รายการสินค้า/ยอดหลัง-ก่อน VAT):
  `ตรง` หรือ `ไม่ตรง — {อธิบายภาษาคน: ไฟล์ X วันที่ Y: ...} รีเช็คครับ` (อ้างไฟล์/วันที่/ลำดับรายการ/หน่วย จาก `detail` ของกฎ; ไม่มีโค้ดกฎหลุด)
- ท้าย: `{ชื่อเต็ม} {MM/YY} {สรุปภาษาคน}` → `ตรงครับ` หรือ `รีเช็ค {หัวข้อที่ผิด}และหมายเหตุ นะครับผม`
- `หมายเหตุ :` (เฉพาะเมื่อ ที่อยู่/เลขภาษี ไม่ตรง) — โชว์ **ค่า Master data vs ค่าในบิล + จุดต่าง**

**จุดที่ปรับให้ทนจริง:**
- จัดกลุ่มผู้ขายด้วย **ชื่อบริษัท (normalize)** แทนเลขภาษี → ผู้ขายที่ "เลขภาษีพิมพ์ผิดบางบิล"
  จะไม่ถูกแยกออกเป็นหลายรายงาน (เลขภาษีคือสิ่งที่กำลังจะรายงาน)
- จับคู่ Master ด้วยเลขภาษีที่พบบ่อยสุดในกลุ่ม แล้ว fallback เป็นชื่อ → ทนบิลเดียวที่เลขผิด
- deterministic 100% (เรียง/ยุบรายละเอียดคงที่) — รันซ้ำได้ผลเท่ากันเป๊ะ

**ข้อจำกัดที่ต้องรู้:** ช่อง ชื่อบจ./ที่อยู่/เลขภาษี/สาขา จะตรวจ "ตรง/ไม่ตรง" ได้ก็ต่อเมื่อ
ผู้ขายนั้น **มีใน `master_companies.json`** (ตอนนี้มี 1 ราย); ผู้ขายที่ยังไม่ใส่ master จะแสดง "ตรง"
(เพราะไม่มีตัวเทียบ) — เป็นไปตาม workflow ที่ลูกค้าใส่ข้อมูล master ทีละผู้ขายก่อนตรวจ.

**ผลตรวจจริง (corpus 106 ไฟล์):** 834 บิล → 62 รายงาน (ผู้ขาย×เดือน), ไม่ crash, golden คง `73f5bf87…`.
เทส `test_vendor_report.py` ตรึงฟอร์แมต 34 เช็ค (เดิม 19 + ใหม่ 15) — ผ่านครบ; wired ใน CI ขั้น `[4b]`.

---

## รอบแก้ตามที่ลูกค้ารายงาน (3 ไฟล์ KRR/STC/TKH 69.05) — "รายงานเด้ง false-positive"

**อาการที่แจ้ง:** บิลที่จริง ๆ ถูก กลับโดนรายงานว่าผิดเยอะ ("เหมือนผิดหมด").

**สิ่งที่พบเมื่อรันโค้ด v9.2 บน 3 ไฟล์นี้ (forensic):**
- บิล AKT `CB69050671` (TKH): ผลรวมรายการ = subtotal = **242,955 เป๊ะ** → **double-count หายแล้ว** (485,910 = ค่าโค้ดเก่า) ✅ แก้ตั้งแต่ BUG-1/2
- ยอดตัวอักษรไทย ("สองแสนห้าหมื่น…" = total 259,9xx) **ไม่ถูกนับเป็นรายการ** ✅ BUG-3
- ราคา/หน่วย 3.00 (ITM008 "0.0×") **ไม่เด้ง** ✅ BUG-4 (มาจาก `r_itm008` ที่เดียว ไม่มี agent ทำเอง)
- **ต้นเหตุที่เหลือ = noise INFO 25 ตัว** ที่รายงานโชว์หมด: `ITM005` (เดาหน่วย "อาจไม่เหมาะ [soft]") + `ITM004` (เว้นวรรคไทย-อังกฤษ/อักขระแปลก).

**แก้ 3 จุด (surgical):**
1. **ตัวกรอง noise ในรายงานลูกค้า** (`agents/vendor_report._is_noise_issue`) — ตัด INFO/soft เชิงรูปแบบ
   (เดาหน่วย/เว้นวรรค/อักขระแปลก) + การเดาคำผิดด้วย fuzzy พจนานุกรม (X ใกล้เคียง Y ~96%) ที่ไม่แน่นอน
   กับศัพท์เทคนิค/ทับศัพท์ ออกจาก `.txt`. *advisory → ไม่กระทบ golden.*
2. **จับ "HAI UNION → THAI UNION"** — เพิ่ม pattern `(?<![A-Za-z])HAI␣+UNION` ใน `config.SPELLING_PATTERNS`
   (lookbehind กัน match ใน "THAI UNION" เอง → false-positive = 0). *corpus ไม่มี "HAI UNION" → **golden ไม่ขยับ**.*
3. **สำนวน "ภาษาคน" ของช่องรายการสินค้า/ฟิลด์** — เขียนใหม่ที่ชั้นรายงาน (golden-safe) ตามที่ลูกค้ากำหนด:
   `STC วันที่ 11.05.2026 รายการสินค้า ลำดับที่ 2 คำว่า HAI UNION รีเช็คครับ` (prefix สั้น + วันที่จุด +
   "ลำดับที่ N" + "คำว่า {คำผิด}" ที่ดึงด้วย fuzzy จากชื่อรายการ ไม่ hardcode ; ตัดสำนวนเครื่อง "(สะกดตก T)"/ชื่อยาวออก).

**ผลลัพธ์:** 3 ไฟล์ (15 บิล) รายงานสะอาด — ทุกช่อง "ตรง" เหลือเฉพาะ HAI UNION จริง 2 จุด (noise 25 หาย).
**golden คง `73f5bf87…`** + CI ผ่านครบ. **Regression:** `test_realcases_v9_2.py` (16 เช็ค) + 3 ไฟล์ใน
`tests/real_cases/` (self-contained) — wired CI `[4c]`.

**ปรับสำนวนเพิ่มตามที่ลูกค้ากำกับ (รอบ 2 — ทั้งหมดที่ชั้นรายงาน golden-safe):**
- **1 รายงาน/ผู้ขาย (รวมทุกเดือน)** ; หัวแสดง **ช่วงเดือน** เช่น `69.01-02` (เดือนเดียว = `69.05`) ; ท้าย `M/YY`.
- ช่องรายการสินค้า/ฟิลด์อ้างอิงด้วย **"ไฟล์ {prefix} วันที่ {วัน.เดือน.ปี}"** ; ช่อง "วันที่" ไม่ใส่วันที่ซ้ำ.
- **สรุปท้ายซอฟ**: ตรงหมด→`ตรงครับ` ; มีจุด+หมายเหตุ→`รีเช็ค{ฟิลด์}และแก้ไขตามหมายเหตุนะครับผม` ;
  มีจุด ไม่มีหมายเหตุ→`รีเช็ค{ฟิลด์}ครับ ที่เหลือตรงครับผม` (เลิกใช้คำแข็ง "จุดต้องแก้").
- เทส `test_vendor_report.py` = 47 เช็ค.

**ปรับเพิ่มตามที่ลูกค้ากำกับ (รอบ 3 — ทั้งหมดที่ชั้นรายงาน golden-safe):**
- **ยอด "ก่อน VAT" แบบทนทาน** (`_pre_vat`): ปกติใช้ subtotal ; แต่ถ้าบาง layout เก็บ "ยอดรวม VAT"
  ใส่ช่อง subtotal (subtotal ≈ total ทั้งที่มี vat แยก) → derive `total − vat` ไม่ให้ยอดรวม VAT.
- **หน่วยผิด "ชัดเจน" ให้โชว์** (curated `_DEFINITE_UNIT`): เช่น "เหล็กเพลท" ใช้หน่วย "เส้น" →
  โชว์ "หน่วยควรเป็น แผ่น ไม่ใช่ เส้น" (ITM005 ทั่วไป เช่น เต้ารับ/ชิ้น ยังกรองเป็น noise). เทส = 52 เช็ค.

---

## วิธีตรวจซ้ำ (reproduce)



```bash
export PYTHONHASHSEED=0 PUOPUY_AUDIT_DATE=2026-06-02
python3 test_v9_2_fixes.py                       # 32 เช็ค ของบั๊ก 4 ตัว
python3 test_vendor_report.py                     # 42 เช็ค ฟอร์แมต+ตัวกรอง noise (ภาษาคน)
python3 test_realcases_v9_2.py                    # 13 เช็ค เคสจริง KRR/STC/TKH (false-positive)
bash run_ci.sh /mnt/project                       # CI เต็ม + regression ข้อมูลจริง (ต้องได้ 73f5bf87…)
python3 golden_master.py . out.json /mnt/project  # พิมพ์ 73f5bf87… (deterministic)
python3 verify_parallel.py /mnt/project 8         # serial == parallel
```

## ไฟล์ที่แก้
- `parser_p0.py` — `_dic_find_seq` + `_is_seq_run` (BUG-1) · `_is_thai_amount_words` + `_THAI_NUM_WORD_RE` (BUG-3)
- `parser_p1.py` — `_detect_vat_rows` รู้บริบท (BUG-2)
- `parser_p2.py` — guard บาทตัวอักษรใน `_pb_extract_items` (BUG-3)
- `rules_engine.py` — `RULES['ITM008'].enabled=False` (BUG-4)
- `agents/vendor_report.py` — ยกเครื่องรายงานลูกค้า "ภาษาคน" + ตัวกรอง noise (`_is_noise_issue`) ; เทส `test_vendor_report.py` (42 เช็ค)
- `config.py` — เพิ่ม pattern "HAI UNION → THAI UNION" ใน `SPELLING_PATTERNS` (golden ไม่ขยับ)
- `test_realcases_v9_2.py` (ใหม่) + `tests/real_cases/{KRR,STC,TKH}_69_05.xls` + `run_ci.sh` ขั้น `[4c]`
- `baseline.json` — golden `73f5bf87…` (คงเดิม ; ของเก่าก่อน v9.2 อยู่ใน `_ORIG_BACKUP/`)
- `test_v9_2_fixes.py` (ใหม่) + `run_ci.sh` (เพิ่มขั้น [3l2]) + `README.md` (อัปเดต hash)
