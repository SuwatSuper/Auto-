# รายงานตรวจบั๊กเชิงลึก — เฉพาะกฎที่ "เปิดใช้งาน" (57 กฎ) · ปุ้มปุ้ย v9.3.4

> วันที่: 2026-06-22 · โหมด: **LOCKED (อ่าน/สืบ/reproduce — ไม่แก้)** · ขอบเขตตามที่เจ้าของสั่ง: **เฉพาะกฎ `enabled:True` 57 ตัว** (ข้าม 5 ตัวที่ปิด: DOC002/ITM008/VAT004/BR003/VAT010)
> วิธี: กระจาย 5 สายตรวจขนาน (VAT · TAX/BR · ITM · CMP/ADDR · DOC/IV/DT) อ่าน source จริง + reproduce ทุกจุด · ผู้ตรวจ verify จุดสำคัญเองซ้ำทุกข้อ
> ยืนยันโครงสร้าง: `run_rules` กรอง `if not rule['enabled']: continue` (rules_engine.py:259) → **กฎที่ปิดไม่ทำงานจริง** ✅

---

## 0) สรุปผู้บริหาร

- **ร้ายแรง (CRITICAL กลไก): 2** — แต่ **โอกาสเจอจริงต่ำ–กลาง** (ต้องมี input ผิดปกติเฉพาะ). กระทบกฎเลน "ต้องแก้/CRITICAL"
- **กลาง (MEDIUM): 7** — ส่วนใหญ่เป็น **false-positive ที่หลุดเข้าเลน "ต้องแก้"/รายงานลูกค้า** (กวนสัญญาณ)
- **ต่ำ (LOW): 6** — edge/นอกเส้นทางหลัก/มี filter กันปลายทาง + **1 กฎตายเงียบ (ITM009)**
- **ทั้งหมด golden-neutral บน corpus ปัจจุบัน** (148 ไฟล์/1056 บิล ไม่มีเคสพวกนี้ → ไม่เคยขยับ hash → ไม่เคยถูกจับ)
- **ทุกข้อแตะ "logic กฎ" → golden-sensitive class → ต้อง STOP-AND-ASK + regression 148 ไฟล์ก่อนแก้** (ต่างจาก M-2/M-3/M-4 รอบก่อนที่เป็น non-rule แก้ได้เลย)

> หมายเหตุปรัชญา: ระบบถือ "false-negative อันตรายกว่า false-positive". ในชุดนี้ FN ที่ต้องระวังสุด = VAT007 (ข้อ 1) + TAX008 anti-fraud (ข้อ 2). ที่เหลือเป็น FP (กวน แต่ไม่ซ่อนความจริง)

---

## 1) 🔴 ร้ายแรง (CRITICAL กลไก · reachability ต่ำ–กลาง · ยืนยันด้วยการรันจริงครบ)

### C-1 · VAT006/VAT007 crash เมื่อเงินเป็น inf/NaN → กฎ VAT ถูกข้ามแบบ "เกือบเงียบ" (false-negative) ★ผู้ตรวจ verify end-to-end
- **ตำแหน่ง**: `rules_engine_rules_c.py:57` (VAT006 `_D(tot)/1.07`), `:77` (VAT007 `items_sum <= _D(sub)`)
- **กลไก**: type-gate รับ `float` ทุกตัวรวม `inf/nan` → แต่ `_D(inf)=None` (guard is_finite ที่เพิ่งใส่รอบก่อน) → `None/Decimal` หรือ `Decimal<=None` = **TypeError** ซึ่ง **ไม่ใช่** `ArithmeticError` ที่กฎดักไว้ → หลุดไปโดน `run_rules` (rules_engine.py:261) ดักเป็น `SYS-VAT006/007` แล้วรันต่อ → **VAT007 (CRITICAL เลน "ต้องแก้") ไม่ตรวจบิลนั้นเลย**
- **reachability (ยืนยันแล้ว)**: เส้น parser หลัก (`_cell_to_num`) กัน inf ด้วย `math.isfinite` **แต่เส้น TOR-format scan ไม่กัน** — `parser_p2.py:447/461/472` ใช้ `float(str(v).replace(',',''))` ดิบ. เซลล์เงินที่เป็นข้อความ `"inf"`/`"Infinity"`/`"1e400"` หรือ **ตัวเลขยาว ≥309 หลัก** (เช่น barcode/เลขต่อกันผิด) → `float()` ได้ ±inf/nan. **ผมรัน `_tor_scan_total` จริง → `result['total']=inf`** แล้วส่งเข้า `run_rules` → ผลคืน `[]` (ไม่มี VAT issue) + log `SYS-VAT006`
- **ผลกระทบ**: บิล TOR-format ที่มีเซลล์เงินเพี้ยน → VAT006/VAT007 ถูกปิดเงียบ (มี breadcrumb ใน SYS summary ท้ายรัน = "เกือบเงียบ" ไม่ใช่เงียบสนิท). FN บนกฎ CRITICAL
- **ความรุนแรงจริง**: กลไก CRITICAL (FN บนกฎ must-fix) · โอกาสเจอ **ต่ำ–กลาง** (ต้องมีเซลล์เงินเพี้ยนแบบ non-finite) · เป็น **ความไม่สอดคล้อง** (เส้น parser อื่นกันแล้ว เส้น TOR ไม่กัน)
- **ทิศทางแก้**: gate `_D(...) is not None` (เลียน VAT001 ที่ทำถูกอยู่แล้ว) หรือขยาย except เป็น `(ArithmeticError, TypeError)` + ปิดรูที่ `_tor_scan_*` ด้วย `math.isfinite`. golden-neutral ถ้า corpus ไม่มีเซลล์ non-finite (ควรเช็ก `SYS-VAT006/007` บน 148 ไฟล์ก่อน)
- **หมายเหตุ**: pattern เดียวกัน (`_D()` แล้วคำนวณโดยไม่เช็ก None) ยังอยู่ที่ `rules_a.py:568` (ITM001 qty×price) + `rules_c.py:417` (ITM018 region) **แต่ item qty/price ผ่าน `_cell_to_num` ที่กันแล้ว → ไม่ reachable (defensive)**. เส้น subtotal/total ของ VAT006/007 คือตัวเดียวที่ทะลุ

### C-2 · TAX001/003/005/007/008 — เลขภาษี "เลขไทย ๐-๙" ไม่ถูกแปลงเป็นเลขอารบิก → กฎเลขภาษีเพี้ยนทั้งกลุ่ม ★ผู้ตรวจ verify
- **ตำแหน่ง**: ราก `puopuy_core.py:44` `clean_tax_id` (ใช้ `re.sub(r'\D','')` + `.isdigit()` แบบ Unicode → ยอมรับเลขไทย แต่**ไม่มี map เลขไทย→อารบิกที่ใดในระบบ**)
- **กลไก**: เลขภาษีเขียนด้วยเลขไทย (`๐๑๐๕๕๕๖๐๐๐๐๑๐`) ผ่าน `clean_tax_id` เป็น 13 "หลัก" → TAX001/TAX002 ถือว่า **valid** → แต่การเทียบสตริงทุกจุด (`bt != mt`, `==`) เทียบเลขไทยกับ master อารบิก → **ไม่มีวันตรงกัน**
- **reproduce (ผมรันเอง)**: `clean_tax_id('๐๑๐๕๕๕๖๐๐๐๐๑๐')` = เลขไทยเดิม (len 13, ≠ อารบิก) · `r_tax001` = `[]` (รับว่า valid) · `r_tax003` (เทียบกับ master เลข**เดียวกัน**แบบอารบิก) = **`['...ไม่ตรงกับ... — อันตราย']`** (false-positive บนกฎ CRITICAL เลน "ต้องแก้")
- **ผลกระทบ**: **FP** TAX003 (CRITICAL "อันตราย" บนบิลที่เลขถูก) + **FN** TAX005/TAX008 (กฎจับ "สวมเลขภาษี" = anti-fraud → เลขไทยข้ามบิลชื่อต่างกันจับไม่ได้) + TAX007 FN
- **ความไม่สอดคล้อง (ทำให้ relevant)**: `_D()` ของ "เงิน" **รองรับเลขไทย** (`๑๒๓.๔๕`→123.45) + วันที่รับเลขไทยบางรูป → ระบบ "คาดหวังเลขไทย" ที่อื่น แต่เส้นเลขภาษีลืมแปลง
- **ความรุนแรงจริง**: CRITICAL (FP กฎ must-fix + FN กฎ anti-fraud) · โอกาสเจอ ต่ำ–กลาง (เลขภาษีไทยเป็นเลขไทยพบไม่บ่อย แต่ "เป็นไปได้" ในเอกสารราชการไทย)
- **ทิศทางแก้**: normalize เลขไทย→อารบิก ใน `clean_tax_id` (จุดเดียว ครอบทุกกฎ). golden-neutral (corpus 1056 บิลไม่มีเลขไทย — สแกนแล้ว)
- **CLEAN**: TAX006 (checksum) ไม่โดน — `int()` เข้าใจเลขไทย → checksum ตรง (ทดสอบ 300k เคส)

---

## 2) 🟡 กลาง (MEDIUM) — false-positive หลุดเข้าเลน "ต้องแก้"/รายงานลูกค้า (golden-neutral · ยืนยันแล้ว)

| รหัส | ไฟล์:บรรทัด | อาการ (reproduce แล้ว) | เลน | ผู้ตรวจ verify |
|---|---|---|---|---|
| **M·CMP003** | `rules_a.py:64-69` + `config_base.py:478` | brand blacklist เทียบ substring ดิบ → `'CP'`∈"CPF Trading", `'Tops'`∈"Laptops" → ฟ้อง "พบชื่อแบรนด์" ผิด | ERROR/ต้องแก้ | ✅ |
| **M·BR001/BR004** | `rules_a.py:359, :382` | label สาขามีคำว่า "สำนัก" (เช่น "สาขา สำนักงานพระราม9") → ถือเป็นสนญ. → ฟ้อง "ควร 00000" ผิด ทั้งที่ branch_no=00007 ถูก | ERROR/ต้องแก้ | ✅ |
| **M·ITM016** | `rules_c.py:358-362` | dedup key = (ชื่อ,หน่วย,ราคา/หน่วย) **ไม่รวม qty/amount** → รายการแยกจริง (ปูน 10 ถุง vs 5 ถุง ราคาเท่ากัน) ถูกฟ้อง "รายการซ้ำ" · **corpus จริง: 4 บิลที่ ITM016 ฟ้อง = FP ทั้งหมด** | WARNING/ต้องแก้ (ถึงลูกค้า) | ✅ |
| **M·DOC001** | `rules_a.py:419` | ไม่เช็กช่วงวัน 1–31 → ชื่อชีตเลข "32"/"100"/"2026" ถูกตีเป็น "วัน" แล้วฟ้องไม่ตรง iv_date (เมื่อมีชีต day-named ≥2). เส้นพี่น้อง `validators.py:515` มี guard `1<=dd<=31` แล้ว แต่เส้นนี้ไม่มี | ERROR/ต้องแก้ | ✅ source |
| **M·DT004** | `validators.py:343` | เลขรัน 4 หลัก**มี prefix** (เช่น `PO2501`=PO#2501) ถูกแยกเป็น ปี25/เดือน01 → ฟ้อง "วันที่ขัดเลข IV" ผิด (คนละเคสกับ L-1 ที่เป็นเลขล้วน ≥5 หลัก) | WARNING/ต้องแก้ | ✅ agent |
| **M·ADDR002** | `rules_a.py:263` | regex ถนนคว้า 1 token แต่ master (`parse_address_input`) คว้า 2 token → "พระราม4" vs "พระราม 4" → fuzz 93% → ฟ้อง "อาจสะกดผิด" ผิด (ADDR001 strip space แล้วแต่ ADDR002 ไม่) | WARNING/ดูตรวจตา | ✅ |
| **M·ADDR006** | `thai_postal.py:136` | ใช้ `zips[0]` (= บ้านเลขที่) เป็น "รหัสไปรษณีย์" ในข้อความ ทั้งที่ ADDR005 แก้เป็น `zips[-1]` แล้ว → คำเตือนชี้เลขผิด (คำตัดสินถูก แต่ข้อความหลอก) | WARNING/ดูตรวจตา | ✅ agent |

---

## 3) ⚪ ต่ำ (LOW)

| รหัส | ไฟล์:บรรทัด | อาการ | หมายเหตุ |
|---|---|---|---|
| L·ADDR006 (FN) | `thai_postal.py:130` | บ้านเลขที่ 5 หลักที่ขึ้นต้นตรง prefix จังหวัด → บัง zip ผิดจริงไว้ (ไม่ฟ้อง) | FN (philosophy ยอมรับ) golden-neutral |
| L·ADDR005 (FP) | `rules_c.py:143` | เลข 5 หลักท้าย (เบอร์โทร/ตู้ ปณ.) ถูกเลือกเป็นไปรษณีย์ → ฟ้องผิด | INFO/ดูตรวจตา (blast ต่ำ) |
| L·DOC003 (FP) | `rules_c.py:290` | บิลไม่มีวันที่ 2 ใบ (None==None) → ข้าม guard "รีเซ็ตเลขต่องวด" → ฟ้องซ้ำ | แคบ (ต้องวันที่อ่านไม่ได้ทั้งคู่) |
| L·ITM004 (FP) | `rules_b.py:104` + `config:258` | เลขติดอักษรไทย ("5นิ้ว") ถูกป้ายเป็น "อังกฤษ+ไทยติดกัน" — **269 เคสใน corpus** | INFO + ถูก noise-filter ตัดในรายงานลูกค้า (`vendor_report_base.py:244`) → ไม่ถึงลูกค้า |
| L·ITM010 (FP) | `config` THAI_TYPO_PATTERNS | "วาว" (ประกายวาว) → เดาเป็น "วาล์ว" | 0 เคสใน corpus (theoretical) |
| **L·ITM009 (dead)** | `rules_b.py` | **กฎเปิดอยู่แต่ตายเงียบ** — `product_master.json` ไม่มีในแพ็ก → `PRODUCT_MASTER` ว่าง → คืน `[]` เสมอ | กฎ enabled ที่ไม่ทำงานจริง (ควรรู้ไว้) |

---

## 4) ✅ ที่ตรวจแล้ว "สะอาด" (ยืนยัน)

- **โครงสร้าง**: `enabled` flag กรองจริง (กฎปิดไม่รัน) · per-rule try/except แยก crash · checksum เลขภาษี (TAX006) ถูก 100%
- **VAT**: VAT001/002/003/005/008/009 ตรรกะถูก (tolerance boundary, Decimal+HALF_UP, สูตร 7%, ขอบ 0.50)
- **DATE (สำคัญต่อ 5 ปี)**: **DT002 future-date CLEAN** — เดินตาม `audit_today()` ถูก ไม่ฟ้องบิลปีปัจจุบันที่ audit 2026/2028/2031 · DT001/DT004(range)/IV001/IV007 CLEAN → **ยืนยันซ้ำว่า DT003 (M-1) คือระเบิดเวลาวันที่ "ตัวเดียว" ในกรอบ 5 ปี**
- **ITM**: 13/18 กฎ CLEAN (seq/qty×price/unit/typo ส่วนใหญ่ถูก)
- **CMP/ADDR**: CMP001/002/004/005/006 + ADDR001/003/004 CLEAN

---

## 5) ข้อเสนอ (เคารพ LOCKED — ยังไม่แตะ)

> **ต่างจากรอบก่อน:** M-2/M-3/M-4 เป็น non-rule (golden-neutral แท้) จึงแก้ได้หลังอนุมัติ.
> **ชุดนี้ทั้งหมดแตะ "logic กฎ"** → แม้ golden-neutral บน corpus ปัจจุบัน ก็ต้อง **STOP-AND-ASK + regression 148 ไฟล์ (Python 3.12) ยืนยัน `ae84d3f0` ก่อน** ตามวินัย §3/§7.1

**ลำดับแนะนำ (ถ้าจะให้แก้ ผมจะ simulate delta + 1 ADR ต่อจุด ก่อนแตะโค้ดเสมอ):**
1. **C-2 (เลขไทย→อารบิก ใน clean_tax_id)** — จุดเดียว ครอบ TAX ทั้งกลุ่ม, น่าจะ golden-neutral สูง
2. **C-1 (VAT006/007 + `_tor_scan_*` กัน non-finite)** — ปิด FN บนกฎ CRITICAL
3. **M·ITM016 / M·CMP003 / M·BR001/004 / M·DOC001 / M·DT004** — FP เข้าเลน "ต้องแก้" (กวนลูกค้า)
4. **ADDR002/006 + LOW** — เก็บกวาด
5. **ITM009 dead** — ตัดสินใจ: ใส่ product_master.json หรือปิดกฎ (ตอนนี้ enabled แต่ไม่ทำงาน)

### สรุป 1 บรรทัด
**กฎที่เปิด 57 ตัว: ไม่มีบั๊กที่ขยับ golden · แต่เจอ 2 CRITICAL-กลไก (VAT non-finite FN, เลขภาษีไทยเพี้ยน) + 7 MEDIUM FP เข้าเลนต้องแก้ + 6 LOW (รวม ITM009 กฎตาย) · ทั้งหมด golden-neutral บน corpus → แตะ logic กฎ ต้องอนุมัติ + regression ก่อนแก้.**
